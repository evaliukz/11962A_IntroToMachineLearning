# LoRA 论文笔记（LoRA: Low-Rank Adaptation of Large Language Models）

chrome-extension://efaidnbmnnnibpcajpcglclefindmkaj/https://arxiv.org/pdf/2106.09685

---

# 一、这篇论文到底想解决什么问题？

论文作者首先提出了一个非常现实的问题：假设微软训练了一个 GPT-3（175B 参数）。

现在：

- 法律部门想要一个法律助手；
- 医疗部门想要一个医疗助手；
- 财务部门想要一个财务助手。

传统做法（Full Fine-tuning）就是：每个部门都复制一份 GPT-3，然后训练自己的模型。

例如：

```
175B GPT
        │
 ┌──────┼──────┐
 │      │      │
法律GPT 医疗GPT 财务GPT
```

那么每一个模型都是：175B 参数

假设：训练了 100 个不同任务。就需要：175B × 100，几乎无法部署。

因此：论文提出一个问题：**有没有办法只训练一点点参数，而不是重新训练整个 GPT？**

---

# 二、论文最核心的发现

LoRA 的核心思想只有一句话： **微调并不是重新学习整个模型，而只是对原模型进行一个很小的修改。**

作者认为训练结束以后，真正变化的是：ΔW 而不是：W，

例如：原来的权重：W，经过 Fine-tuning，变成：W' 其实 W'=W+ΔW

真正学到的是：ΔW

论文提出：**既然真正变化的是 ΔW，那为什么不直接学习 ΔW？** 这就是 LoRA 的出发点。

---

# 三、为什么 ΔW 可以很小？

这是整篇论文最重要的思想。

作者发现，虽然 GPT 有几百亿参数。但是：真正为了完成一个新任务：例如：英文 -> 法律英文

其实：不用修改整个模型。很多参数根本不用动。真正需要改变的方向：非常少。作者把这种现象叫：Low Rank

也就是说：真正需要学习的信息，远远少于，模型拥有的信息。

举个例子。假设：有一家五星级酒店。现在：准备举办婚礼。

是不是不用：拆掉酒店重新盖。只需要：换桌布，摆鲜花，换灯光。酒店主体：完全不用改。

LoRA 就认为：GPT 也是一样。

---

# 四、什么叫 Low Rank？

很多同学第一次看到：Low Rank都会害怕。

其实：这里只需要理解： **Rank 可以理解为"自由度"或者"表达能力"。**

例如：一个：1000 × 1000矩阵。理论上：可以有：1000 个独立方向。

但是：论文发现Fine-tuning真正修改可能只有：8，16，32几个方向。

因此：不用学习1000 个方向。只学习：16 个方向。就够了。

这就是：Low Rank

---

# 五、LoRA 怎么做到的？

论文提出不要直接学习ΔW，而是把ΔW拆成：两个很小矩阵。 ΔW = BA

于是整个权重变成：W'=W+BA

其中：W冻结,真正训练只有A,B两个矩阵。

因此训练参数一下减少很多。

---

# 六、为什么 B,A 比 ΔW 小？

假设原来的：Linear Layer：4096 -> 4096, Weight大小：4096 × 4096, 大约：1600 万参数

如果LoRA选择Rank = 8, 那么只需要：

```
A

↓

8 × 4096


B

↓

4096 × 8
```

总参数变成：8×4096 + 4096×8 ≈ 65000

是不是一下从1600 万 ->6 万, 少了几百倍。

所以LoRA训练特别便宜。

---

# 七、为什么推理不会变慢？

很多Adapter 方法都会增加新的Layer。推理必须经过更多网络。所以：Inference变慢。

LoRA不会。因为训练结束可以直接把：BA + W 合并得到 W'

于是真正推理还是一个：矩阵。

因此： **LoRA 几乎不会增加推理延迟（No Additional Inference Latency）。**

---

# 八、为什么论文初始化 B=0？

论文有一个很巧妙的小设计。初始化：A 随机, B = 0

于是开始训练第一步：BA=0, 所以：W'=W 模型一开始和原模型完全一样。

训练过程中：BA慢慢长出来。因此：LoRA不会破坏原模型。训练也更加稳定。

---

# 九、为什么只训练 Attention？

论文实验发现：真正最重要的是：Attention。尤其：Wq, Wv

因此原论文只训练：Query Projection, Value Projection

后来：QLoRA、Llama Factory：越来越激进。通常都会训练：Q, K, V, O, MLP

但是：Embedding, LayerNorm 通常仍然冻结。

---

# 十、论文实验结果

论文最大的贡献不是提出LoRA。而是证明LoRA：效果几乎和Full Fine-tuning：一样。

例如：GPT-3、RoBERTa、DeBERTa 很多BenchmarkLoRA：甚至略好。

与此同时LoRA还能：

- 可训练参数减少约 **10,000 倍**
- GPU 显存需求减少约 **3 倍**
- 推理速度几乎不变
- 可以为不同任务保存多个很小的 Adapter，而不用复制整个模型。:contentReference[oaicite:4]{index=4}

---

# 十一、LoRA 为什么能成功？

论文最后真正告诉我们的思想不是BA, 而是：**Fine-tuning 真正需要学习的信息远远少于模型本身拥有的信息。**

换句话说：一个已经 Pretrain 完的大模型已经拥有99% 的知识。

Fine-tuning只是告诉它：以后回答更像律师。或者：以后回答：更像医生。

并不是重新学习语言。

因此真正需要修改只有很小一部分。

LoRA正是利用这一点。

---

# 本章重点

- LoRA 解决的是 **Full Fine-tuning 成本过高** 的问题。
- LoRA 冻结原模型参数，只学习一个很小的权重更新（ΔW）。
- LoRA 将权重更新表示为两个低秩矩阵：

W' = W + BA

其中：

- W 为冻结的预训练参数；
- BA 为需要学习的低秩更新。

- Low Rank 的核心思想是：Fine-tuning 真正需要修改的方向远远少于模型参数数量，因此不需要更新整个矩阵。
- LoRA 的训练参数从 \(dk\) 降低到 \(r(d+k)\)，其中 \(r \ll d,k\)，因此能够大幅降低显存和训练成本。
- 推理时可以将 BA 合并回 W，因此不会增加额外推理延迟。
- 原论文主要在 Attention 的 Query 和 Value Projection 上应用 LoRA，现代 QLoRA 则通常扩展到更多 Linear Layer。
- LoRA 在 GPT-2、GPT-3、RoBERTa、DeBERTa 等模型上都取得了与 Full Fine-tuning 相当甚至更好的性能，同时训练参数减少约 10,000 倍，GPU 显存需求降低约 3 倍。:contentReference[oaicite:5]{index=5}

---

# 一句话总结

> **LoRA 的核心思想是：一个已经训练好的大模型在适应新任务时，并不需要重新学习整个模型，而只需要学习一个很小的权重修改（ΔW）。LoRA 将这个修改表示成两个低秩矩阵 BA，只训练这两个矩阵，同时冻结原模型，从而在几乎不损失性能的情况下，大幅降低训练参数、显存需求和部署成本。**
