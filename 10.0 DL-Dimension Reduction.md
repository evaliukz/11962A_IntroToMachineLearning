# PCA and Other Dimensionality Reduction Techniques（大白话理解）

## 一、为什么需要 Dimensionality Reduction（降维）？

现实中的数据往往维度非常高。例如：

- 一张 224×224×3 的图片有 **150,528 个特征**
- GPT 的 embedding 通常有 **768、1024、4096 维**
- 一个人的医疗数据可能有几千个指标

高维数据会带来很多问题：

- 计算速度慢
- 存储成本高
- 很难可视化（人只能看到 2D 或 3D）
- 有很多重复信息（冗余）

因此我们希望： **用更少的维度表示尽可能多的信息。**

例如：1000维 -> 50维 -> 2维（方便画图）

这就是 **Dimensionality Reduction（降维）**。

---

# 二、降维主要分成两类

## 1. Parametric（参数化）

特点：模型学习的是一个**函数（Function）**。

例如：f(x)=embedding

以后来了任何新的数据：New x -> 同一个函数 -> 新的 embedding

所以：一个模型可以处理所有数据。

例如：

- Neural Network
- AutoEncoder
- Transformer Embedding

优点：

- 可以处理新的数据
- 推理速度快
- 信息保留较好
- 可以部署上线

缺点：模型能力受到参数限制。

---

## 2. Nonparametric（非参数化）

这里没有一个统一的函数。而是：每一个数据点都有自己的坐标。

例如：1000个点：

```
Point1 → (1.2,0.3)

Point2 → (-0.5,2.1)

Point3 → ...
```

来了一个新的点：不能直接知道它的位置。必须重新计算整个 embedding。

例如：

- t-SNE
- UMAP（严格来说近似非参数）

优点：非常适合可视化。

缺点：不能直接处理新的数据。

---

# 三、PCA（Principal Component Analysis）

PCA 是最经典的降维算法。一句话： **寻找数据变化最大的方向。**

## 为什么找变化最大的方向？

假设二维数据：

```
        ●

      ●

    ●

  ●

●
```

明显数据沿着一条斜线分布。

如果压缩成一维,最好的方法不是：↓ 竖着投影

也不是：→ 横着投影

而是沿着：＼ 投影。因为这样损失最少。

这个方向就叫：**Principal Component（主成分）**。

---

# 四、PCA 的目标

老师写：

$$
\arg\max_{\|w\|=1}\|Xw\|^2
= \arg\max_{\|w\|=1}w^TX^TXw
$$

很多人第一次看到就懵。其实一句话： **找到一个方向 w，使得所有数据投影以后，方差最大。**

为什么？因为：方差越大说明：数据分得越开。信息越丰富。

如果所有点都投影成：1111 方差：0。信息全没了。

所以我们希望投影以后还能尽量分散。

---

# 五、Projection（投影）

老师写：z=Xw 什么意思？

假设每个数据：x_i 都有100维。

我们找到一个方向：w 然后每个数据都投影过去。

于是原来：100维变成：1维

这个新的值就是：z_i

全部组成：z=Xw

---

# 六、为什么是 Variance？

PCA 的核心就是：最大化：Var(z) 投影后的方差

老师推导：

```
Var(z)

↓

1/n z^Tz

↓

1/n (Xw)^T(Xw)

↓

1/n w^TX^TXw
```

最后变成：

$$
w^TX^TXw
$$

所以PCA就是最大化这个量。其实就是寻找方差最大的方向。

---

# 七、PCA 和 SVD

老师最后说PCA通常不用自己求：Eigenvalue。

为什么？因为直接做SVD。假设数据：X

SVD分解：X = UΣV^T

这里：V里面第一列就是第一主成分。第二列第二主成分。……

所以很多Python库实际上都是：PCA -> SVD

例如：sklearn.decomposition.PCA

底层就是SVD。

---

# 八、PCA 的优缺点

优点：

- 非常快
- 数学简单
- 可解释性强
- 保留整体结构（Global Structure）
- 常用于预处理

缺点：只能发现**线性关系。**

例如：○ 这种圆环PCA展开不了。

---

# 九、t-SNE

一句话：

> **保持邻居关系。**

例如：原来A B C很近

降维以后仍然希望：A B C很近

至于整个地图旋转、拉伸、翻转：都没关系。

所以：t-SNE特别适合画Cluster。

---

优点：

- 聚类特别漂亮
- 很适合论文画图

缺点：

- 很慢
- 全局距离容易失真
- 每次结果可能不同
- 很难处理百万数据

---

# 十、UMAP

UMAP可以理解成： **t-SNE 的升级版。**

它也保持局部结构。但是还尽量保持整体结构。而且：更快。

更适合：大数据。

现在很多Embedding可视化：都是UMAP。

---

# 十一、PCA vs t-SNE vs UMAP

<img width="1220" height="744" alt="image" src="https://github.com/user-attachments/assets/30208c36-d2b9-4385-bb6c-9bf578a8819f" />


| 方法 | 类型 | 保留什么 | 优点 | 缺点 | 常见用途 |
|------|------|---------|------|------|---------|
| PCA | Linear | Global Variance | 快、稳定、可解释 | 不能处理非线性 | 降维、去噪、预处理 |
| t-SNE | Nonlinear | Local Neighborhood | 聚类漂亮 | 很慢，全局结构差 | 数据可视化 |
| UMAP | Nonlinear | Local + 部分 Global | 快、可扩展 | 参数稍复杂 | Embedding 可视化、大规模数据 |

---

# 十二、什么时候用？

如果想减少Feature数量：例如：1000维 -> 100维, 通常用：**PCA**

如果想论文画图：768维 -> 2维,通常用：**t-SNE**

如果数据很多：例如100万Embedding：通常用**UMAP**


# 十三、一句话总结

> **PCA 找的是“方差最大的方向”，属于线性降维，适合数据压缩和预处理；t-SNE 专注于保持局部邻居关系，适合聚类可视化；UMAP 在保持局部结构的同时尽量保留全局结构，速度更快、扩展性更好，因此成为现代机器学习中最常用的高维 Embedding 可视化方法。**


# PCA 从数学到代码（为什么这么写？）

## 一、回顾 PCA 的目标

PCA 的目标是： **找到一个方向 \(w\)，使数据投影后的方差最大。**

数学表达式：

$$
\arg\max_{\|w\|=1}\|Xw\|^2
=\arg\max_{\|w\|=1}w^TX^TXw
$$

这里：

- \(X\)：数据矩阵（n 个样本，每个样本 d 个特征）
- \(w\)：寻找的投影方向（Principal Component）
- \(Xw\)：所有数据投影后的新坐标

一句话： **PCA 就是在所有可能方向中，寻找能够保留最多信息（最大方差）的方向。**

---

# 二、为什么开始要计算 Covariance Matrix？

代码：

```python
cov = np.cov(X.T)
```

为什么？因为 PCA 不是直接在数据 X 上找方向，而是在： **Covariance Matrix（协方差矩阵）** 上找方向。

---

假设：X shape (100,2)100 个样本 2 个特征

例如：

```
Height

Weight
```

协方差矩阵就是：

```
          Height   Weight

Height      Var      Cov

Weight      Cov      Var
```

写成矩阵：

$$
\Sigma=
\begin{bmatrix}
Var(x_1)&Cov(x_1,x_2)\\
Cov(x_2,x_1)&Var(x_2)
\end{bmatrix}
$$

它描述： **每个 Feature 的变化大小，以及 Feature 之间如何一起变化。**

为什么：X.T？

因为：np.cov

默认认为：每一行 -> 一个 Feature

但是：机器学习通常：

```
每一列

↓

一个 Feature
```

所以要转置。

---

# 三、为什么要求 Eigenvalue 和 Eigenvector？

代码：

```python
eigenvalues, eigenvectors = np.linalg.eig(cov)
```

这里发生的是：老师前面的数学：

$$
\arg\max_{\|w\|=1}
w^TX^TXw
$$

可以证明最后等价于：求协方差矩阵

$$
\Sigma
```

的：

```
Eigenvectors
```

和：

```
Eigenvalues
```

所以PCA其实就是Eigen Decomposition。

---

## Eigenvector 是什么？

可以理解成：**数据最重要的几个方向。**

例如数据：＼

最大的变化方向就是：＼

这个方向就是第一Eigenvector。

---

## Eigenvalue 是什么？

可以理解成 **这个方向的重要程度。**

例如：Eigenvalue=5.24

说明这个方向变化很大。

如果：0.03说明几乎没有信息。

---

# 四、为什么选最大的 Eigenvalue？

代码：

```python
first_principal_component =
eigenvectors[:, np.argmax(eigenvalues)]
```

这句非常经典。


第一步：找到最大的Eigenvalue。

例如：

```python
eigenvalues

[5.24,
0.03]
```

最大的就是：5.24

于是：

```python
np.argmax(eigenvalues)
```

得到：0


第二步：取对应Eigenvector。

假设：

```
eigenvectors

[[0.70,-0.71],

 [0.71, 0.70]]
```

第一列：

```
0.70

0.71
```

就是第一主成分。

所以：

```
eigenvectors[:,0]
```

得到：

```
[0.70,
0.71]
```

---

为什么？因为最大的Eigenvalue：对应最大的Variance。

所以它就是PCA第一方向。



---

# 五、为什么第二个 Principal Component 一定垂直？

老师说：

```
The second component will be orthogonal.
```

原因：协方差矩阵一定是：Symmetric Matrix。

数学证明：Symmetric Matrix不同：

Eigenvectors：一定互相Orthogonal。

所以：第一主成分：＼ 第二就是：／ 两个90°

图中长的是第一主成分，短的垂直是第二主成分。

<img width="387" height="371" alt="image" src="https://github.com/user-attachments/assets/329d4f56-fc83-41b5-8c27-7a8728fcb9de" />

在这张图里，红色箭头本身就是 Eigenvectors（特征向量）！

具体来说，以 A 图 为例：

第一根长箭头 = 第一个 Eigenvector (v₁) = 第一主成分 PC1

第二根短箭头 = 第二个 Eigenvector (v₂) = 第二主成分 PC2

为什么 Eigenvector 会变成箭头？

PCA 的步骤是：对数据计算 Covariance Matrix（协方差矩阵）

<img width="182" height="69" alt="image" src="https://github.com/user-attachments/assets/47f496f5-a2ab-41c9-8040-6337bd561f28" />

求它的 Eigenvalues（特征值） 和 Eigenvectors（特征向量）

每个 Eigenvector 给出了一个方向（direction）。

所以图上的红箭头就是这些方向。

蓝点：原始数据

红箭头：Eigenvectors（主成分方向）

箭头长度：对应 Eigenvalue 的大小（Variance）为什么第一根箭头更长？因为：第一主成分解释的数据变化最多。

---

# 六、为什么 sklearn 一句话就能完成？

代码：

```python
pca = PCA(n_components=2)

X_pca = pca.fit_transform(X)
```

其实里面做事情，就是刚才全部过程：

```
计算 Covariance

↓

Eigen Decomposition

↓

找最大的 Eigenvectors

↓

Projection
```

只是帮你封装好了。

---

# 七、为什么 Projection？

最终不是要方向。而是新的数据。

老师计算：X_pca

其实就是：

$$
Z=XW
$$

其中：

```
W=Principal Components
```

于是：

```
原来：

100维

↓

2维
```

---

# 八、为什么总 Variance 不变？

代码：

```python
np.sum(np.diag(np.cov(X.T)))
```

什么意思？

```
diag
```

取协方差矩阵：主对角线。

例如：

```
Cov

[5 1

 1 3]
```

主对角线：

```
5

3
```

加起来：8

其实就是：Total Variance

老师比较：

```
Original

↓

8
```

和：

```
PCA

↓

8
```

发现一样。

为什么？因为：PCA只是旋转坐标轴。没有丢信息。如果保留全部Principal Components。

---

# 九、为什么 PCA 后协方差矩阵变成 Diagonal？

代码：

```python
np.cov(X_pca.T)
```

结果类似：

```
[5.24   0

 0    0.03]
```

为什么？因为：Principal Components彼此Orthogonal。

所以：协方差=0。

意味着新的：Feature互相独立。

---

# 十、Explained Variance

代码：

```python
pca.explained_variance_
```

输出：

例如：

```
[5.24

0.03]
```

其实就是：Eigenvalues。

代表：每个Principal Component贡献多少Variance。

---

代码：

```python
pca.explained_variance_ratio_
```

输出例如：

```
[0.994

0.006]
```

意思：

第一主成分：解释99.4%的信息。

第二：只有0.6%。

所以很多时候只保留第一Principal Component。

---

# 十一、整个 PCA 流程

```
原始数据 X

↓

计算 Covariance Matrix

↓

Eigen Decomposition

↓

Eigenvalues + Eigenvectors

↓

选择最大的 Eigenvalue

↓

得到 Principal Components

↓

Projection

Z=XW

↓

得到低维表示

↓

计算 Explained Variance Ratio

↓

决定保留几个 Principal Components
```

---

# 十二、一句话总结

> **PCA 的本质就是：先计算协方差矩阵，然后求它的特征值和特征向量。最大的特征值对应第一主成分，它表示数据变化最大的方向；对应的特征向量就是投影方向。最后把数据投影到这些主成分上，就得到了新的低维表示，同时利用 explained variance ratio 来衡量每个主成分保留了多少原始信息。**
