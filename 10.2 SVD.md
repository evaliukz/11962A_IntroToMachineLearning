# Singular Value Decomposition (SVD) —— 大白话理解

## 一、为什么需要 SVD？

一句话： **SVD 就是把一个复杂矩阵拆成三个简单矩阵，每个矩阵分别负责不同的工作。**

例如：有一个矩阵 A

它可能表示：

- 用户 × 商品评分矩阵（推荐系统）
- 图片矩阵
- Word Embedding
- Transformer Weight Matrix

这些矩阵都非常复杂。

SVD 告诉我们： **任何矩阵都可以拆成三个矩阵相乘。**

即

$$
A = U\Sigma V^T
$$

所以：SVD 就像把一个复杂动作拆成三个简单动作。

---

# 二、SVD 到底拆成了什么？

公式：

$$
A = U\Sigma V^T
$$

很多人看到这个公式就懵。

其实可以理解成：

```
原始数据

↓

先旋转

↓

再拉伸

↓

再旋转
```

也就是说：

```
V^T

↓

Σ

↓

U
```

三个步骤。

---

## 第一步：Vᵀ（第一次旋转）

V：叫做**Right Singular Vectors**

作用： **找到输入数据最重要的方向。**

例如原始数据：

```
        ●

      ●

    ●

  ●

●
```

V：

把坐标轴旋转到：＼ 这个方向。

注意这里只是旋转。没有拉长。没有缩短。

---

## 第二步：Σ（拉伸）

Σ：叫Singular Values。

它长这样：

$$
\Sigma=
\begin{bmatrix}
\sigma_1&0&0\\
0&\sigma_2&0\\
0&0&\sigma_3
\end{bmatrix}
$$

只有对角线不是0。

为什么？

因为这里只负责沿着各个方向拉伸。

例如：

```
原来：

○

↓

变成：

⬭
```

最大的σ说明这个方向最重要。

如果：σ很小。

说明这个方向几乎没有信息。

---

## 第三步：U（第二次旋转）

最后再旋转一次。得到最终矩阵。

所以整个SVD就是：

```
旋转

↓

拉伸

↓

旋转
```

---

# 三、为什么 U 和 V 都满足

老师写：

$$
UU^T=I
$$

$$
V^TV=I
$$

什么意思？说明：U、V都是：Orthogonal Matrix（正交矩阵）。

正交矩阵只有一个作用：

```
旋转

或者

翻转
```

不会改变长度。

所以真正改变信息大小的是：Σ。

---

# 四、为什么 Σ 要从大到小排列？

老师：

写：

```
σ1 ≥ σ2 ≥ σ3
```

为什么？

因为最大的Singular Value代表最多信息。

例如：

```
100

10

0.1

0
```

说明：第一方向最重要。后面越来越不重要。

所以SVD天然告诉我们哪些方向值得保留。

---

# 五、Low-rank Approximation（为什么 SVD 能压缩数据？）


最重要。假设Σ：

```
100

20

5

0.01

0.0001
```

是不是后面两个几乎没有贡献。于是直接删掉。

变成：

```
100

20

5
```

重新相乘得到新的矩阵：Â

这个就是Low-rank Approximation。

意思： **只保留最重要的信息。**

---

## Eckart–Young–Mirsky Theorem

老师这里说： 最好的 Rank-k Approximation。

什么意思？

例如原来矩阵：1000×1000

现在只能保留：Rank=20。到底怎么保留？

答案就是：保留最大的20个：Singular Values。

数学证明这是：误差最小。没有任何别的方法更好。

所以SVD天然就是最优压缩。

---

# 六、为什么 PCA 用 SVD？

上一章我们讲PCA要求：Covariance, Eigenvectors。

实际上很多库没有真的算Covariance。而是直接SVD。

为什么？因为可以证明：

$$
X=U\Sigma V^T
$$

那么PCA需要Principal Components：其实就是V的列。

第一列：就是第一Principal Component。

第二列：第二Principal Component。

所以很多Python库直接：

```
SVD

↓

PCA
```

比Eigen更稳定。

---

# 七、老师最后一页为什么算 AᵀA？

老师说：

```
Compute Eigenvalues

of

A^TA
```

为什么？假设：

$$
A=U\Sigma V^T
$$

那么：

$$
A^TA
=
V\Sigma^2V^T
$$

于是Eigenvectors就是：V

Eigenvalues就是：σ²

所以先算：A^TA

就得到V。

然后利用公式：

$$
u_i=\frac1{\sigma_i}Av_i
$$

得到U。

所以整个SVD就求出来。

---

# 八、SVD 在 LLM 中为什么这么重要？

老师最后举论文：Representing LLMs in Prompt Semantic Space

其实LLM到处都是：SVD。

例如：

### LoRA

LoRA：其实就是假设Weight Matrix可以Low-rank。

所以只学习：BA而不是整个W。

这本质就是SVD思想。

---

### Embedding Compression

例如：Embedding：768维

可以SVD压缩：64维

---

### Recommendation System

Netflix：MovieLens：评分：矩阵：都是：SVD。

---

### Image Compression

JPEG：很多地方也：利用：Low-rank。

---

### Semantic Analysis

LSA：Latent Semantic Analysis：就是：SVD。

---

# 九、PCA vs SVD

| PCA | SVD |
|------|-----|
| 作用于 Covariance Matrix | 直接作用于原始矩阵 |
| 求 Eigenvectors | 求 Singular Vectors |
| 找最大方差方向 | 找最佳低秩表示 |
| Principal Components | Right Singular Vectors (V) |
| sklearn 底层通常调用 SVD | 数值更稳定 |

---

# 十、一句话总结

> **SVD 可以把任意矩阵拆成 "旋转 → 拉伸 → 再旋转" 三个步骤。U 和 V 是两个旋转矩阵，Σ 包含每个方向的重要程度（Singular Values）。最大的 Singular Values 保留了绝大部分信息，因此只保留前 k 个 Singular Values 就可以得到最优的低秩近似（Low-rank Approximation）。PCA、LoRA、Embedding Compression、推荐系统等大量机器学习算法，本质上都建立在 SVD 的思想之上。**

# 例题

<img width="799" height="393" alt="image" src="https://github.com/user-attachments/assets/5fb5c257-e1cb-41e1-a184-705f8096b541" />

<img width="788" height="290" alt="image" src="https://github.com/user-attachments/assets/8314f2a1-7061-4d81-adf8-b98a24211866" />

<img width="790" height="389" alt="image" src="https://github.com/user-attachments/assets/f7b607d7-eec2-4ca2-ba4f-7a39e09828fd" />

<img width="806" height="375" alt="image" src="https://github.com/user-attachments/assets/7851cf53-f85d-4bc6-8d8d-e116274a30b2" />

<img width="780" height="564" alt="image" src="https://github.com/user-attachments/assets/3091d409-7714-4188-9fcd-1767299a4545" />

<img width="735" height="549" alt="image" src="https://github.com/user-attachments/assets/035f8a7f-b9f0-4723-9d50-ce4851f13fda" />

<img width="798" height="598" alt="image" src="https://github.com/user-attachments/assets/2c782b88-dec7-457c-88ad-9c206f14e28f" />

<img width="810" height="289" alt="image" src="https://github.com/user-attachments/assets/dbf0d452-99ea-40d3-a399-072ba71f4949" />

<img width="804" height="492" alt="image" src="https://github.com/user-attachments/assets/75ed1cf6-46e3-4594-8906-5706f0931c89" />

<img width="779" height="449" alt="image" src="https://github.com/user-attachments/assets/2e126178-8acd-4a0e-a1f7-deb1111a4c20" />



# SVD（Singular Value Decomposition，奇异值分解）

## 1. 什么是 SVD？

对于任意矩阵 \(A\)，都可以分解成：

\[
A = U\Sigma V^T
\]

SVD 的作用是把一个复杂矩阵分解成 **三个更容易理解的矩阵**。

可以把整个过程理解成：

> **先旋转 → 再拉伸 → 再旋转**

```text
原始空间
    │
    ▼
 Vᵀ（第一次旋转）
    │
    ▼
 Σ（沿坐标轴拉伸）
    │
    ▼
 U（第二次旋转）
    │
    ▼
最终得到矩阵 A
```

---

# 2. U、Σ、Vᵀ 分别表示什么？

假设：

\[
A=
\begin{pmatrix}
2&-1&0\\
-2&0&0.4\\
1&-1&1
\end{pmatrix}
\]

NumPy 中：

```python
U, S, Vt = np.linalg.svd(A)
```

---

## （1）U —— 左奇异向量（Left Singular Vectors）

U 表示：

> **输出空间（Output Space）的方向**

例如：

```text
U

u1   u2   u3
│    │    │
│    │    │
```

- 每一列都是一个单位向量（长度为1）
- 每一列叫做 **Left Singular Vector（左奇异向量）**

可以理解成：

> **数据最终被映射到哪些主要方向。**

---

## （2）Σ（S）—— 奇异值（Singular Values）

NumPy 返回的是：

```python
S
```

例如：

```text
[3.18
 1.37
 0.37]
```

数学上真正的 Σ 是：

\[
\Sigma=
\begin{pmatrix}
3.18&0&0\\
0&1.37&0\\
0&0&0.37
\end{pmatrix}
\]

因此：

> **NumPy 返回的是 Σ 的对角线。**

---

### 奇异值表示什么？

表示：

> **每个方向被拉伸了多少倍。**

例如：

```text
方向1

×3.18

方向2

×1.37

方向3

×0.37
```

奇异值越大，

说明这一方向越重要。

---

## （3）Vᵀ —— 右奇异向量（Right Singular Vectors）

Vᵀ 表示：

> **输入空间（Input Space）的方向**

NumPy 返回的是：

```python
Vt
```

如果需要 V：

```python
V = Vt.T
```

每一列都是一个 Right Singular Vector（右奇异向量）。

可以理解成：

> **原始数据最重要的方向。**

---

# 3. SVD 最重要的公式

SVD 还可以写成：

\[
A
=
\sigma_1u_1v_1^T
+
\sigma_2u_2v_2^T
+
\sigma_3u_3v_3^T
\]

这表示：

> **原矩阵 = 多个 Rank-1 Matrix 相加。**

可以把它想象成：

```text
A

=

第一层

+

第二层

+

第三层
```

其中：

```text
第 i 层

=

σᵢ uᵢ vᵢᵀ
```

每一层都是一个 Rank-1 Matrix。

---

# 4. 什么是 Rank-1 Approximation？

只保留最大的奇异值：

\[
A_1
=
\sigma_1u_1v_1^T
\]

称为：

> **Rank-1 Approximation（秩1近似）**

它只保留矩阵中最重要的信息。

可以理解成：

```text
原图

😀

↓

Rank-1

🙂
```

虽然变模糊，

但是保留了主体结构。

---

# 5. 什么是 Rank-2 Approximation？

保留前两个奇异值：

\[
A_2
=
\sigma_1u_1v_1^T
+
\sigma_2u_2v_2^T
\]

可以理解成：

```text
第一层

+

第二层
```

比 Rank-1 更接近原矩阵。

---

# 6. 完整 SVD 重建

保留所有奇异值：

\[
A
=
\sigma_1u_1v_1^T
+
\sigma_2u_2v_2^T
+
\sigma_3u_3v_3^T
\]

最终得到：

> **完全恢复原矩阵。**

---

# 7. 为什么叫 Rank？

因为：

每一项

\[
u_iv_i^T
\]

都是一个 Rank=1 的矩阵。

例如：

\[
u=
\begin{pmatrix}
1\\
2\\
3
\end{pmatrix}
,
\qquad
v^T=
\begin{pmatrix}
4&5&6
\end{pmatrix}
\]

相乘：

\[
uv^T=
\begin{pmatrix}
4&5&6\\
8&10&12\\
12&15&18
\end{pmatrix}
\]

所有行都是第一行的倍数，

因此：

```text
Rank = 1
```

所以：

```text
σ₁u₁v₁ᵀ
```

就是一个 Rank-1 Matrix。

两层：

```text
Rank ≤ 2
```

三层：

```text
Rank ≤ 3
```

---

# 8. 为什么 Rank-1 是最佳近似？

奇异值已经按照大小排序：

\[
\sigma_1
\ge
\sigma_2
\ge
\sigma_3
\]

例如：

```text
σ

10
2
0.1
```

贡献大小：

```text
10   ########################

2    ####

0.1  .
```

因此：

第一项已经保留了最多的信息。

所以：

\[
\sigma_1u_1v_1^T
\]

就是：

> **最佳 Rank-1 Approximation（Eckart–Young 定理）。**

---

# 9. NumPy 实现

```python
import numpy as np

U, S, Vt = np.linalg.svd(A)

# Rank-1 Approximation
A_rank1 = (
    S[0] *
    np.outer(U[:,0], Vt[0,:])
)

# Rank-2 Approximation
A_rank2 = (
    S[0] * np.outer(U[:,0], Vt[0,:])
    +
    S[1] * np.outer(U[:,1], Vt[1,:])
)
```

---

# 10. 面试 / 考试速查表

| 符号 | 含义 |
|------|------|
| **U** | 左奇异向量（输出空间方向） |
| **Σ（S）** | 奇异值（每个方向的重要程度/拉伸倍数） |
| **Vᵀ** | 右奇异向量（输入空间方向） |
| **σ₁u₁v₁ᵀ** | 一个 Rank-1 Matrix |
| **Rank-1 Approximation** | 只保留最大的奇异值 |
| **Rank-2 Approximation** | 保留前两个最大的奇异值 |
| **Full SVD** | 保留全部奇异值，可恢复原矩阵 |

---

# 11. 一张图记住 SVD

```text
                A

                 │
      A = U Σ Vᵀ
                 │

      U        Σ          Vᵀ

 输出方向    拉伸倍数     输入方向

  ↑          ↑           ↑

u1,u2,u3   σ1 σ2 σ3   v1,v2,v3
```

然后：

```text
Rank-1

σ₁u₁v₁ᵀ

↓

只保留最重要的一层


Rank-2

σ₁u₁v₁ᵀ
+
σ₂u₂v₂ᵀ

↓

保留前两层


Full SVD

σ₁u₁v₁ᵀ
+
σ₂u₂v₂ᵀ
+
σ₃u₃v₃ᵀ

↓

恢复原矩阵
```

---

# ⭐ 一句话总结（考试必背）

- **U**：输出空间最重要的方向（左奇异向量）。
- **Σ（S）**：每个方向的重要程度（奇异值）。
- **Vᵀ**：输入空间最重要的方向（右奇异向量）。
- **Rank-1 Approximation**：只保留最大的奇异值及对应方向。
- **Rank-2 Approximation**：保留前两个最大的奇异值及对应方向。
- **完整 SVD**：保留所有奇异值，可完全恢复原矩阵。





