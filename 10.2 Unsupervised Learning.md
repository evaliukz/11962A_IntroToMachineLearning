# CMU ML Week11 —— Unsupervised Learning（学习版）

---

# 1. 什么是 Unsupervised Learning？

监督学习（Supervised Learning）：

```
X  ------>  y
```

数据里已经告诉我们答案（Label）。

例如：

图片

↓

猫 / 狗

模型学习：

```
Image
↓

Cat
```

---

无监督学习（Unsupervised Learning）

没有 Label。

只有

```
X
```

模型必须自己发现：

- 哪些数据相似？
- 数据有没有规律？
- 有没有低维结构？
- 数据到底来自几个分布？

所以：Unsupervised Learning = Discover hidden structure from data.

整个目标就是： Find Structure.

---

## 为什么重要？

现实世界：99%的数据没有标签。

例如：

用户点击

日志

网页

声音

视频

DNA

没人会全部人工标注。

所以监督学习：需要 Label成本高。

无监督学习：不用 Label更容易扩展。

---

# 2. 四大问题

整个课程其实只有四件事情。

---

## 第一类：Clustering

目标：把相似的数据放在一起。

例如：

```
● ● ●

      ▲ ▲ ▲

              ■ ■ ■
```

自动发现：三个群体。

代表算法：

- K-Means
- Hierarchical
- GMM

---

## 第二类：Projection

目标降维。例如：原来10000维, 变成：10维

但是：信息尽量保留。

代表算法：

- PCA
- SVD

---

## 第三类：Density Estimation

目标: 学习P(x)数据真正来自哪个概率分布。

代表：

Gaussian Mixture Model

EM

---

## 第四类：Visualization

目标：方便人看。不是给模型。

代表：

t-SNE

UMAP

---

# 3. 所有算法其实都做同一件事

老师这里讲得很好。

所有算法：都是在寻找一个Compact Representation

也就是压缩后的表示。

原始数据：

```
x
```

↓

Encode

↓

```
z
```

↓

Decode

↓

```
x'
```

如果：

```
x'≈x
```

说明：压缩后的表示保留了主要信息。

不同算法z 不一样。

例如：

K-Means：

```
z

=

Cluster ID
```

例如：

```
Cluster 2
```

---

PCA：

```
z

=

几个主成分
```

---

GMM：

```
z

=

属于各Cluster的概率
```

例如：

```
[0.1

0.8

0.1]
```

---

所以：所有无监督学习其实都在寻找 一个更简单、更紧凑的数据表示。

---

# Part 1 —— Clustering

---

# 4. Clustering 的核心思想

一句话： Similar things belong together.

例如：客户：

横坐标：

```
消费金额
```

纵坐标：

```
来店次数
```

肉眼已经能看到：

```
Budget

Regular

VIP
```

但是：

算法不知道：

这些名字。

它只知道：

```
距离
```

所以：Cluster就是距离近。

---

# 5. K-Means

目标：让每个Cluster尽量紧。

什么意思？例如：

```
● ● ●

● × ●
```

×

就是：Centroid

希望：所有点距离中心越近越好。

---

数学目标最小化：

```
WCSS

Within Cluster Sum of Squares
```

公式：

```
Σ ||xi−μ||²
```

解释：每个点到自己中心平方距离。全部加起来。越小越好。

---

为什么平方？因为平方远点惩罚更大。

例如：距离：

```
1

2

5
```

平方：

```
1

4

25
```

远离中心的数据：影响更大。

---

# 6. Lloyd Algorithm

K-Means真正执行过程。

Step0

随机放：

K个中心。

↓

Step1

Assign

每个点：

找到最近中心。

↓

Step2

Update

重新计算：

中心。

就是：

平均值。

↓

重复。

直到：

没人换Cluster。

---

为什么一定收敛？

因为：每一步WCSS不会增加。只能下降。

所以：一定停止。

但是：只能保证Local Minimum。不是Global Minimum。

---

# 7. 为什么必须Standardize？

这是考试非常喜欢问的。

原因距离有单位。

例如：

收入：

```
20000
```

访问次数：

```
5
```

距离：

```
(20000-21000)^2

+

(5-50)^2
```

前者：

1000000

后者：

2025

几乎：

收入决定了一切。

访问次数：

完全没意义。

所以：必须z=(x−μ)/σ标准化。

否则距离没有意义。

PCA同样需要。

因为：Variance也有单位。

---

# 8. 如何选择K？

没有标准答案。

常见：

## Elbow Method

画：

```
WCSS

↓

越来越小
```

找到：

下降开始变缓的位置。

就是：

Elbow。

---

## Silhouette Score

衡量：Cluster好不好。
范围：[-1,1]

越接近：1越好。

意义：自己的Cluster够近。其它Cluster够远。

---

# 9. K-Means缺点

它默认认为：Cluster：

都是：○ ○ ○圆形。

如果：

```
~~~~~~

~~~~~~
```

Two Moons。

K-Means：一定切错。

另外：细长：

```
========
```

也会失败。

还有：Cluster大小不同也容易失败。

---

所以：K-Means适合：Round, Equal Size, Separated

---

# Hierarchical Clustering

思想：不要先决定K。

而是一直Merge。最后形成：一棵树。叫Dendrogram。

---

流程：

开始：

每个点：

自己一个Cluster。

↓

找到：

最近两个Cluster。

↓

Merge。

↓

重复。

↓

直到：

所有点：

合并。

---

最后：

树长这样：

```
|

|     |

|   | |

A B C D
```

之后：你再决定在哪里Cut。得到几个Cluster。

---

## Linkage

两个Cluster：怎么定义距离？

Single Linkage：最近点。

```
MIN
```

容易：Chaining。

---

Complete Linkage：最远点。

```
MAX
```

Cluster更紧。

---

Ward：最常见。让：Cluster内部Variance增加最少。

---

# PCA

为什么需要？很多数据维度太高。

例如：图片：

```
200×200

=

40000维。
```

很多Feature：其实重复。

所以：想找几个最重要方向。

---

PCA目标：找到Variance最大的方向。

第一主成分：Variance最大。

第二主成分：Variance第二。

而且必须正交。

---

为什么Variance？

Variance大的方向：说明数据变化最大。信息最多。

Variance很小：通常噪声。

所以：保留Variance大的方向。丢掉Variance小的方向。

---

PCA优点

✔ 可视化

✔ 去噪

✔ Feature Compression

✔ Faster Training

✔ 降低Overfitting

---

# PCA 和 SVD

很多人容易混。

PCA：

算法思想。

SVD：

矩阵分解方法。

实际：

Python里面：

PCA：

几乎全部：

调用SVD。

所以：

```
PCA

↓

SVD
```

---

# GMM

K-Means：

一个点：

只能属于：

一个Cluster。

GMM：

不是。

例如：

一个点：

```
70%

Cluster A

30%

Cluster B
```

因此：

叫：

Soft Clustering。

更真实。

---

# EM

EM：

专门训练：

GMM。

E：

计算：

属于每个Gaussian：

概率。

M：

更新：

Gaussian参数。

不断：

重复。

直到：

Likelihood：

不再增加。

---

# t-SNE

目标：

保持：

Local Neighborhood。

谁原来近。

现在：

还近。

但是：

全局距离：

不可信。

主要：

画图。

---

# UMAP

思想：

类似：

t-SNE。

但是：

速度：

快。

保留：

更多Global Structure。

大数据：

更常用。

---

# 总结

Unsupervised Learning

↓

四件事：

```
Clustering

↓

K-Means

Hierarchical

GMM

↓

Projection

↓

PCA

SVD

↓

Density

↓

GMM

EM

↓

Visualization

↓

t-SNE

UMAP
```

---

# 面试高频

为什么Standardize？

为什么K-Means只能找到Local Minimum？

为什么K-Means不能处理Two Moons？

Elbow和Silhouette区别？

为什么PCA最大化Variance？

为什么PCA需要Standardize？

PCA为什么用SVD？

为什么GMM比K-Means灵活？

EM为什么一定收敛？

t-SNE为什么不能做预测？

UMAP为什么越来越流行？

---

| 方法                            | 使用的信息           | 核心思想                |
| ----------------------------- | --------------- | ------------------- |
| Collaborative Filtering       | 用户—物品评分或交互      | 相似用户喜欢什么，或者相似物品被谁喜欢 |
| Content-Based Filtering       | 物品内容特征          | 推荐和用户过去喜欢的物品相似的内容   |
| Collaborative Filtering 的主要问题 | 数据稀疏、cold start | 新用户或新物品没有交互         |
| Content-Based 的主要问题           | 依赖特征质量          | 推荐结果可能过于相似          |

