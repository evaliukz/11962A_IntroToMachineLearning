# 推荐系统与 UV Factorization 梯度推导

## 1. 这道题到底在问什么？

题目给出一个用户评分矩阵：

```python
R = np.array([
    [5, 4, np.nan, 1],
    [4, np.nan, 5, 1],
    [1, 2, np.nan, 5],
    [np.nan, 1, 4, 5],
    [np.nan, 3, 3, np.nan]
])
```

然后问两个问题：

1. **Collaborative Filtering 和 Content-Based Filtering 有什么区别？**
2. 在 UV Factorization 中，损失函数对 \(U\) 的梯度是什么？

所以，这道题的前半部分是概念题，后半部分是求导题。

---

# 2. 评分矩阵 \(R\) 是什么？

矩阵 \(R\) 可以表示用户对物品的评分。

假设：

- 每一行代表一个用户
- 每一列代表一个物品
- 数字代表用户给物品的评分
- `np.nan` 代表用户还没有给这个物品评分

例如：

\[
R=
\begin{pmatrix}
5&4&?&1\\
4&?&5&1\\
1&2&?&5\\
?&1&4&5\\
?&3&3&?
\end{pmatrix}
\]

其中问号对应代码中的 `np.nan`。

---

## 2.1 如何理解这个矩阵？

第一行：

\[
(5,4,?,1)
\]

表示用户 1：

- 给物品 1 打了 5 分
- 给物品 2 打了 4 分
- 没有评价物品 3
- 给物品 4 打了 1 分

推荐系统的任务就是利用已有评分，预测问号位置可能是多少。

例如：

```text
用户 1 会给物品 3 打多少分？

用户 2 会给物品 2 打多少分？

用户 5 会给物品 1 和物品 4 打多少分？
```

---

# 3. Collaborative Filtering 是什么？

Collaborative Filtering 中文通常叫：

> **协同过滤**

它只看用户和物品之间的交互数据，例如：

- 评分
- 点击
- 购买
- 收藏
- 观看记录

它不一定需要知道物品本身是什么。

---

## 3.1 大白话理解

假设 Alice 和 Bob 过去喜欢的电影非常相似：

```text
Alice 喜欢 A、B、C

Bob 喜欢 A、B、C、D
```

由于 Alice 和 Bob 的兴趣很像，而 Bob 还喜欢电影 D，因此系统可能把电影 D 推荐给 Alice。

核心思想是：

> 和你兴趣相似的人喜欢什么，就推荐什么给你。

---

## 3.2 在这道题中

Collaborative Filtering 直接使用评分矩阵：

\[
R=
\begin{pmatrix}
5&4&?&1\\
4&?&5&1\\
1&2&?&5\\
?&1&4&5\\
?&3&3&?
\end{pmatrix}
\]

它会从矩阵中学习：

- 哪些用户比较相似
- 哪些物品比较相似
- 用户隐藏的偏好
- 物品隐藏的属性

然后预测缺失评分。

---

# 4. Content-Based Filtering 是什么？

Content-Based Filtering 中文叫：

> **基于内容的过滤**

它主要看物品本身的特征。

例如，对于电影，特征可能包括：

```text
类型：动作、喜剧、爱情

导演

演员

上映年份

语言

片长
```

如果一个用户以前经常喜欢动作片，系统就继续推荐动作片。

---

## 4.1 大白话理解

假设用户喜欢：

```text
科幻电影
太空题材
人工智能题材
```

那么系统会推荐其他同样具有这些特征的电影。

核心思想是：

> 你过去喜欢什么样的东西，就继续推荐相似的东西。

---

# 5. 两种推荐方法的区别

| 对比项 | Collaborative Filtering | Content-Based Filtering |
|---|---|---|
| 中文名称 | 协同过滤 | 基于内容的过滤 |
| 使用数据 | 用户和物品的交互 | 物品自身特征 |
| 是否需要物品特征 | 不一定需要 | 需要 |
| 核心思想 | 相似用户喜欢什么 | 推荐相似内容 |
| 典型输入 | 评分、点击、购买记录 | 类型、关键词、演员、标签 |
| 主要问题 | 新用户和新物品冷启动 | 推荐内容可能过于相似 |

一句话总结：

> Collaborative Filtering 看“大家是怎么选择的”，Content-Based Filtering 看“这个物品本身是什么”。

---

# 6. 什么是 UV Factorization？

UV Factorization 是 Collaborative Filtering 的一种常见实现方式。

它的目标是：

> 使用两个较小的矩阵 \(U\) 和 \(V\)，近似原始评分矩阵 \(R\)。

公式是：

\[
R\approx UV^T
\]

假设：

\[
R\in\mathbb{R}^{m\times n}
\]

其中：

- \(m\)：用户数量
- \(n\)：物品数量

我们将它分解成：

\[
U\in\mathbb{R}^{m\times k}
\]

和

\[
V\in\mathbb{R}^{n\times k}
\]

其中 \(k\) 是隐藏特征的数量，一般远小于用户数和物品数。

---

## 6.1 这道题中的矩阵维度

题目中的 \(R\) 有：

- 5 行：5 个用户
- 4 列：4 个物品

因此：

\[
R\in\mathbb{R}^{5\times4}
\]

假设选择两个隐藏特征，即 \(k=2\)，那么：

\[
U\in\mathbb{R}^{5\times2}
\]

\[
V\in\mathbb{R}^{4\times2}
\]

所以：

\[
UV^T
\]

的维度是：

\[
(5\times2)(2\times4)=5\times4
\]

正好与 \(R\) 相同。

---

# 7. \(U\) 和 \(V\) 分别是什么？

## 7.1 \(U\)：用户隐藏特征

\(U\) 的每一行代表一个用户。

\[
U=
\begin{pmatrix}
- &u_1^T&-\\
- &u_2^T&-\\
- &u_3^T&-\\
- &u_4^T&-\\
- &u_5^T&-
\end{pmatrix}
\]

其中 \(u_i\) 是用户 \(i\) 的隐藏特征向量。

例如在电影推荐中，两个隐藏特征可能粗略对应：

```text
特征 1：喜欢严肃电影还是轻松电影

特征 2：喜欢动作电影还是爱情电影
```

这些特征通常不是人工指定的，而是模型从评分数据中自动学习出来的。

---

## 7.2 \(V\)：物品隐藏特征

\(V\) 的每一行代表一个物品。

\[
V=
\begin{pmatrix}
- &v_1^T&-\\
- &v_2^T&-\\
- &v_3^T&-\\
- &v_4^T&-
\end{pmatrix}
\]

其中 \(v_j\) 是物品 \(j\) 的隐藏特征向量。

---

# 8. 如何预测一个评分？

用户 \(i\) 对物品 \(j\) 的预测评分是：

\[
\hat r_{ij}=u_i^Tv_j
\]

也就是用户向量和物品向量的点积。

假设：

\[
u_i=
\begin{pmatrix}
2\\
1
\end{pmatrix},
\qquad
v_j=
\begin{pmatrix}
1.5\\
0.5
\end{pmatrix}
\]

那么：

\[
\hat r_{ij}
=
u_i^Tv_j
=
2\times1.5+1\times0.5
=
3.5
\]

因此模型预测用户 \(i\) 会给物品 \(j\) 打 3.5 分。

---

# 9. 损失函数在说什么？

题目给出：

\[
J =
\sum_{(i,j)\in\Omega}
\left(r_{ij}-u_i^T v_j\right)^2
\]

看起来复杂，其实就是：

> 把所有已知评分的“真实值和预测值之差”平方，再全部加起来。

---

## 9.1 各个符号的含义

| 符号 | 含义 |
|---|---|
| \(J\) | 总损失 |
| \(r_{ij}\) | 用户 \(i\) 对物品 \(j\) 的真实评分 |
| \(u_i^Tv_j\) | 模型预测评分 |
| \(r_{ij}-u_i^Tv_j\) | 预测误差 |
| \(\Omega\) | 所有已知评分的位置 |
| \(\sum\) | 把所有已知位置的损失加起来 |

---

## 9.2 为什么只对 \(\Omega\) 求和？

因为评分矩阵中有很多 `np.nan`。

这些位置没有真实评分，所以无法判断预测是否正确。

因此，只能在已知评分的位置计算损失。

例如第一行：

\[
(5,4,?,1)
\]

第一行的损失是：

\[
J_1
=
(5-u_1^Tv_1)^2
+
(4-u_1^Tv_2)^2
+
(1-u_1^Tv_4)^2
\]

没有：

\[
(?-u_1^Tv_3)^2
\]

因为第三个评分不存在。

---

# 10. 什么是梯度？

梯度可以理解成：

> 如果稍微改变参数，损失函数会朝哪个方向变化、变化多快。

模型训练的目标是降低损失 \(J\)。

因此，Gradient Descent 会执行：

\[
U\leftarrow U-\eta\nabla_UJ
\]

其中：

- \(\nabla_UJ\)：损失函数关于 \(U\) 的梯度
- \(\eta\)：learning rate
- 减去梯度：朝损失下降的方向更新

---

# 11. 题目为什么要求对 \(U\) 求梯度？

因为 \(U\) 中保存的是用户隐藏向量。

我们希望通过训练修改 \(U\)，让预测评分：

\[
u_i^Tv_j
\]

越来越接近真实评分：

\[
r_{ij}
\]

所以需要知道：

> 怎样修改 \(U\)，才能让损失 \(J\) 下降？

这个修改方向就是：

\[
\frac{\partial J}{\partial U}
\]

---

# 12. 先对一个用户向量 \(u_i\) 求梯度

完整损失是：

\[
J =
\sum_{(i,j)\in\Omega}
\left(r_{ij}-u_i^Tv_j\right)^2
\]

对某个固定用户 \(i\)，只需要考虑这个用户已经评分的物品：

\[
J_i=
\sum_{j:(i,j)\in\Omega}
\left(r_{ij}-u_i^Tv_j\right)^2
\]

---

## 12.1 先看一个评分的损失

对于一个已知评分：

\[
L_{ij}
=
\left(r_{ij}-u_i^Tv_j\right)^2
\]

令：

\[
e_{ij}=r_{ij}-u_i^Tv_j
\]

那么：

\[
L_{ij}=e_{ij}^2
\]

对 \(u_i\) 求导，使用链式法则：

\[
\frac{\partial L_{ij}}{\partial u_i}
=
2e_{ij}
\frac{\partial e_{ij}}{\partial u_i}
\]

由于：

\[
e_{ij}=r_{ij}-u_i^Tv_j
\]

其中 \(r_{ij}\) 是固定数字，因此：

\[
\frac{\partial r_{ij}}{\partial u_i}=0
\]

而：

\[
\frac{\partial u_i^Tv_j}{\partial u_i}=v_j
\]

所以：

\[
\frac{\partial e_{ij}}{\partial u_i}
=
-v_j
\]

最终：

\[
\frac{\partial L_{ij}}{\partial u_i}
=
-2(r_{ij}-u_i^Tv_j)v_j
\]

等价地写成：

\[
\frac{\partial L_{ij}}{\partial u_i}
=
2(u_i^Tv_j-r_{ij})v_j
\]

---

# 13. 对用户 \(i\) 的所有评分求和

因为用户 \(i\) 可能评价了多个物品，所以需要将所有已知评分产生的梯度加起来：

\[
\boxed{
\frac{\partial J}{\partial u_i}
=
2
\sum_{j:(i,j)\in\Omega}
\left(u_i^Tv_j-r_{ij}\right)v_j
}
\]

也可以写成：

\[
\boxed{
\frac{\partial J}{\partial u_i}
=
-2
\sum_{j:(i,j)\in\Omega}
\left(r_{ij}-u_i^Tv_j\right)v_j
}
\]

两种形式完全一样。

---

# 14. 用第一位用户具体理解

第一位用户的评分是：

\[
(5,4,?,1)
\]

他只评价了物品 1、2、4。

所以第一位用户的损失是：

\[
J_1
=
(5-u_1^Tv_1)^2
+
(4-u_1^Tv_2)^2
+
(1-u_1^Tv_4)^2
\]

对 \(u_1\) 求梯度：

\[
\boxed{
\frac{\partial J}{\partial u_1}
=
2(u_1^Tv_1-5)v_1
+
2(u_1^Tv_2-4)v_2
+
2(u_1^Tv_4-1)v_4
}
\]

物品 3 没有评分，因此不参与梯度计算。

---

# 15. 这个梯度直觉上是什么意思？

看其中一项：

\[
2(u_i^Tv_j-r_{ij})v_j
\]

可以分成两部分。

## 第一部分：预测误差

\[
u_i^Tv_j-r_{ij}
\]

- 如果预测太高，该值为正
- 如果预测太低，该值为负
- 如果预测正确，该值接近 0

## 第二部分：物品方向

\[
v_j
\]

它告诉模型应该沿着物品特征的哪个方向调整用户向量。

所以整个梯度的意思是：

> 根据预测误差，沿着物品向量 \(v_j\) 的方向调整用户向量 \(u_i\)。

---

# 16. 对整个矩阵 \(U\) 的梯度

定义 mask 矩阵：

\[
M_{ij}=
\begin{cases}
1,&r_{ij}\text{ 已知}\\
0,&r_{ij}\text{ 缺失}
\end{cases}
\]

预测矩阵：

\[
\hat R=UV^T
\]

预测误差矩阵：

\[
UV^T-R
\]

但是缺失位置不应该参与，所以使用逐元素乘法：

\[
M\odot(UV^T-R)
\]

其中 \(\odot\) 表示 element-wise multiplication。

最终：

\[
\boxed{
\nabla_UJ
=
2\left[M\odot(UV^T-R)\right]V
}
\]

---

# 17. 检查矩阵维度

假设：

\[
R\in\mathbb{R}^{5\times4}
\]

\[
U\in\mathbb{R}^{5\times k}
\]

\[
V\in\mathbb{R}^{4\times k}
\]

首先：

\[
UV^T
\]

维度为：

\[
(5\times k)(k\times4)=5\times4
\]

所以：

\[
M\odot(UV^T-R)
\]

也是 \(5\times4\)。

再乘以 \(V\)：

\[
(5\times4)(4\times k)=5\times k
\]

因此：

\[
\nabla_UJ\in\mathbb{R}^{5\times k}
\]

它与 \(U\) 的形状相同，可以用来更新 \(U\)。

---

# 18. NumPy 实现

```python
import numpy as np

R = np.array([
    [5, 4, np.nan, 1],
    [4, np.nan, 5, 1],
    [1, 2, np.nan, 5],
    [np.nan, 1, 4, 5],
    [np.nan, 3, 3, np.nan]
], dtype=float)

num_users, num_items = R.shape
k = 2

rng = np.random.default_rng(0)

U = rng.normal(size=(num_users, k))
V = rng.normal(size=(num_items, k))

# True 表示该位置存在真实评分
mask = ~np.isnan(R)

# 暂时将 NaN 替换为 0，方便矩阵计算
R_filled = np.nan_to_num(R, nan=0.0)

# 预测所有评分
predictions = U @ V.T

# 只有已知评分位置保留误差
error = np.where(
    mask,
    predictions - R_filled,
    0.0
)

# 对 U 的梯度
grad_U = 2 * error @ V

print(grad_U)
```

对应数学公式：

```python
predictions = U @ V.T
```

对应：

\[
UV^T
\]

```python
error = np.where(mask, predictions - R_filled, 0.0)
```

对应：

\[
M\odot(UV^T-R)
\]

```python
grad_U = 2 * error @ V
```

对应：

\[
\nabla_UJ
=
2\left[M\odot(UV^T-R)\right]V
\]

---

# 19. 这道题不要求什么？

题目只给了评分矩阵 \(R\)，没有提供具体的 \(U\) 和 \(V\)。

所以无法计算出一个具体的数字梯度。

题目真正要求的是：

> 推导梯度的数学表达式。

最终答案是：

\[
\boxed{
\frac{\partial J}{\partial u_i}
=
2
\sum_{j:(i,j)\in\Omega}
(u_i^Tv_j-r_{ij})v_j
}
\]

对于整个 \(U\)：

\[
\boxed{
\nabla_UJ
=
2\left[M\odot(UV^T-R)\right]V
}
\]

---

# 20. 考试版答案

## Collaborative Filtering 与 Content-Based Filtering

Collaborative Filtering 根据用户和物品之间的评分或交互模式进行推荐，不要求物品本身的特征。

Content-Based Filtering 根据物品的内容特征，以及用户过去喜欢的物品特征进行推荐。

```text
Collaborative Filtering：

相似的人喜欢什么？


Content-Based Filtering：

与过去喜欢的东西相似吗？
```

---

## UV Factorization

使用：

\[
R\approx UV^T
\]

预测用户 \(i\) 对物品 \(j\) 的评分：

\[
\hat r_{ij}=u_i^Tv_j
\]

损失函数：

\[
J=
\sum_{(i,j)\in\Omega}
(r_{ij}-u_i^Tv_j)^2
\]

对用户向量 \(u_i\) 的梯度：

\[
\boxed{
\frac{\partial J}{\partial u_i}
=
2
\sum_{j:(i,j)\in\Omega}
(u_i^Tv_j-r_{ij})v_j
}
\]

对整个矩阵 \(U\) 的梯度：

\[
\boxed{
\nabla_UJ
=
2\left[M\odot(UV^T-R)\right]V
}
\]

---

# 21. 一句话总结

> 这道题先让我们区分两种推荐系统，然后让我们推导：为了让预测评分 \(u_i^Tv_j\) 更接近真实评分 \(r_{ij}\)，应该怎样更新用户特征矩阵 \(U\)。
