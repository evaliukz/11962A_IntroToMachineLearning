# Ensemble Learning（集成学习）

## 1. 什么是 Ensemble Learning？

Ensemble Learning 中文叫：

> **集成学习**

它的核心思想是：

> 不完全相信一个模型，而是让多个模型一起做决定。

<img width="1344" height="885" alt="image" src="https://github.com/user-attachments/assets/e97ab48c-ea99-4599-9f1a-88f47ff29be9" />


例如，3 个模型判断一张图片是不是山羊：

```text
模型 1：是
模型 2：是
模型 3：不是
```

采用多数投票：

```text
“是”有 2 票
“不是”有 1 票
```

因此最终预测是：

```text
是山羊
```

这就是最简单的集成学习。

---

## 2. 为什么多个普通模型可能比一个模型更好？

假设 3 个模型的准确率都只有 60%。

单独使用一个模型：

```text
准确率 = 60%
```

但是，如果 3 个模型犯的错误不完全相同，就可以通过多数投票抵消一部分错误。

```text
模型 1 看错了，但模型 2 和模型 3 看对了
→ 最终仍然预测正确

模型 2 看错了，但模型 1 和模型 3 看对了
→ 最终仍然预测正确
```

所以关键不是每个模型都非常强，而是：

1. 每个模型至少比随机猜测好一点；
2. 不同模型不要总是在同一个地方犯错。

---

## 3. 集成学习的两个重要条件

### 3.1 每个模型要比随机猜测好

对于二分类问题，随机猜测的准确率是：

$$
50\%
$$

因此通常要求单个模型：

$$
p > 0.5
$$

其中，$p$ 表示单个模型预测正确的概率。

例如：

```text
p = 0.60
```

表示模型平均有 60% 的概率预测正确。

如果模型准确率低于 50%，增加更多这样的模型，可能会让多数投票越来越稳定地给出错误答案。

---

### 3.2 模型之间的错误要有差异

理想情况下，不同模型的错误应当尽量独立。

例如：

```text
模型 1 容易把光线暗的图片看错

模型 2 容易把角度特殊的图片看错

模型 3 容易把背景复杂的图片看错
```

一个模型出错时，其他模型可能把它纠正回来。

相反，如果所有模型都一样：

```text
模型 1 错
模型 2 也错
模型 3 也错
```

多数投票就没有帮助。

一句话总结：

> 集成学习不仅需要多个模型，还需要模型具有多样性。

---

## 4. Majority Vote（多数投票）

对于分类问题，最简单的组合方式是多数投票。

假设有 5 个模型：

```text
模型 1：正类
模型 2：正类
模型 3：负类
模型 4：正类
模型 5：负类
```

结果：

```text
正类：3 票
负类：2 票
```

最终预测：

```text
正类
```

这种方法叫做 **Hard Voting**，因为每个模型直接投出一个类别标签。

---

### 4.1 Soft Voting

模型也可以输出概率。

例如：

```text
模型 1：正类概率 0.8
模型 2：正类概率 0.6
模型 3：正类概率 0.4
```

平均概率为：

$$
\frac{0.8+0.6+0.4}{3}=0.6
$$

因为：

$$
0.6 > 0.5
$$

所以最终预测为正类。

这叫做 **Soft Voting**。

---

## 5. 为什么平均多个模型可以降低方差？

假设每个模型的预测误差可以看作随机变量：

$$
X_1,X_2,\ldots,X_M
$$

其中：

- $M$：模型数量
- 每个模型的误差方差都是 $\sigma^2$

也就是说：

$$
\mathrm{Var}(X_i)=\sigma^2
$$

把多个模型的结果取平均：

$$
\bar X=\frac{1}{M}\sum_{i=1}^{M}X_i
$$

我们关心平均预测的方差：

$$
\mathrm{Var}(\bar X)
$$

方差越小，说明模型的预测越稳定。

---

## 6. 模型完全独立时的方差

如果各模型的误差相互独立，那么：

$$
\boxed{
\mathrm{Var}
\left(
\frac{1}{M}\sum_{i=1}^{M}X_i
\right)
=
\frac{\sigma^2}{M}
}
$$

也就是：

$$
\boxed{
\mathrm{Var}(\bar X)=\frac{\sigma^2}{M}
}
$$

---

### 6.1 公式推导

首先使用方差的缩放规则：

$$
\mathrm{Var}(aX)=a^2\mathrm{Var}(X)
$$

因此：

$$
\mathrm{Var}
\left(
\frac{1}{M}\sum_{i=1}^{M}X_i
\right)
=
\frac{1}{M^2}
\mathrm{Var}
\left(
\sum_{i=1}^{M}X_i
\right)
$$

如果变量彼此独立，和的方差等于方差之和：

$$
\mathrm{Var}
\left(
\sum_{i=1}^{M}X_i
\right)
=
\sum_{i=1}^{M}\mathrm{Var}(X_i)
$$

因为每个模型的方差都是 $\sigma^2$：

$$
\sum_{i=1}^{M}\mathrm{Var}(X_i)
=
M\sigma^2
$$

所以：

$$
\mathrm{Var}(\bar X)
=
\frac{1}{M^2}M\sigma^2
$$

最终：

$$
\boxed{
\mathrm{Var}(\bar X)=\frac{\sigma^2}{M}
}
$$

---

### 6.2 大白话理解

假设单个模型的预测方差是：

$$
\sigma^2=100
$$

使用 1 个模型：

$$
\mathrm{Var}=100
$$

平均 10 个独立模型：

$$
\mathrm{Var}
=
\frac{100}{10}
=
10
$$

平均 100 个独立模型：

$$
\mathrm{Var}
=
\frac{100}{100}
=
1
$$

模型越多，平均预测越稳定。

---

## 7. 两个模型平均后的方差

假设有两个随机变量 $X$ 和 $Y$：

$$
\mathrm{Var}(X)
=
\mathrm{Var}(Y)
=
\sigma^2
$$

它们的平均值是：

$$
\frac{X+Y}{2}
$$

根据方差公式：

$$
\mathrm{Var}(X+Y)
=
\mathrm{Var}(X)
+
\mathrm{Var}(Y)
+
2\mathrm{Cov}(X,Y)
$$

因此：

$$
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{1}{4}
\left[
\mathrm{Var}(X)
+
\mathrm{Var}(Y)
+
2\mathrm{Cov}(X,Y)
\right]
$$

代入：

$$
\mathrm{Var}(X)
=
\mathrm{Var}(Y)
=
\sigma^2
$$

得到：

$$
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{1}{4}
\left[
2\sigma^2+2\mathrm{Cov}(X,Y)
\right]
$$

---

## 8. Correlation $\rho$ 是什么？

相关系数定义为：

$$
\rho
=
\frac{\mathrm{Cov}(X,Y)}
{\sigma_X\sigma_Y}
$$

因为这里：

$$
\sigma_X=\sigma_Y=\sigma
$$

所以：

$$
\mathrm{Cov}(X,Y)
=
\rho\sigma^2
$$

代入前面的公式：

$$
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{1}{4}
\left[
2\sigma^2+2\rho\sigma^2
\right]
$$

整理得到：

$$
\boxed{
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{\sigma^2}{2}(1+\rho)
}
$$

等价地：

$$
\boxed{
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{\sigma^2}{2}
+
\frac{\rho\sigma^2}{2}
}
$$

---

## 9. 如何理解相关系数 $\rho$？

### 情况一：模型不相关

如果：

$$
\rho=0
$$

那么：

$$
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{\sigma^2}{2}
$$

方差降低了一半。

---

### 情况二：完全正相关

如果：

$$
\rho=1
$$

那么：

$$
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\sigma^2
$$

方差完全没有下降。

原因是两个模型总是一起变化、一起犯错。

---

### 情况三：负相关

如果：

$$
\rho<0
$$

那么平均后的方差会下降得更多。

例如：

$$
\rho=-0.5
$$

则：

$$
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{\sigma^2}{2}(1-0.5)
=
\frac{\sigma^2}{4}
$$

一个模型偏高时，另一个模型容易偏低，因此两者可以互相抵消。

---

## 10. $M$ 个相关模型的方差

假设有 $M$ 个模型：

$$
X_1,\ldots,X_M
$$

每个模型都有相同方差：

$$
\mathrm{Var}(X_i)=\sigma^2
$$

任意两个模型之间的相关系数都是：

$$
\rho
$$

平均预测为：

$$
\bar X=
\frac{1}{M}\sum_{i=1}^{M}X_i
$$

它的方差是：

$$
\boxed{
\mathrm{Var}(\bar X)
=
\frac{\sigma^2}{M}
+
\frac{M-1}{M}\rho\sigma^2
}
$$

也可以写成：

$$
\boxed{
\mathrm{Var}(\bar X)
=
\sigma^2
\left[
\rho+\frac{1-\rho}{M}
\right]
}
$$

---

## 11. $M$ 个模型方差公式的详细推导

从：

$$
\bar X=
\frac{1}{M}\sum_{i=1}^{M}X_i
$$

开始：

$$
\mathrm{Var}(\bar X)
=
\frac{1}{M^2}
\mathrm{Var}
\left(
\sum_{i=1}^{M}X_i
\right)
$$

多个变量之和的方差为：

$$
\mathrm{Var}
\left(
\sum_{i=1}^{M}X_i
\right)
=
\sum_{i=1}^{M}\mathrm{Var}(X_i)
+
2\sum_{i<j}\mathrm{Cov}(X_i,X_j)
$$

第一部分：

$$
\sum_{i=1}^{M}\mathrm{Var}(X_i)
=
M\sigma^2
$$

任意两个模型之间：

$$
\mathrm{Cov}(X_i,X_j)
=
\rho\sigma^2
$$

$M$ 个模型一共有：

$$
\binom{M}{2}
=
\frac{M(M-1)}{2}
$$

对不同模型。

所以协方差部分是：

$$
2
\cdot
\frac{M(M-1)}{2}
\cdot
\rho\sigma^2
$$

即：

$$
M(M-1)\rho\sigma^2
$$

因此：

$$
\mathrm{Var}(\bar X)
=
\frac{1}{M^2}
\left[
M\sigma^2
+
M(M-1)\rho\sigma^2
\right]
$$

整理：

$$
\boxed{
\mathrm{Var}(\bar X)
=
\frac{\sigma^2}{M}
+
\frac{M-1}{M}\rho\sigma^2
}
$$

---

## 12. 这个方差公式告诉我们什么？

公式：

$$
\mathrm{Var}(\bar X)
=
\frac{\sigma^2}{M}
+
\frac{M-1}{M}\rho\sigma^2
$$

可以分成两部分：

$$
\underbrace{\frac{\sigma^2}{M}}_{\text{增加模型数量可以降低}}
+
\underbrace{\frac{M-1}{M}\rho\sigma^2}_{\text{模型相关性带来的限制}}
$$

第一部分会随着 $M$ 增加而变小。

第二部分取决于模型之间的相关性 $\rho$。

当：

$$
M\rightarrow\infty
$$

有：

$$
\boxed{
\lim_{M\to\infty}
\mathrm{Var}(\bar X)
=
\rho\sigma^2
}
$$

因此，如果模型之间高度相关，无论加入多少模型，方差也无法降到零。

---

## 13. 集成学习真正看重的是多样性

模型数量多，并不一定意味着效果好。

更准确地说：

> 模型数量多，而且模型之间相关性低，才真正有用。

例如：

### 方案 A

```text
100 个几乎完全一样的决策树
```

它们的错误高度相关：

$$
\rho\approx1
$$

效果提升可能很小。

### 方案 B

```text
使用不同训练样本
使用不同特征
使用不同初始化
使用不同模型结构
```

它们的错误相关性更低：

$$
\rho \text{ 较低}
$$

通常能获得更大的集成收益。

---

## 14. 为什么 Random Forest 有效？

Random Forest 使用很多决策树，并通过随机性制造差异：

1. 每棵树使用不同的 Bootstrap 样本；
2. 每次分裂只考虑随机选出的一部分特征。

最终：

```text
分类：多数投票

回归：预测平均
```

Random Forest 的核心不是单纯“树很多”，而是：

> 树很多，并且树之间存在差异。

---

## 15. 为什么弱模型可以组成强模型？

假设每个模型准确率为：

$$
p=0.6
$$

也就是每个模型有 60% 的概率判断正确。

它虽然不算很强，但比随机猜测的 50% 好。

如果模型彼此独立，就可以通过多数投票提高总体正确率。

> 多个略好于随机的独立弱学习器，可以组合成一个更强的学习器。

---

## 16. 三个模型多数投票的概率

假设有三个独立模型，每个模型正确的概率为：

$$
p
$$

多数投票正确，需要至少两个模型正确。

包括两种情况：

1. 恰好两个模型正确；
2. 三个模型全部正确。

---

### 16.1 恰好两个正确

从三个模型中选择两个正确：

$$
\binom{3}{2}
$$

每一种组合的概率是：

$$
p^2(1-p)
$$

所以总概率：

$$
\binom{3}{2}p^2(1-p)
=
3p^2(1-p)
$$

---

### 16.2 三个都正确

概率是：

$$
p^3
$$

因此，多数投票正确的总概率为：

$$
P_{\text{majority}}
=
3p^2(1-p)+p^3
$$

整理：

$$
P_{\text{majority}}
=
3p^2-2p^3
$$

---

### 16.3 当 $p=0.6$

代入：

$$
P_{\text{majority}}
=
3(0.6)^2(0.4)+(0.6)^3
$$

$$
=
3(0.36)(0.4)+0.216
$$

$$
=
0.432+0.216
$$

$$
=
0.648
$$

因此：

$$
\boxed{
P_{\text{majority}}=64.8\%
}
$$

单个模型准确率是 60%，三个独立模型多数投票后，理论准确率约为 64.8%。

---

## 17. 一般情况下的多数投票公式

假设有奇数个模型：

$$
N=2k+1
$$

每个模型独立，且正确概率为：

$$
p
$$

多数投票正确意味着至少有 $k+1$ 个模型正确。

因此：

$$
\boxed{
P_{\text{majority}}
=
\sum_{r=k+1}^{2k+1}
\binom{2k+1}{r}
p^r(1-p)^{2k+1-r}
}
$$

其中：

- $r$：预测正确的模型数量；
- $\binom{2k+1}{r}$：选出 $r$ 个正确模型的方法数；
- $p^r$：这 $r$ 个模型都正确的概率；
- $(1-p)^{2k+1-r}$：其他模型出错的概率。

---

## 18. 使用错误模型数量表示

也可以令 $i$ 表示错误模型数量。

多数投票正确时，最多允许 $k$ 个模型错误：

$$
\boxed{
P_{\text{majority}}
=
\sum_{i=0}^{k}
\binom{2k+1}{i}
p^{2k+1-i}
(1-p)^i
}
$$

这与前面的公式等价。

---

### 18.1 图片公式中的小问题

图片中写的是：

$$
\sum_{i=0}^{k-1}
\binom{2k+1}{i}
p^{2k+1-i}(1-p)^i
$$

如果 $i$ 表示错误模型数量，多数投票正确时可以允许：

$$
i=0,1,\ldots,k
$$

所以求和上限应该通常是：

$$
\boxed{k}
$$

而不是 $k-1$。

例如有 3 个模型：

$$
2k+1=3
\Rightarrow k=1
$$

多数投票正确可以容许：

```text
0 个模型错误
或者
1 个模型错误
```

---

## 19. 为什么模型数量增加后准确率会提高？

假设：

$$
p>0.5
$$

每个模型都有一点点正确倾向。

随着独立模型数量增加，正确模型所占比例会越来越接近 $p$。

因为：

$$
p>0.5
$$

所以正确模型通常会超过一半。

模型越多，偶然出现错误模型占多数的概率越低。

---

## 20. 41 个准确率为 60% 的模型

假设：

$$
N=41
$$

每个模型准确率为：

$$
p=0.6
$$

多数投票至少需要 21 个模型正确：

$$
P_{\text{majority}}
=
\sum_{r=21}^{41}
\binom{41}{r}
(0.6)^r(0.4)^{41-r}
$$

结果约为：

$$
\boxed{
P_{\text{majority}}\approx0.9035
}
$$

也就是：

$$
\boxed{
90.35\%
}
$$

因此，41 个独立的 60% 准确率模型，多数投票后的理论准确率约为 90%。

---

## 21. 这个 90% 结果的前提

这个结果依赖于以下假设：

1. 每个模型准确率都是 60%；
2. 每个模型的错误相互独立；
3. 所有模型处理同一个二分类问题；
4. 所有模型的权重相同；
5. 数据分布稳定。

现实中，模型通常不是完全独立的。

所以：

> “41 个 60% 模型可以达到 90%”是独立假设下的理论结果，不代表任意 41 个模型都能达到 90%。

---

## 22. Independent Errors 是什么意思？

模型错误独立意味着：

> 知道模型 A 是否犯错，不会改变模型 B 犯错的概率。

数学上：

$$
P(A\text{ 错且 }B\text{ 错})
=
P(A\text{ 错})P(B\text{ 错})
$$

例如，每个模型错误率都是：

$$
0.4
$$

如果错误独立：

$$
P(A\text{ 和 }B\text{ 同时错})
=
0.4\times0.4
=
0.16
$$

如果模型高度相关，它们可能经常一起出错，实际同时出错概率就会高于 0.16。

---

## 23. Independence 和 Correlation 的关系

完全独立通常意味着：

$$
\rho=0
$$

但是：

$$
\rho=0
$$

只代表没有线性相关，不一定表示严格独立。

在集成学习的直觉讨论中，经常把以下概念放在一起理解：

```text
错误低相关

模型多样性高

接近独立
```

核心都是：

> 不同模型不要总是在相同样本上犯相同错误。

---

## 24. 加权投票

最简单的多数投票给每个模型相同权重：

$$
\hat y
=
\mathrm{majority}
(\hat y_1,\hat y_2,\ldots,\hat y_M)
$$

如果某些模型更强，可以使用加权投票：

$$
\hat y
=
\arg\max_c
\sum_{m=1}^{M}
w_m
\mathbf{1}(\hat y_m=c)
$$

其中：

- $w_m$：第 $m$ 个模型的权重；
- $\mathbf{1}(\hat y_m=c)$：模型是否预测类别 $c$。

更准确或更可信的模型可以获得更高权重。

---

## 25. Mixture of Experts 是什么？

Mixture of Experts 通常包含：

```text
多个 Expert Models

+

一个 Gating / Routing Model
```

Router 根据输入决定：

> 这个样本更适合交给哪些专家模型处理？

例如：

```text
数学问题 → 数学专家

代码问题 → 编程专家

语言问题 → 语言专家
```

它与简单多数投票不同：

- Majority Voting：所有模型都参与；
- Mixture of Experts：不同输入可以调用不同专家。

---

## 26. 常见 Ensemble 方法

### 26.1 Voting

多个模型直接投票。

```text
Logistic Regression
Decision Tree
SVM
```

可以共同完成最终分类。

---

### 26.2 Bagging

并行训练多个模型，然后平均或投票。

代表算法：

```text
Random Forest
```

主要作用：

> 降低 Variance。

---

### 26.3 Boosting

模型按顺序训练，后面的模型重点学习前面模型做错的样本。

代表算法：

```text
AdaBoost
Gradient Boosting
XGBoost
LightGBM
```

主要作用：

> 不断纠正前面模型的错误。

---

### 26.4 Stacking

先训练多个基础模型，再训练一个新模型学习如何组合它们。

```text
Model 1 ─┐
Model 2 ─┼→ Meta Model → Final Prediction
Model 3 ─┘
```

---

## 27. Bagging 和 Boosting 的区别

| 对比项 | Bagging | Boosting |
|---|---|---|
| 训练方式 | 多个模型通常并行训练 | 模型顺序训练 |
| 数据处理 | 每个模型使用不同采样数据 | 后续模型关注之前的错误 |
| 主要目标 | 降低方差 | 降低偏差并纠正错误 |
| 模型关系 | 相对独立 | 后一个依赖前一个 |
| 代表算法 | Random Forest | AdaBoost、XGBoost |

---

## 28. Bias 和 Variance

### High Variance

模型对训练数据非常敏感：

```text
训练数据稍微变化
模型结果就变化很大
```

例如一棵很深的决策树。

Bagging 和 Random Forest 常用于降低这种 Variance。

### High Bias

模型过于简单：

```text
训练集拟合不好
测试集表现也不好
```

Boosting 可以通过不断修正错误，构造更复杂、更强的模型。

---

## 29. 图片内容的核心逻辑

```text
单个模型不需要特别强
        ↓
只需要比随机猜测好
        ↓
不同模型需要犯不同的错误
        ↓
通过平均或多数投票
        ↓
一些错误互相抵消
        ↓
整体模型更稳定、更准确
```

数学上有两条主线。

### 回归或连续预测

通过平均降低方差：

$$
\mathrm{Var}(\bar X)
=
\frac{\sigma^2}{M}
+
\frac{M-1}{M}\rho\sigma^2
$$

### 分类

通过二项分布计算多数投票正确的概率：

$$
P_{\text{majority}}
=
\sum_{r=k+1}^{2k+1}
\binom{2k+1}{r}
p^r(1-p)^{2k+1-r}
$$

---

## 30. 最重要的结论

### 结论一：模型数量很重要

在其他条件相同的情况下，更多模型可以降低随机误差。

### 结论二：模型多样性更重要

如果所有模型高度相关：

$$
\rho\approx1
$$

加入更多模型帮助不大。

### 结论三：单个模型必须比随机猜测好

对于二分类：

$$
p>0.5
$$

集成准确率才会随着模型数量增加而提高。

### 结论四：理论结果依赖独立假设

多个 60% 准确率模型不一定真的能达到 90%。

只有错误足够独立时，理论提升才可能实现。

---

## 31. 考试速查表

| 概念 | 含义 |
|---|---|
| Ensemble Learning | 将多个模型组合成一个最终模型 |
| Weak Learner | 单独表现一般，但比随机猜测好 |
| Majority Voting | 分类任务中选择票数最多的类别 |
| Averaging | 回归任务中平均多个模型预测 |
| Model Diversity | 不同模型犯不同的错误 |
| Independent Errors | 一个模型是否犯错不影响另一个 |
| Correlation $\rho$ | 模型误差之间的相关程度 |
| Bagging | 并行训练多个模型，主要降低方差 |
| Boosting | 顺序训练模型，不断纠正之前错误 |
| Random Forest | 使用随机样本和随机特征训练多棵树 |
| Mixture of Experts | Router 根据输入选择不同专家模型 |

---

## 32. 必背公式

### 独立模型平均后的方差

$$
\boxed{
\mathrm{Var}(\bar X)
=
\frac{\sigma^2}{M}
}
$$

### 相关模型平均后的方差

$$
\boxed{
\mathrm{Var}(\bar X)
=
\frac{\sigma^2}{M}
+
\frac{M-1}{M}\rho\sigma^2
}
$$

### 两个模型平均后的方差

$$
\boxed{
\mathrm{Var}
\left(
\frac{X+Y}{2}
\right)
=
\frac{\sigma^2}{2}(1+\rho)
}
$$

### $2k+1$ 个模型多数投票正确的概率

$$
\boxed{
P_{\text{majority}}
=
\sum_{r=k+1}^{2k+1}
\binom{2k+1}{r}
p^r(1-p)^{2k+1-r}
}
$$

等价形式：

$$
\boxed{
P_{\text{majority}}
=
\sum_{i=0}^{k}
\binom{2k+1}{i}
p^{2k+1-i}(1-p)^i
}
$$

---

## 33. 一句话总结

> Ensemble Learning 的本质，是让多个比随机猜测稍好的、犯错方式不同的模型一起工作，通过平均或投票抵消部分错误，从而得到更稳定、更准确的最终模型。
