# Random Forest（随机森林）

# 1. 什么是 Random Forest？

Random Forest（随机森林）是一种 **Ensemble Learning（集成学习）** 方法。

它不是训练一棵决策树，而是训练很多棵决策树。

最后：

- 分类（Classification）→ 多数投票（Majority Vote）
- 回归（Regression）→ 平均预测（Average）

可以表示为：

```text
             Training Data
                    │
     ┌──────────────┼──────────────┐
     │              │              │
 Decision Tree1 Decision Tree2 Decision Tree3 ...
     │              │              │
     └──────────────┼──────────────┘
                    │
          Vote / Average
                    │
             Final Prediction
```

一句话：

> Random Forest = 很多棵不同的 Decision Tree 一起工作。

---

# 2. 为什么不用一棵树？

因为 Decision Tree 有一个很大的缺点：

> **High Variance（高方差）**

什么意思？

训练数据稍微变化一点，

整棵树可能就完全不同。

例如：

第一次训练：

```text
Age > 30 ?
```

第二次训练：

```text
Salary > 50000 ?
```

虽然数据只改了一点，

树可能长得完全不同。

所以：

```text
Decision Tree

↓

容易过拟合（Overfitting）

↓

Variance 很高
```

PPT 中说：

```text
Training a decision tree is largely deterministic.
```

意思是：

> 给定同一份数据，决策树几乎总是长成一样。

因为：

每一步都会选择：

```text
Information Gain 最大

或者

Gini 最小
```

所以训练过程几乎没有随机性。

---

# 3. Random Forest 的目标

Random Forest 希望：

> 每棵树都尽量不同（Independent）。

为什么？

因为 Ensemble Learning 告诉我们：

如果模型犯不同的错误：

```text
Tree1 错

Tree2 对

Tree3 对
```

多数投票：

```text
最终仍然正确
```

如果所有树都一样：

```text
Tree1 错

Tree2 错

Tree3 错
```

那 Random Forest 就完全没有意义。

所以：

Random Forest 最重要的一句话就是：

> **让每棵树尽量不同。**

---

# 4. 怎么让树不同？

PPT 提到了四种方法。

---

## 方法一：Randomize Training Data（Bootstrap Sampling）

这是最经典的方法。

每棵树都随机抽取训练数据。

例如：原始数据1000 条数据


Tree1：

```text
随机抽 1000 条（允许重复）
```

Tree2：

```text
再随机抽 1000 条
```

Tree3：

```text
再随机抽一次
```

注意这里采用：**Bootstrap Sampling（有放回抽样）**

什么意思？

例如：

```text
A

B

C

D
```

抽样可能得到：

```text
A

B

B

D
```

也可能：

```text
C

C

A

D
```

有些样本重复出现, 有些样本没出现
```

因此：每棵树看到的数据都不一样。

---

## 方法二：Random Feature Selection

除了随机数据，

还随机选择特征。

例如有：20 个特征

建立树时不是每次都看：20 个

而是随机选：

```text
√20≈4~5 个
```

例如：

第一次：

```text
Age

Income

Height

Weight
```

第二次：

```text
Salary

Gender

Education

City
```

这样：不同树第一次分裂可能就不同。

---

## 方法三：Random Split

PPT 中写：

```text
Choose among the best k splits
```

普通决策树总选择：最好的 split

Random Forest：可以从前几个比较好的 split随机选一个。

例如：

Information Gain：

```text
Age

0.53

Income

0.52

Height

0.51
```

普通树：

一定选：

```text
Age
```

Random Forest：可能随机：

```text
Age

Income

Height
```

其中一个。

这样：

树就更加不同。

---

## 方法四：Combination

实际 Random Forest通常同时采用：

- Bootstrap Sampling
- Random Feature Selection

有些实现还加入：Random Split。

---

# 5. 为什么随机化以后效果更好？

Random Forest：

不是让每棵树越来越强。

而是让每棵树犯不同的错误。

例如：

Tree1：

```text
容易看错猫
```

Tree2：

```text
容易看错狗
```

Tree3：

```text
容易看错背景
```

于是：

```text
错误互相抵消
```

整体准确率：

提高。

---

# 6. 最终怎么预测？

Random Forest：最后组合所有树。

---

## 分类（Classification）

采用：

Majority Vote。

例如：

```text
Tree1：

Cat

Tree2：

Dog

Tree3：

Dog

Tree4：

Dog

Tree5：

Cat
```

结果：

```text
Dog：3票

Cat：2票
```

最终：

```text
Dog
```

---

## 回归（Regression）

不是投票。

而是平均。

例如：

Tree1：

```text
23
```

Tree2：

```text
25
```

Tree3：

```text
24
```

预测：

$$
\frac{23+25+24}{3}=24
$$

这就是 PPT：

```text
Vote or Average Predictions
```

意思。

---

# 7. 为什么叫 Random Forest？

因为：

Random：

来自：

```text
随机数据

随机特征

随机分裂
```

Forest：

来自：

```text
很多 Decision Tree
```

很多树：

组成：

Forest。

---

# 8. 为什么 Random Forest 能降低 Variance？

假设：

每棵树：

预测误差方差：

$$
\sigma^2
$$

如果：

树之间独立：

平均以后：

方差：

$$
\boxed{
\mathrm{Var}=
\frac{\sigma^2}{M}
}
$$

其中：

- $$M$$：树的数量。

如果：

树之间：

相关系数：

$$
\rho
$$

则：

$$
\boxed{
\mathrm{Var}=
\frac{\sigma^2}{M}
+
\frac{M-1}{M}\rho\sigma^2
}
$$

说明：

Random Forest：

真正想降低的是：

$$
\rho
$$

也就是树之间的相关性。

所以Random Forest最重要不是：树很多。

而是：树很多，而且互相不同。

---

# 9. Bagging vs Random Forest

很多人容易混。

Bagging：

```text
Bootstrap

+

任意模型
```

例如：

可以：

Bagging：

```text
Decision Tree

SVM

Neural Network
```

Random Forest：

就是：

```text
Bagging

+

Decision Tree

+

Random Feature Selection
```

所以：

```text
Random Forest

=

Bagging

+

更多随机性
```

---

# 10. Random Forest 优缺点

## 优点

✅ 容易训练

✅ 不容易过拟合

✅ 对噪声鲁棒

✅ 可以处理高维数据

✅ 可以计算 Feature Importance

---

## 缺点

❌ 可解释性比单棵树差

❌ 树很多，占内存

❌ 推理速度比单棵树慢

---

# 11. 面试常考

### Q1：为什么要随机抽样？

答：降低树之间相关性。

---

### Q2：为什么随机选择 Feature？

答：避免所有树都选择同一个最佳特征。降低相关性。

---

### Q3：为什么 Random Forest 效果好？

答：因为很多低相关的树通过Vote / Average降低 Variance。

---

### Q4：Random Forest 属于什么 Ensemble？

答：Bagging。

---

### Q5：分类和回归分别怎么组合？

分类：Majority Vote

回归：Average

---

# 12. 一张图总结

```text
                Training Data
                       │
          Bootstrap Sampling
                       │
          Random Feature Selection
                       │
      ┌────────┬────────┬────────┐
      │        │        │
    Tree1    Tree2    Tree3 ...
      │        │        │
      └────────┴────────┘
               │
     Classification → Majority Vote
          Regression → Average
               │
       Final Prediction
```

---

# ⭐ 一句话总结（考试必背）

Random Forest 是一种 **Bagging Ensemble** 方法，它通过 **Bootstrap Sampling** 和 **Random Feature Selection** 构建大量彼此差异较大的 Decision Tree，最后使用 **分类投票（Majority Vote）** 或 **回归平均（Average）** 得到最终预测，从而降低模型的 **Variance** 并提高泛化能力。
