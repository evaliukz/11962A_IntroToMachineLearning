# Pretraining vs Fine-tuning（预训练 vs 微调）

## 为什么要分两步训练？

如果让一个 AI 一开始就学习："帮医院诊断肺癌", 它什么都不会。

因为：

- 不认识英语
- 不懂数学
- 不懂医学
- 不知道什么是图片
- 不知道什么是语言

所以： **必须先把 AI 培养成一个"什么都懂一点的人"，然后再教它某个专业。**

这就是：

```
Pretraining
      ↓
Foundation Model
      ↓
Fine-tuning
      ↓
Specialized Model
```

---

# 第一阶段：Pretraining（预训练）

一句话： **学习通用知识。**

例如：GPT 在预训练时：

学习：

- Wikipedia
- Books
- GitHub
- 新闻
- 网页

几十 TB 的数据。

目的不是：回答某一个问题。

而是： **学习整个世界。**

例如, 它慢慢学会：

```
猫会喵喵叫

巴黎在法国

Python 是编程语言

苹果可以吃

太阳从东边升起
```

这些都是：**General Knowledge（通用知识）**

---

# 为什么叫 Self-supervised Learning？

PPT 第一条： Self-supervised learning

很多同学第一次都会疑惑：谁给它答案？

答案： **数据自己给自己答案。**

例如一句话：The cat sits on the ____.

模型要预测：mat

这里输入：The cat sits on the

标签：mat, 并不是人工标注。而是：原始文本自己提供。

所以叫： **Self-supervised（自监督）**

没有老师。数据自己就是老师。

---

# Broad Knowledge about the Dataset

PPT： broad knowledge about the dataset

意思：模型不是学某一个任务,而是什么都学一点。

例如：

```
数学

英语

编程

历史

医学

法律
```

最后得到：Foundation Model

例如：GPT,Llama,Gemini,Claude

---

# Expensive Computation

PPT：expensive computation 为什么？

因为：数据太多。

例如 GPT：

```
几万亿 Token

↓

训练几个月

↓

几千块 GPU

↓

花几千万美元
```

所以：Pretraining **非常昂贵。**

通常普通公司做不了。

---

# Foundation Model（基础模型）

PPT：produces a foundation model

意思：预训练结束以后，得到Foundation Model

例如：

```
GPT-4

Llama

Qwen

Gemma
```

它们已经懂很多知识。

但是还不是专家。

例如可以聊天, 但是不一定最会：

```
法律

医疗

金融
```

---

# 第二阶段：Fine-tuning（微调）

一句话： **教 AI 学一个具体工作。**

例如Foundation Model：会很多东西。

现在：让它只学医疗。或者只学法律。或者只学微软内部代码。这就是Fine-tuning。

---

# PPT 第一条

> update the weights of a pretrained model

什么意思？不是重新训练整个模型。而是从已经训练好的 GPT开始。然后继续训练。

更新：Weights（参数）

例如：

```
GPT

↓

继续学习

↓

Medical GPT
```

模型参数：发生一点改变。但是不用重新学英语。不用重新学数学。

---

# 用 Supervised Dataset

PPT：using a specific supervised dataset

和 Pretraining 最大区别：

Pretraining：Self-supervised

Fine-tuning：Supervised

例如：Question -> Correct Answer

都是人工准备好的。

例如：

```
Question:
肺癌怎么办？

Answer:
......
```

所以：Fine-tuning通常需要Label（标签）。

---

# Improve Performance on Specific Tasks

PPT： improve performance on specific tasks

例如：GPT什么都会一点。但是医院希望医学回答更专业。于是继续训练：100 万条医疗问答

得到：Medical GPT, 聊天能力变化不大。但是医学提升很多。

---

# 一个生活中的例子

想象培养一个医生。

## 第一步

大学：数学英语生物化学解剖学, 学很多。这就是：Pretraining, 得到：Foundation Doctor

---

## 第二步

进入心脏科。天天学习：心电图,心脏手术,病例, 这就是：Fine-tuning

最后：成为Cardiologist

---

# 整个流程

```
大量互联网数据

↓

Pretraining

↓

Foundation Model

↓

Specific Dataset

↓

Fine-tuning

↓

Task-specific Model
```

---

# Pretraining vs Fine-tuning

| Pretraining | Fine-tuning |
|--------------|-------------|
| 学通用知识 | 学具体任务 |
| Self-supervised | Supervised |
| 数据量巨大 | 数据量较小 |
| 成本极高 | 成本低很多 |
| 得到 Foundation Model | 得到 Specialized Model |
| 一般训练一次 | 可以训练很多次 |

---

# 面试最容易问

## 什么是 Pretraining？

> 使用海量无标签数据，通过 Self-supervised Learning 学习通用知识，训练出 Foundation Model。

---

## 什么是 Fine-tuning？

> 在预训练模型基础上，使用有标签的数据继续训练，使模型适应某个具体任务。

---

## 为什么需要 Fine-tuning？

因为：

Foundation Model：

```
什么都会一点
```

但是：

```
不够专业。
```

Fine-tuning：

让模型：

```
成为专家。
```

---

# 一句话总结

> **Pretraining teaches the model to understand the world; Fine-tuning teaches it to perform a specific task well.**
