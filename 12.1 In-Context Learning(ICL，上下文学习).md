# In-Context Learning（ICL，上下文学习）

## 一句话理解

> **不训练模型，只通过 Prompt 教模型怎么做。**

例如：

```
Translate to Chinese:

Dog -> 狗
Cat -> 猫
Bird ->
```

模型马上知道：

```
Bird -> 鸟
```

它：

- 没有重新训练
- 没有更新参数
- 只是根据 Prompt 学会当前任务

这就是：**In-Context Learning**

---

# 工作流程

```
User Prompt
        ↓
LLM
        ↓
Prediction
```

例如：Summarize this article.

或者：Translate to French.

模型直接完成任务。

---

# 为什么叫 In-Context？

因为：模型学习的信息全部来自：当前 Prompt（Context）

例如：

```
English → Chinese

Dog → 狗
Cat → 猫
Bird →
```

模型：看到前两个例子，就推断：

```
Bird → 鸟
```

Prompt 就像：临时说明书。

---

# PPT 第一条

> The prompt is read by the model and helps activate different paths.

什么意思？模型内部已经学会：很多知识。

Prompt 的作用：不是新增知识。

而是：**激活（Activate）不同知识。**

例如：同一个 GPT：

Prompt：Write Python code.

激活：Programming Knowledge

Prompt：Translate to Japanese.

激活：Language Knowledge

Prompt：Explain quantum mechanics.

激活：Physics Knowledge

所以：Prompt 像：**告诉模型："现在该用哪部分知识。"**

---

# Latent Concept Retrieval

PPT： latent concept retrieval

意思：模型已经知道很多东西。

Prompt：只是帮助**把相关知识取出来（Retrieve）。**

例如：

```
GPT 已经会 Python。

Prompt：

Write a binary search.
```

不是Prompt 教 GPT Python。

而是Prompt 激活：Python Concepts

---

# PPT 第二条 No weights are actually changed.

这是：ICL 和 Fine-tuning 最大区别。

ICL：Weights -> 完全不变

Fine-tuning：Weights -> Update -> New Model

所以：ICL**不会训练模型。**

---

# PPT 第三条

> Low cost, but recurring.

什么意思？每次都要重新输入 Prompt。

例如：Translate to Chinese.

下一次还得再写一次。

因此：

```
Prompt

↓

Inference

↓

结束
```

不会永久保存。

所以虽然成本很低。但是每次都要重新做。

---

# In-Context Learning vs Fine-tuning

| In-Context Learning | Fine-tuning |
|----------------------|-------------|
| 只修改 Prompt | 更新模型参数 |
| 不训练模型 | 继续训练模型 |
| Weights 不变 | Weights 改变 |
| 成本低 | 成本较高 |
| 每次都需要 Prompt | 一次训练，长期使用 |
| 临时学习 | 永久学习 |

---

# 一个生活例子

假设有一个很聪明的员工。

## In-Context Learning

老板说：

```
今天所有回复都要礼貌一点。
```

员工马上照做。但是明天又忘了。需要重新提醒。

---

## Fine-tuning

老板培训员工一个月。

以后员工永远都会礼貌回答。

---

# 一句话总结

> The model does not learn new patterns during in-context learning. The patterns were already learned during pre-training, and the prompt simply activates the relevant knowledge for the current task.

中文就是：模型在 In-Context Learning 中并没有学习新的知识，它识别和使用的模式都是在 Pre-training 阶段学到的。Prompt 的作用只是激活这些已经存在的知识，而不是训练模型。

# RAG是ICL吗？
不是。一个很好的类比，假设你考试。

ICL:老师直接把参考资料放到试卷第一页。

你阅读资料 -> 回答问题

RAG:老师没有给资料。你先：

去图书馆找资料 ->拿回来放桌上 -> 阅读资料 -> 回答问题

真正回答问题时，你还是：阅读上下文（In-Context Learning）只是RAG 多了去图书馆找书（Retrieval）


| 方法                  | 更新模型参数 | 有 Retrieval | 利用 Prompt |
| ------------------- | ------ | ----------- | --------- |
| Fine-tuning         | ✅ 是    | ❌ 否         | 可有可无      |
| In-Context Learning | ❌ 否    | ❌ 否         | ✅ 是       |
| RAG                 | ❌ 否    | ✅ 是         | ✅ 是       |

