# From Base Model to Reliable ML Product

## 1. 整份课件的核心思想

一个真实可用的机器学习产品，不只是一个训练好的模型。真正的生产系统不是“模型训练完就结束”，而是一个持续循环：

<pre>
Pretrained
    ↓
Adapted
    ↓
Evaluated
    ↓
Served
    ↓
Monitored
    └────────→ 重新适配和评估
</pre>

完整系统需要同时回答四个问题：

1. Base model 是怎样形成的？
2. 应该用什么方式适配它？
3. 怎样证明新版本真的更好？
4. 怎样低成本、稳定、安全地运行它？

对应四个阶段：

| 阶段 | 核心问题 | 主要内容 |
|---|---|---|
| Base | 什么塑造了模型？ | 训练数据、Tokenizer、精度、内存 |
| Adapt | 最小的有效改动是什么？ | Prompt、RAG、LoRA、SFT、Preference Tuning |
| Evaluate | 怎样知道它改进了？ | 任务指标、回归测试、人工评估 |
| Operate | 能否可靠运行？ | Latency、Throughput、Cost、Monitoring、Rollback |

本课使用一个 Helpdesk Triage Assistant 作为贯穿案例：

```text
输入：
“I can’t log in; my account is locked.”

输出：
{
    "category": "password",
    "urgency": "high",
    "reply": "..."
}
```

这类系统并不是只输出一段自然语言，而是同时要完成：

- 分类；
- 判断紧急程度；
- 生成结构化 JSON；
- 生成对用户有帮助的回复；
- 在生产环境中稳定运行。

## 理解 Base Model 是怎样形成的

在考虑微调或部署之前，首先需要理解两件事：

(1) Base model 从什么数据中学到了什么？
(2) 训练和运行这个模型需要多少计算与内存？

一个模型的行为不仅由 Transformer 架构决定，还会被以下因素塑造：

- 训练语料；
- 数据过滤方法；
- 数据去重；
- Tokenizer；
- 训练精度；
- 参数量；
- Optimizer；
- Batch size；
- Sequence length。

# 2. Pretraining Data Pipeline

大模型预训练数据不是把整个互联网直接倒进模型。

真实的数据管道通常类似：

```text
Raw Web Data
    ↓
Text Extraction
    ↓
Language Identification
    ↓
Deduplication
    ↓
Heuristic Quality Filters
    ↓
Model-based Quality Filters
    ↓
Final Training Corpus
```

课件中的 FineWeb 示例：

| 阶段 | 剩余文档数 |
|---|---:|
| Raw Common Crawl | 200B |
| Text extraction + language ID | 36B |
| Deduplication | 20B |
| Heuristic quality filters | 17B |
| Model-based quality filter | 15B |

最终只保留了大约：

$$
\frac{15B}{200B}=7.5\%
$$

也就是起始数据中只有约 8% 被保留下来。

## 2.1 为什么数据过滤如此重要？

过滤规则决定了模型长期看到什么、不看到什么。

例如，如果过滤系统偏爱：

- 百科全书式写作；
- 标准英语；
- 正式网页；
- 长篇结构化文本；

那么模型可能会更擅长这些类型的内容，却较少学习：

- 口语；
- 小众领域术语；
- 非标准语言；
- 少数群体表达方式。

因此：

> Dataset filtering is a modeling decision.

数据管道本身就在塑造模型。

## 2.2 “过滤器本身也是模型”

FineWeb-Edu 中会训练一个 classifier 来判断：

> “这段内容是否像百科全书中会被引用的内容？”

然后只保留分数较高的数据。

因此数据生成过程中已经包含机器学习：

```text
Raw data
    ↓
A model scores data quality
    ↓
Selected data trains another model
```

这说明：

> 模型的训练分布部分由另一个模型决定。

# 3. Tokenizer 与 Byte-Pair Encoding

模型看不到“单词”，它只能看到 Token IDs。

例如：

```text
reset my andrewid password
```

如果 Tokenizer 不认识 `andrewid`，它可能被切成：

```text
and
rew
id
```

甚至：

```text
a n d r e w i d
```

如果 Tokenizer 见过大量 Helpdesk 数据，它可能直接拥有：

```text
andrewid
```

这个完整 Token。

## 3.1 BPE 的核心思想

Byte-Pair Encoding 从最小单位开始，例如字符：

```text
p a s s w o r d
```

然后反复查找最常出现的相邻 Token Pair，并将其合并。

例如：

```text
s + s → ss
```

之后可能继续：

```text
pa + ss → pass
```

最终形成词或子词单位。

## 3.2 Tokenizer 为什么重要？

### Tokens are money

API 和推理成本通常按输入和输出 Token 计费。

如果一个专业词被拆成 6 个 Token，而不是 1 个 Token：

```text
Token Cost ≈ 6 倍
```

### Tokens are context

Context window 是按 Token 数计算的。

如果专业词汇总被切碎，能放进 Context 的有效信息会减少。

### Output tokens add latency

Autoregressive decoding 一次只产生一个 Token。

输出越长，Decode step 越多。

### Tokens affect quality

一个完整概念如果有独立 Token，模型更容易形成稳定表示。

如果它总被拆成杂乱碎片，模型学习这个概念会更困难。

## 3.3 实际工程建议

选择 Base Model 前，应测试它如何 Tokenize 自己领域中的关键词，例如：

```text
AndrewID
Microsoft Entra
Conditional Access
service principal
```

不要只看 Benchmark，也要看：

```python
tokenizer.encode(domain_term)
```

到底产生了多少个 Token。

# 4. Full Fine-Tuning 的训练内存估计

对于使用 mixed precision 和 AdamW 的一种常见训练配置，模型状态内存可以粗略估计为：

$$
M_{\text{train}}
\approx
2P+4P+2P+8P
=
16P
$$

其中：

- $P$ 是模型参数数量；
- 每个系数表示每个参数需要多少 Bytes。

具体来源：

| 内容 | Bytes / Parameter |
|---|---:|
| bf16 model weights | 2 |
| fp32 master weights | 4 |
| gradients | 2 |
| Adam first and second moments | 8 |
| 总计 | 16 |

因此：

$$
M_{\text{train}}\approx16P
$$

## 4.1 8B 模型示例

假设模型有：

$$
P=8.03\times10^9
$$

则模型状态内存大约是：

$$
16\times8.03\times10^9
=
128.48\times10^9\text{ bytes}
$$

按课件使用的换算方式，约为：

$$
119.7\text{ GB}
$$

其中：

| 部分 | 内存 |
|---|---:|
| bf16 weights | 15.0 GB |
| fp32 master copy | 29.9 GB |
| gradients | 15.0 GB |
| Adam moments | 59.8 GB |
| 总计 | 119.7 GB |

## 4.2 这还不是 Peak Memory

上面的 $16P$ 只计算了 Model State。

实际 Peak Memory 还包括：

```text
Model state
+ Activations
+ Temporary buffers
+ Framework overhead
```

### Activations

Forward pass 生成的中间值。

Backpropagation 为了计算梯度，需要保存许多中间 Activation。

Activation memory 会随以下因素增加：

- Batch size；
- Sequence length；
- Number of layers；
- Hidden dimension；
- Attention implementation。

所以：

> 8B 模型训练需要约 120GB，不代表 120GB GPU 就一定能训练。

因为 Peak VRAM 可能明显更高。

# 5. 降低训练显存的四种方法

## 5.1 Lower Precision

使用更少的 Bit 存储或计算 Tensor，例如：

```text
FP32 → BF16 → FP16 → INT8 → INT4
```

优点：

- 每个值占用更少内存；
- 可能提高吞吐量。

代价：

- 数值稳定性风险；
- 可能产生精度损失；
- 部分计算仍需高精度。

---

## 5.2 Activation Checkpointing

普通 Backpropagation：

```text
Forward
    ↓
保存大部分 Activations
    ↓
Backward 直接使用
```

Checkpointing：

```text
Forward
    ↓
只保存部分 Activations
    ↓
Backward 时重新计算缺失部分
```

它交换的是：

```text
Less memory
↔
More computation
```

---

## 5.3 Shard Model State

使用：

- ZeRO；
- FSDP；

将以下内容分到多块 GPU：

- optimizer states；
- gradients；
- parameters。

这样每块 GPU 只保存一部分模型状态。

代价是：

```text
Less memory per GPU
↔
More communication
```

---

## 5.4 Train Fewer Parameters

冻结 Base Model，只训练很小的 Adapter，例如 LoRA。

此时不需要为所有 Base Parameters 保存：

- 梯度；
- Optimizer state。

通常这是最显著、最实用的内存降低方式之一。

# 6. Adaptation Ladder

适配模型时，不应该一开始就选择最贵的方法。

正确策略是：

> Choose the least complex intervention that meets the requirement.

可以将适配方式从简单到复杂排列为：

```text
Prompting
    ↓
Few-shot Prompting
    ↓
Retrieval-Augmented Generation
    ↓
SFT / LoRA
    ↓
Preference Tuning
    ↓
Continued Pretraining
```

## 6.1 Prompting

特点：

- 不训练；
- 不更新权重；
- 成本最低；
- 上线最快。

适合：

- 模型已经具备能力；
- 只是需要更清楚的指令；
- Output format 可以通过 Prompt 控制。

---

## 6.2 Few-shot Prompting

在 Prompt 中放入几个示例：

```text
Input → Desired Output
Input → Desired Output
New Input →
```

优点：

- 不训练；
- 可以临时规定格式和行为。

代价：

- Prompt 更长；
- 每次请求都重复付出 Token 成本；
- 示例数量受 Context 限制。

---

## 6.3 Retrieval-Augmented Generation

适合模型缺少：

- 内部政策；
- 最新价格；
- 客户记录；
- 实时事实；
- 需要出处的知识。

核心：

```text
Query
    ↓
Retrieve relevant evidence
    ↓
Put evidence in prompt
    ↓
Generate grounded answer
```

---

## 6.4 SFT / LoRA

当模型知道事实，但行为不稳定时使用。

适合改变：

- 输出格式；
- 语气；
- Domain language；
- 决策风格；
- 稳定的任务模式。

---

## 6.5 Preference Tuning

当需要模型更符合偏好，而不是简单模仿一个标准答案时使用。

例如：

- 哪个回答更有帮助；
- 哪个回答更安全；
- 哪个回答更简洁；
- 哪个回答更符合公司语气。

---

## 6.6 Continued Pretraining

当 Base Model 几乎不理解整个领域时才考虑。

例如：

- 特殊科学语言；
- 新语言；
- 高度专业的代码；
- 与原始预训练数据差异极大的文档。

代价最大，需要大量未标注 Domain Corpus 和 Compute。

## RAG 就是：模型先去“翻书找答案”，再根据找到的资料回答你，而不是只靠自己脑子里的记忆。

RAG = 搜索（Retrieve）+ 阅读（Read）+ 回答（Generate）。模型不会凭空猜，而是先去知识库找资料，再基于资料生成答案。

所以你以后可以把 RAG 理解成：

"给 AI 接了一个会自动查资料的大脑外挂。"

模型本身没有变聪明，只是回答之前多了一步“查资料”。这也是为什么课程里强调：

Fine-tuning 解决的是 "How"（怎么回答、什么风格、什么格式）;
RAG 解决的是 "What"（回答需要哪些最新知识）

# 7. Retrieval vs Fine-Tuning

最好记的区别：

> Retrieval supplies what the model should know now.

> Fine-tuning changes how the model behaves persistently.

## 7.1 Fine-tuning 解决 How

Fine-tune 用于调整：

- Format；
- Tone；
- Domain language；
- Stable decision patterns。

例如：

```text
始终输出指定 JSON Schema
始终使用礼貌但简洁的语气
对类似 ticket 使用一致分类规则
```

---

## 7.2 Retrieval 解决 What

Retrieval 用于提供：

- 最新政策；
- 当前库存；
- 用户记录；
- 实时价格；
- 带来源的证据。

例如：

```text
“目前学校账户锁定政策是什么？”
```

政策可能下个月变化，因此不应永久写入权重。

---

## 7.3 为什么不把所有政策都 Fine-tune 进去？

因为写进权重后：

- 难以更新；
- 无法直接引用；
- 难以审计；
- 政策改变时不会自动更新；
- 不容易知道模型使用的是哪个版本。

RAG 中只需：

```text
更新文档
    ↓
重新 Index
    ↓
新查询自动获得最新内容
```

因此：

| 问题类型 | 首选方法 |
|---|---|
| Missing current facts | RAG |
| Needs citations | RAG |
| Wrong format | Fine-tuning |
| Wrong tone | Fine-tuning |
| Inconsistent behavior | Fine-tuning |
| Both facts and behavior | RAG + Fine-tuning |

# 8. RAG End-to-End Pipeline

## Offline 路径

```text
Documents
    ↓
Chunk by structure
    ↓
Embedding model
    ↓
Vector index
```

## Online 路径

```text
User query
    ↓
Query embedding
    ↓
Retrieve candidates
    ↓
Rerank
    ↓
Put top chunks into prompt
    ↓
LLM generation
```

## 8.1 Chunking

不应盲目按照固定字符数切分。

例如 500-character window 可能将：

```text
“Reset the password unless the account is locked.”
```

切成：

```text
Chunk 1: Reset the password
Chunk 2: unless the account is locked
```

这样条件和规则被分离。

更好的方式是按照：

- Heading；
- Paragraph；
- Semantic boundary；
- Sentence structure；

进行切分，并加入适当 Overlap。

## 8.2 Embedding Model

General embedder 可能不理解 Domain Synonyms。

例如：

```text
AndrewID
student identity
campus account
```

对学校 Helpdesk 来说可能指同一概念，但通用模型可能将它们放得很远。

因此需要使用真实查询测试：

- Recall@k；
- MRR；
- Top-k hit rate；
- Domain synonym retrieval。

## 8.3 Reranker

Retriever 通常分别编码：

```text
query embedding
chunk embedding
```

这样检索速度快，但交互理解较弱。

Reranker 会把：

```text
query + candidate chunk
```

一起输入模型，因此更能理解细微区别。

典型 Cascade：

```text
Retrieve top 50 cheaply
    ↓
Rerank to top 5 carefully
```

第一阶段追求高 Recall，第二阶段追求高 Precision。

# 9. LoRA in Practice

LoRA 不直接修改原始权重 $W$，而是学习一个低秩更新：

$$
W'
=
W
+
\frac{\alpha}{r}BA
$$

其中：

- $W$：冻结的 Base Weight；
- $A$ 和 $B$：可训练的小矩阵；
- $r$：Rank；
- $\alpha$：Scaling Factor。

矩阵形状通常是：

$$
W\in\mathbb{R}^{d\times k}
$$

$$
A\in\mathbb{R}^{r\times k}
$$

$$
B\in\mathbb{R}^{d\times r}
$$

因此：

$$
BA\in\mathbb{R}^{d\times k}
$$

与原始矩阵 $W$ 形状相同。

---

## 9.1 Rank $r$

$r$ 控制 Adapter Capacity。

较小的 $r$：

- 参数少；
- 显存低；
- 训练快；
- 可能 underfit。

较大的 $r$：

- 表达能力更强；
- 参数和显存增加；
- 可能更容易 overfit。

正确策略：

> Start modestly, increase only when validation shows underfitting.

---

## 9.2 Scaling $\alpha$

更新被缩放为：

$$
\frac{\alpha}{r}BA
$$

$\alpha$ 控制 LoRA 更新相对 Base Weight 的强度。

常见设置：

```python
r = 16
lora_alpha = 32
```

此时：

$$
\frac{\alpha}{r}=2
$$

---

## 9.3 Target Modules

常见起点是 Attention Projections：

```text
q_proj
k_proj
v_proj
o_proj
```

也可以加上 MLP Linear Layers，增加 Capacity，但会：

- 增加参数；
- 增加显存；
- 增加训练时间。

---

## 9.4 Learning Rate

LoRA 参数量小，通常能承受比 Full Fine-Tuning 更高的 Learning Rate。

但不能照搬固定答案，仍需使用 Validation Set 调参。

---

## 9.5 Merge 还是保留 Adapter？

### Merge

将 LoRA Update 合并进 Base Weight：

```text
Wmerged = W + LoRA update
```

适合：

- 单一模型部署；
- 简单 Artifact；
- 不需频繁切换任务。

### Keep Separate

Base Model 与 Adapter 分开。

适合：

- 一个 Base Model 服务多个任务；
- 快速切换 Adapter；
- 快速 Rollback；
- 节省多个完整模型副本。

---

## 9.6 为什么 LoRA 节省内存？

Base Model：

```text
stored
mostly frozen
```

Adapter：

```text
trainable
needs gradients
needs optimizer state
```

只有 Adapter Parameters 需要：

- Gradient；
- Adam moments；
- Training update。

因此训练状态内存大幅降低。

# 10. Fine-Tuning Dataset Quality

课件强调：

> For behavior tuning, clean and representative examples matter more than raw count.

这并不意味着：

```text
1000 个样本永远足够
```

真正含义是：

> 大量噪声数据不能替代高质量监督。

## 10.1 Good Dataset 的四个条件

### Consistent

类似输入应获得类似标签。

否则模型收到矛盾监督：

```text
相同 Ticket
一次标 password
一次标 account
```

### Covers the Tail

必须包含少见但代价高的案例。

例如：

- PII 泄露；
- Security Incident；
- Account Takeover；
- Emergency Escalation。

### Correct Shape

训练目标必须严格符合最终 Contract。

例如生产要求：

```json
{
  "category": "...",
  "urgency": "...",
  "reply": "..."
}
```

训练数据也必须保持完全一致的字段与格式。

### Honest Split

不能随机拆分高度相似样本，使 near-duplicate 同时进入 Train 和 Validation。

更合理的是：

- 按时间切分；
- 按 Incident 切分；
- 按客户或会话切分。

---

## 10.2 只在 Assistant Tokens 上计算 Loss

Chat SFT 的输入一般是：

```text
System message
User message
Assistant answer
```

若目标是学习 Assistant Behavior，通常只对 Assistant 部分计算 Loss。

概念代码：

```python
input_ids = tokenize(system + user + assistant)

labels = input_ids.clone()

labels[:assistant_start] = IGNORE_INDEX
```

这样 Loss 只计算：

```text
assistant tokens
```

否则模型可能被奖励去复制：

- System prompt；
- User input。

---

## 10.3 Stop from Validation, not Habit

不要固定训练 3 Epoch 就结束。

小型数据集可能很快过拟合。

需要监控：

- Validation loss；
- Held-out task metrics；
- General capability canary。

# 11. Domain Improvement 与 General Regression

微调中可能出现：

```text
Domain task gets better
General capability gets worse
```

例如：

- Helpdesk 分类更准确；
- 但一般问答能力下降；
- 语言变得僵硬；
- 数学或代码能力退化。

## 11.1 为什么普通 Training Log 看不出来？

如果只看 Domain Validation：

```text
Domain loss keeps decreasing
```

一切看起来正常。

但另一条 General Capability Curve 可能正在恶化：

```text
General loss keeps increasing
```

因此至少需要两个 Validation Sets：

```text
Domain Validation Set
General Capability Canary Set
```

## 11.2 如何应对？

### Early Stopping

当目标指标不再改进时停止训练。

### Capability Canary

保留一小组 Base Model 原本能够回答的问题。

例如：

- 基础数学；
- 一般问答；
- JSON Follow Instruction；
- 简单代码；
- 安全拒绝。

### Mix or Regularize

训练时混入部分通用样本，或使用约束减少偏移。

### Measure the Actual Method

LoRA 通常比 Full Fine-Tuning 干扰更小，但不是绝对保证。

因此仍必须进行 General Regression Test。

# 12. Build the Test Before the Training Run, Evaluation 必须先于 Training

如果训练前没有定义：

```text
什么叫 Better？
```

训练后就无法可靠判断是否改进。

因此正确流程是：

```text
Define requirement
    ↓
Build evaluation
    ↓
Record baseline
    ↓
Train or adapt
    ↓
Run the same evaluation
    ↓
Compare
```

而不是：

```text
Train first
    ↓
Look at some examples
    ↓
Feel that it looks better
```

# 13. Evaluation Taxonomy 四种 Evaluation 类型

Evaluation 方法取决于：

1. Answer Space 是否封闭；
2. 是否存在可自动验证的正确答案。

| 类型 | 示例 | 检查方法 |
|---|---|---|
| Closed-ended | 分类、数字、选择题 | Exact match |
| Open but verifiable | Code、JSON、Proof、DB state | Programmatic checker |
| Open and non-verifiable | Tone、helpfulness | Human / LLM judge |
| Static vs dynamic | 固定 Benchmark、实时知识 | 检查 contamination 和 cutoff |

---

## 13.1 Closed-ended

答案是有限集合，例如：

```text
password
network
billing
hardware
```

可以使用：

- Accuracy；
- Precision；
- Recall；
- F1；
- Confusion Matrix。

成本低、结果稳定，可以每次 Commit 都运行。

---

## 13.2 Open but Verifiable

答案虽然开放，但能自动检查。

例如：

- Code 是否通过 Tests；
- JSON 是否 Parse；
- Required Fields 是否存在；
- SQL 执行后 Database State 是否正确；
- 数学答案是否被 Symbolic Checker 验证。

应尽量自动化。

---

## 13.3 Open and Non-Verifiable

没有唯一正确答案，例如：

- Tone；
- Helpfulness；
- 是否适合直接发送；
- Writing Quality。

通常使用：

- Human Review；
- Rubric；
- LLM Judge。

成本较高，并且存在噪声。

---

## 13.4 Static vs Dynamic

固定 Benchmark 可能进入训练数据。

模型分数变高，可能是因为：

```text
Memorized benchmark
```

而非真正泛化。

因此课件推荐使用自己的：

```text
Private hand-labeled task-specific set
```

例如 150 条内部 Helpdesk Tickets。

这些数据从未公开，更能反映部署效果。

---

## 13.5 将 Triage Task 拆开评估

一个完整输出可以拆成：

### Category

Closed-ended：

- Accuracy；
- Confusion Matrix。

### JSON Contract

Open but verifiable：

- Parse；
- Schema Validation；
- Required Fields。

### Drafted Reply

Open and non-verifiable：

- Human Review；
- Judge Model；
- Rubric。

不要用一个模糊的总分覆盖整个系统。

# 14. LLM Judge 的风险

LLM Judge 不是 Ground Truth。

它本身也是一个模型，因此同样有：

- Training distribution；
- Bias；
- Variance；
- Failure modes。

## 14.1 常见 Bias

| Failure | 表现 | 控制方式 |
|---|---|---|
| Verbosity bias | 长答案看起来更好 | 使用 Rubric，控制长度 |
| Position bias | 偏好第一个或第二个答案 | Swap order 后取平均 |
| Self-preference | 偏爱熟悉的模型风格 | 与其他 Judge 或人类比较 |
| Variance | 多次评分不一致 | Temperature 0，重复评估 |
| Vague score | 7/10 没稳定含义 | 定义具体 Criteria 和 Examples |

## 14.2 先校准 Judge

步骤：

```text
Human labels a sample
    ↓
Judge labels same sample
    ↓
Measure judge–human agreement
    ↓
Compare with human–human agreement
```

如果 Judge 与人类的一致程度很低，就不能将其当作主要评估指标。

## 14.3 合理用途

- 版本回归对比；
- 标记可疑案例；
- 检查是否提及必要行动；
- 大规模筛选后交给 Human Review。

## 14.4 不应单独依赖的情况

- 高风险医疗或法律输出；
- 对外发布的核心质量指标；
- 绩效或政策相关判断；
- 最终产品验收。

# 15. Retry Success vs Workflow Success, pass@k 与多步骤工作流可靠性

这两个公式看起来相似，但回答的是相反的问题。

## 15.1 Best-of-k / Retry

单次成功概率为 $p$。

尝试 $k$ 次，至少一次成功的概率：

$$
\mathrm{pass@k}=
1-(1-p)^k
$$

因为：

$$
P(\text{all fail})=(1-p)^k
$$

所以：

$$
P(\text{at least one succeeds})=
1-(1-p)^k
$$

例如：

$$
p=0.8,\quad k=8
$$

则：

$$
1-(1-0.8)^8=
1-0.2^8
\approx99.9997\%
$$

适用于：

- Sampling；
- Candidate generation；
- Code generation retries；
- Best-of-n selection。

---

## 15.2 All-Steps Workflow

如果一个工作流必须连续完成 $k$ 个步骤，每一步成功率为 $p$：

$$
P(\text{all steps succeed})=p^k
$$

例如：

$$
p=0.8,\quad k=8
$$

则：

$$
0.8^8
\approx16.8\%
$$

这说明：

```text
每一步 80% 看起来不错
```

但 8 步 Agent Workflow 的整体成功率很低。

## 15.3 产品含义

- Retry 可以提高 Sampling Task 成功率；
- Agent Workflow 会累积失败；
- 减少步骤可能比将模型从 80% 提升到 82% 更有效；
- 指标必须匹配真实用户体验。

# 16. Evaluation Harness 就是一套 Test Suite

课件将 ML Evaluation 类比为普通软件测试。

## 16.1 Golden Set

建立 50–200 个：

- Hand-labeled；
- Frozen；
- Version-controlled；
- Representative；

的测试样本。

最好按时间或 Incident 切分。

---

## 16.2 Regression Suite

每次发现并修复一个 Bug，就将其加入永久测试。

```text
Every bug becomes a test case.
```

这样同一个错误不会重复发生。

---

## 16.3 Capability Canary

保留模型改动前能够完成的一组通用任务。

用于发现：

- Forgetting；
- General capability regression；
- Format following regression；
- Safety behavior regression。

---

## 16.4 CI Gate

模型只有通过所有评估才能部署。

概念代码：

```python
BASELINE = 0.812

def test_golden_set_does_not_regress():
    score = evaluate(load_model(), golden_set())
    assert score >= BASELINE - 0.01

def test_every_output_is_valid_json():
    for ticket in golden_set():
        assert SCHEMA.validate(predict(ticket))

def test_general_capability_canary():
    assert general_qa_score() >= 0.58
```

核心原则：

> A model version that has not passed the gate cannot be deployed.

Gate 必须：

- 自动运行；
- 放在 Repository；
- 可以被讨论；
- 可以阻止 Deployment。

# 17. Hidden Technical Debt模型只是冰山的一小部分

用户通常只看到：

```text
Model
Retrieval
Tools
Demo
```

但真实系统还包括：

- Data versioning；
- Evaluation gates；
- Serving；
- Monitoring；
- Rollout；
- Rollback；
- Access control；
- Incident response。

因此：

> The visible model is only one component of a larger system.

一个模型 Demo 能运行，不代表：

- 可复现；
- 可扩展；
- 可监控；
- 可回滚；
- 可安全上线。

# 18. Reproducibility

要复现一次训练，至少需要 Version 以下四类内容：

## Code

不能只记录：

```text
main branch
```

应该记录：

```text
Git SHA
```

---

## Data

记录训练文件的：

```text
Content Hash
```

因为：

> Same code, different data

是最常见且最隐蔽的不可复现原因之一。

---

## Configuration

记录：

- Learning rate；
- Batch size；
- Rank；
- Alpha；
- Epochs；
- Seed；
- Precision；
- Base model version；
- Target modules。

---

## Environment

记录：

- Lockfile；
- Container digest；
- CUDA version；
- PyTorch version；
- Driver version。

---

## 18.1 Model Registry

一个 Registry 本质上可以只是一个表：

| 字段 | 含义 |
|---|---|
| Version | 模型版本 |
| Artifact | 权重或 Adapter |
| Metrics | Evaluation 结果 |
| Data hash | 数据版本 |
| Git SHA | 代码版本 |
| Promoted by | 谁批准上线 |
| Time | 上线时间 |

工具不是重点，纪律才是重点。

---

## 18.2 Promotion Workflow

```text
Training run
    ↓
Registry candidate
    ↓
Evaluation gate
    ↓
Production
```

如果失败：

```text
fails gate
    ↓
never reaches users
```

# 19. Serving Latency

LLM Serving 中需要区分两个指标。

## 19.1 TTFT

Time to First Token。

主要受 Prefill 影响。

Prefill 是对整个 Prompt 进行一次较大的并行计算：

```text
Prompt tokens
    ↓
Transformer forward
    ↓
First output token
```

长 Prompt 会显著增加 TTFT。

---

## 19.2 TPOT

Time Per Output Token。

主要受 Decode 影响。

Decode 是：

```text
one token at a time
```

每生成一个 Token：

- 读取 KV Cache；
- 运行新 Token 的 Transformer；
- 产生下一个 Token。

TPOT 决定文本在用户眼前出现的速度。

---

## 19.3 Latency vs Throughput

Latency：

> 单个用户感受到的等待时间。

Throughput：

> 系统单位时间能处理多少 Token 或 Request。

降低单请求 Latency 与提高总体 Throughput 不总是同一个目标。

# 20. KV Cache Memory: KV Cache 是 Serving 的内存墙

每层 Attention 都需要保存过去 Token 的 Key 和 Value。

课件给出的每 Token KV Cache 估算：

$$
\text{KV bytes per token}=
2
\times L
\times n_{\mathrm{kv}}
\times d_{\mathrm{head}}
\times \text{bytes}
$$

其中：

- $2$：Key 和 Value；
- $L$：层数；
- $n_{\mathrm{kv}}$：KV Heads 数量；
- $d_{\mathrm{head}}$：每个 Head Dimension；
- bytes：每个元素占用字节数。

## 20.1 8B 模型示例

假设：

$$
L=32
$$

$$
n_{\mathrm{kv}}=8
$$

$$
d_{\mathrm{head}}=128
$$

$$
\text{bytes}=2
$$

则：

$$
2\times32\times8\times128\times2=
131072\text{ bytes}
$$

约为：

$$
128\text{ KB per token}
$$

因此：

| Context length | 每个请求 KV Cache |
|---|---:|
| 2,048 Tokens | 0.25 GB |
| 8,192 Tokens | 1.00 GB |
| 128k Tokens | 16.0 GB |

## 20.2 Concurrency Hard Limit

如果 80GB GPU 中：

```text
15GB = model weights
65GB = KV cache capacity
```

8k Context 每个请求约 1GB：

```text
Maximum concurrent requests ≈ 65
```

这与 GPU 算得多快无关，而是内存容量上限。

## 20.3 三个优化手段

### Grouped-Query Attention

将 KV Heads 从 32 降至 8：

$$
4\times\text{ KV reduction}
$$

### KV Cache Quantization

例如 FP16 → FP8：

$$
2\times\text{ reduction}
$$

### Cap Context Length

不要因为模型支持 128k，就默认所有请求都保留 128k 历史。

# 21. Continuous Batching

## Static Batching 的问题

假设一个 Batch 中有四个请求：

```text
Request A：10 tokens
Request B：100 tokens
Request C：30 tokens
Request D：80 tokens
```

短请求结束后，其 Slot 可能一直空着，直到最长请求完成。

导致大量 Wasted Slot-Steps。

## Continuous Batching: Serving 中最大的免费收益之一

某个请求完成后，立即将新请求放进空出的 Slot。

```text
Request finishes
    ↓
Slot is freed
    ↓
New request enters immediately
```

这样可以显著提高 GPU Utilization。

## PagedAttention

KV Cache 被放进固定大小的 Page，而不是要求连续内存。

因此可以：

- 灵活添加请求；
- 释放已完成请求；
- 减少 Fragmentation；
- 支持 Continuous Batching。

课件示例：

```text
47% utilization
→
89% utilization
```

约等于：

$$
1.87\times\text{ throughput}
$$

同一 GPU，同一模型权重，仅靠调度方式就能获得巨大提升。

# 22. Cost per 1M Tokens

Serving 成本不能只看模型大小。

简化公式：

$$
\text{\$ per 1M tokens}
=
\frac{\text{GPU \$ per hour}}
{\text{tokens per second}\times3600}
\times10^6
$$

例如：

```text
GPU rental = $3/hour
Throughput = 2500 tokens/second
```

则满负载成本：

$$
\frac{3}
{2500\times3600}
\times10^6
\approx \$0.33
$$

每 1M Tokens 约：

$$
\$0.33
$$

如果 Useful Utilization 只有 30%，实际有效吞吐为：

$$
2500\times0.3=750
$$

成本变为：

$$
\frac{3}
{750\times3600}
\times10^6
\approx \$1.11
$$

## 22.1 为什么 Utilization 很重要？

专用 GPU 即使：

- 空闲；
- 等待请求；
- Batch 不充分；
- 被调度阻塞；

仍然按小时付费。

因此成本评估必须使用：

- 实际 Prompt / Output Length；
- 实际 Concurrency；
- 实际 Batch；
- 实际 Precision；
- 实际 Latency Target；
- 实际 Traffic Pattern。

## 22.2 什么时候 Self-host？

不只比较 Token Price，还要考虑：

- Data residency；
- Isolation；
- Custom weights；
- Custom adapters；
- Tail latency；
- Capacity guarantees；
- Serving stack control。

# 23. Data Drift

模型本身没有变化，输入分布仍可能变化。

例如学校 Helpdesk：

```text
Week 2
Midterms
New semester
Password reset season
```

每个时期的 Ticket Topic 都不同。

Drift 是：

> Signal, not diagnosis.

也就是说，分布变化并不自动等于模型质量下降。

---

## 23.1 Population Stability Index

对于每个 Bin：

- $e_i$：Expected proportion；
- $a_i$：Actual proportion。

$$
\mathrm{PSI}
=
\sum_i
(a_i-e_i)
\ln\left(\frac{a_i}{e_i}\right)
$$

PSI 越大，当前分布与 Reference 分布差异越大。

常见经验阈值：

```text
PSI < 0.10：较小变化
0.10–0.25：需要关注
PSI > 0.25：较大变化
```

但这些是行业经验，并不是数学定理。

---

## 23.2 除了 PSI 还应监控什么？

### Outcome Metrics

- Reopen rate；
- Escalation；
- User correction；
- Human override。

### Behavior Metrics

- Refusal rate；
- Schema failure；
- Output length；
- Retrieval hit rate；
- Cost per request。

### Input Metrics

- Topic distribution；
- Prompt length；
- PSI；
- KS；
- Language mix。

最重要的监控顺序是：

```text
1. Outcomes
2. Behaviors
3. Inputs
```

因为 Input Drift 只是预警，Outcome Regression 才是需要采取行动的证据。

# 24. Production Rollout

新模型不应该直接替换旧模型。

推荐阶段：

```text
Shadow
    ↓
Canary
    ↓
A/B Test
    ↓
Full Rollout
```

## 24.1 Shadow

```text
0% users see the new output
```

新模型处理真实流量，但输出不展示给用户。

优点：

- 有真实流量信号；
- 用户风险为零；
- 可以比较 Latency、Cost、Quality。

---

## 24.2 Canary

只给：

```text
1–5%
```

用户使用。

密切观察：

- Error；
- Latency；
- Complaint；
- Safety；
- Cost。

---

## 24.3 A/B Test

例如 50% 用户使用旧版本，50% 使用新版本。

比较真实业务指标。

---

## 24.4 Full Rollout

完全上线，但旧版本仍应保持可用：

```text
keep v7 warm
```

Rollback 应该是：

```text
configuration change
```

而不是重新训练。

---

## 24.5 Guardrails

模型不能是最后一道防线。

至少应包括：

### Schema Validation

Malformed JSON 直接拒绝，不传给下游系统。

### Input and Output Filtering

检查：

- PII；
- Prompt injection；
- Unsafe content。

尤其 Retrieved Documents 也属于 Untrusted Input。

### Fallback

低置信度或检测失败时，转人工。

```text
“I’m not sure. Let me get someone.”
```

是完全合理的产品行为。

# 25. Retraining Triggers

不应该只按照固定日历：

```text
Every 3 months retrain
```

而应根据证据触发。

## 25.1 Drift Alarm

输入分布变化，并且 Outcome 或 Eval 证明它影响了质量。

## 25.2 Evaluation Regression

Golden Set 下降，或 Capability Canary 检测到 Forgetting。

## 25.3 New Ground Truth

生产运行后积累了：

- User Corrections；
- Agent Edits；
- Human Escalations；
- Resolution Outcomes。

这些都是新的监督数据。

## 25.4 Policy or Freshness Change

政策、产品或事实改变，可能只需更新 RAG 文档，不一定要重新训练。

触发后重新回到 Adaptation Ladder：

```text
New evidence
    ↓
Choose intervention
    ↓
Estimate memory
    ↓
Train or update
    ↓
Evaluation gate
    ↓
Gradual rollout
```

# 26. Five Defensible Decisions

## 1. Which Intervention?

从 Prompting 开始。

- Changing facts → Retrieval；
- Persistent behavior → Fine-tuning；
- Missing domain capability → Continued pretraining。

## 2. Will It Fit?

明确估算：

- Weights；
- Optimizer states；
- Gradients；
- Activations；
- Buffers；
- KV cache；
- Precision；
- Sequence length。

## 3. Better by What Measure?

训练前构建：

- Task-specific Evaluation；
- Golden Set；
- Capability Canary；
- CI Gate。

## 4. What Does It Cost?

测量：

- Latency；
- Throughput；
- Memory capacity；
- Cost per request；
- Expected traffic。

## 5. When Should It Change?

根据以下证据更新：

- Evaluation；
- Drift；
- New labels；
- Policy changes；
- Freshness requirements。

最终结论：

> The model was never the hard part.  
> The loop around it is the engineering

# 27. NF4 Quantization

Uniform INT4 将数值范围均匀切成固定 Level。

问题是：

```text
神经网络权重大量集中在 0 附近
```

Uniform Quantization 却对稀疏的 Tail 和密集的零附近分配同样 Resolution。

NF4 根据近似 Normal Distribution 的 Quantiles 分配 Level：

- 0 附近 Level 更密；
- Tail Level 更稀。

因此更适合神经网络 Weight Distribution。

QLoRA 的典型结构：

```text
4-bit quantized base model
+
higher-precision LoRA adapters
```

Base 权重省内存，Adapter 保持可训练精度。

实际 Memory 和 Quantization Error 还取决于：

- Block size；
- Scale storage；
- Double quantization；
- Implementation。

# 28. Deduplication with MinHash and LSH

## 28.1 Shingles

先将文档表示为 Word n-gram 集合。

例如：

```text
machine learning system
```

2-gram shingles：

```text
machine learning
learning system
```

Near-duplicate 文档即使少量词变化，也会共享大量 Shingles。

## 28.2 Jaccard Similarity

$$
J(A,B)=
\frac{|A\cap B|}
{|A\cup B|}
$$

问题是如果有 $N$ 个文档，All-pairs Comparison 需要：

$$
O(N^2)
$$

Web Scale 下不可行。

## 28.3 MinHash

MinHash 具有性质：

$$
P\left(h_{\min}(A)=h_{\min}(B)\right)=
J(A,B)
$$

使用多个 Hash Functions 生成 Signature。

两个 Signature 中相同位置的比例，可以估计 Jaccard Similarity。

## 28.4 LSH

将 Signature 分 Band 放入 Bucket。

只有落入相同 Bucket 的文档成为 Candidate Pair。

然后才计算更精确的 Jaccard。

好处：

- 减少 Memorization；
- 减少 Benchmark Leakage；
- 减少重复训练 Compute；
- 平衡不同来源的影响。

# 29. DPO

DPO 使用偏好对直接训练 Policy，而不需要单独训练 Reward Model 再运行 Online RL。

## 29.1 Preference Pair

对于 Prompt $x$：

```text
y_w = preferred response
y_l = rejected response
```

## 29.2 Relative Improvement

定义：

$$
\Delta_y=
\log\pi_\theta(y|x)-
\log\pi_{\mathrm{ref}}(y|x)
$$

DPO Loss：

$$
\mathcal{L}_{\mathrm{DPO}}=
-\mathbb{E}
\left[
\log
\sigma
\left(
\beta
[
\Delta_w-\Delta_l
]
\right)
\right]
$$

直觉是：

> 当前 Policy 相对于 Reference Policy，应更多提高 Preferred Response，而不是 Rejected Response。

## 29.3 DPO 能做什么？

- 使用 Pairwise Preference Data；
- 不需要 Online RL；
- 用 Reference Policy 限制变化；
- 用 $\beta$ 控制更新强度。

## 29.4 DPO 不能做什么？

它无法学习从未出现在 Preference Pairs 中的行为。

因此仍需要：

- 高质量 Pair Construction；
- Representative Data；
- Held-out Evaluation。

# 30. Little’s Law

Little’s Law：

$$
L=\lambda W
$$

其中：

- $L$：系统中平均并发请求数；
- $\lambda$：到达率；
- $W$：请求平均停留时间。

例如：

$$
\lambda=40/\mathrm{min}=
0.67/\mathrm{s}
$$

$$
W=12\mathrm{s}
$$

所以：

$$
L=
0.67\times12
\approx8
$$

平均有 8 个请求正在系统中。

但：

> Average is not the peak.

真实 Capacity Planning 还必须考虑：

- Burst Traffic；
- Queueing Target；
- Prompt / Output Length Variation；
- KV Cache per Request；
- Failover；
- Headroom。

Little’s Law 只是第一步估算，最终必须 Load Test。

# 31. Drift Metrics

## 31.1 PSI

$$
\mathrm{PSI}
=
\sum_i
(a_i-e_i)
\log
\left(
\frac{a_i}{e_i}
\right)
$$

适合：

- 固定 Categories；
- Binned Continuous Features；
- 看每个 Bin 对 Drift 的贡献。

缺点：

- 依赖 Binning；
- 依赖 Smoothing；
- 不直接说明 Quality 是否下降。

## 31.2 Kolmogorov–Smirnov Statistic

$$
D
=
\sup_x
\left|
F_{\mathrm{ref}}(x)
-
F_{\mathrm{obs}}(x)
\right|
$$

它比较两个 Empirical CDF 的最大距离。

优点：

- Continuous Feature 不需预先分 Bin。

注意：

样本数量很大时，即使微小差异也可能产生显著 p-value。

因此应报告：

```text
Effect size
```

而不是只报告统计显著性。

## 31.3 监控顺序

```text
1. Outcomes
2. Behavior
3. Inputs
```

输入 Drift 只是：

```text
early warning
```

任务或 Outcome Regression 才是采取行动的真正证据。
