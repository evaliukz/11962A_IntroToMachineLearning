# From Base Model to Reliable ML Product

## 1. 整份课件的核心思想

一个真实可用的机器学习产品，不只是一个训练好的模型。真正的生产系统不是“模型训练完就结束”，而是一个持续循环：

<pre>
Pretrained
    ↓
Adapted
    ↓
Evaluated
    ↓
Served
    ↓
Monitored
    └────────→ 重新适配和评估
</pre>

完整系统需要同时回答四个问题：

1. Base model 是怎样形成的？
2. 应该用什么方式适配它？
3. 怎样证明新版本真的更好？
4. 怎样低成本、稳定、安全地运行它？

对应四个阶段：

| 阶段 | 核心问题 | 主要内容 |
|---|---|---|
| Base | 什么塑造了模型？ | 训练数据、Tokenizer、精度、内存 |
| Adapt | 最小的有效改动是什么？ | Prompt、RAG、LoRA、SFT、Preference Tuning |
| Evaluate | 怎样知道它改进了？ | 任务指标、回归测试、人工评估 |
| Operate | 能否可靠运行？ | Latency、Throughput、Cost、Monitoring、Rollback |

本课使用一个 Helpdesk Triage Assistant 作为贯穿案例：

```text
输入：
“I can’t log in; my account is locked.”

输出：
{
    "category": "password",
    "urgency": "high",
    "reply": "..."
}
```

这类系统并不是只输出一段自然语言，而是同时要完成：

- 分类；
- 判断紧急程度；
- 生成结构化 JSON；
- 生成对用户有帮助的回复；
- 在生产环境中稳定运行。

## 理解 Base Model 是怎样形成的

在考虑微调或部署之前，首先需要理解两件事：

(1) Base model 从什么数据中学到了什么？
(2) 训练和运行这个模型需要多少计算与内存？

一个模型的行为不仅由 Transformer 架构决定，还会被以下因素塑造：

- 训练语料；
- 数据过滤方法；
- 数据去重；
- Tokenizer；
- 训练精度；
- 参数量；
- Optimizer；
- Batch size；
- Sequence length。

# 2. Pretraining Data Pipeline

大模型预训练数据不是把整个互联网直接倒进模型。

真实的数据管道通常类似：

```text
Raw Web Data
    ↓
Text Extraction
    ↓
Language Identification
    ↓
Deduplication
    ↓
Heuristic Quality Filters
    ↓
Model-based Quality Filters
    ↓
Final Training Corpus
```

课件中的 FineWeb 示例：

| 阶段 | 剩余文档数 |
|---|---:|
| Raw Common Crawl | 200B |
| Text extraction + language ID | 36B |
| Deduplication | 20B |
| Heuristic quality filters | 17B |
| Model-based quality filter | 15B |

最终只保留了大约：

$$
\frac{15B}{200B}=7.5\%
$$

也就是起始数据中只有约 8% 被保留下来。

## 2.1 为什么数据过滤如此重要？

过滤规则决定了模型长期看到什么、不看到什么。

例如，如果过滤系统偏爱：

- 百科全书式写作；
- 标准英语；
- 正式网页；
- 长篇结构化文本；

那么模型可能会更擅长这些类型的内容，却较少学习：

- 口语；
- 小众领域术语；
- 非标准语言；
- 少数群体表达方式。

因此：

> Dataset filtering is a modeling decision.

数据管道本身就在塑造模型。

## 2.2 “过滤器本身也是模型”

FineWeb-Edu 中会训练一个 classifier 来判断：

> “这段内容是否像百科全书中会被引用的内容？”

然后只保留分数较高的数据。

因此数据生成过程中已经包含机器学习：

```text
Raw data
    ↓
A model scores data quality
    ↓
Selected data trains another model
```

这说明：

> 模型的训练分布部分由另一个模型决定。

# 3. Tokenizer 与 Byte-Pair Encoding

模型看不到“单词”，它只能看到 Token IDs。

例如：

```text
reset my andrewid password
```

如果 Tokenizer 不认识 `andrewid`，它可能被切成：

```text
and
rew
id
```

甚至：

```text
a n d r e w i d
```

如果 Tokenizer 见过大量 Helpdesk 数据，它可能直接拥有：

```text
andrewid
```

这个完整 Token。

## 3.1 BPE 的核心思想

Byte-Pair Encoding 从最小单位开始，例如字符：

```text
p a s s w o r d
```

然后反复查找最常出现的相邻 Token Pair，并将其合并。

例如：

```text
s + s → ss
```

之后可能继续：

```text
pa + ss → pass
```

最终形成词或子词单位。

## 3.2 Tokenizer 为什么重要？

### Tokens are money

API 和推理成本通常按输入和输出 Token 计费。

如果一个专业词被拆成 6 个 Token，而不是 1 个 Token：

```text
Token Cost ≈ 6 倍
```

### Tokens are context

Context window 是按 Token 数计算的。

如果专业词汇总被切碎，能放进 Context 的有效信息会减少。

### Output tokens add latency

Autoregressive decoding 一次只产生一个 Token。

输出越长，Decode step 越多。

### Tokens affect quality

一个完整概念如果有独立 Token，模型更容易形成稳定表示。

如果它总被拆成杂乱碎片，模型学习这个概念会更困难。

## 3.3 实际工程建议

选择 Base Model 前，应测试它如何 Tokenize 自己领域中的关键词，例如：

```text
AndrewID
Microsoft Entra
Conditional Access
service principal
```

不要只看 Benchmark，也要看：

```python
tokenizer.encode(domain_term)
```

到底产生了多少个 Token。

# 4. Full Fine-Tuning 的训练内存估计

对于使用 mixed precision 和 AdamW 的一种常见训练配置，模型状态内存可以粗略估计为：

$$
M_{\text{train}}
\approx
2P+4P+2P+8P
=
16P
$$

其中：

- $P$ 是模型参数数量；
- 每个系数表示每个参数需要多少 Bytes。

具体来源：

| 内容 | Bytes / Parameter |
|---|---:|
| bf16 model weights | 2 |
| fp32 master weights | 4 |
| gradients | 2 |
| Adam first and second moments | 8 |
| 总计 | 16 |

因此：

$$
M_{\text{train}}\approx16P
$$

## 4.1 8B 模型示例

假设模型有：

$$
P=8.03\times10^9
$$

则模型状态内存大约是：

$$
16\times8.03\times10^9
=
128.48\times10^9\text{ bytes}
$$

按课件使用的换算方式，约为：

$$
119.7\text{ GB}
$$

其中：

| 部分 | 内存 |
|---|---:|
| bf16 weights | 15.0 GB |
| fp32 master copy | 29.9 GB |
| gradients | 15.0 GB |
| Adam moments | 59.8 GB |
| 总计 | 119.7 GB |

## 4.2 这还不是 Peak Memory

上面的 $16P$ 只计算了 Model State。

实际 Peak Memory 还包括：

```text
Model state
+ Activations
+ Temporary buffers
+ Framework overhead
```

### Activations

Forward pass 生成的中间值。

Backpropagation 为了计算梯度，需要保存许多中间 Activation。

Activation memory 会随以下因素增加：

- Batch size；
- Sequence length；
- Number of layers；
- Hidden dimension；
- Attention implementation。

所以：

> 8B 模型训练需要约 120GB，不代表 120GB GPU 就一定能训练。

因为 Peak VRAM 可能明显更高。

# 5. 降低训练显存的四种方法

## 5.1 Lower Precision

使用更少的 Bit 存储或计算 Tensor，例如：

```text
FP32 → BF16 → FP16 → INT8 → INT4
```

优点：

- 每个值占用更少内存；
- 可能提高吞吐量。

代价：

- 数值稳定性风险；
- 可能产生精度损失；
- 部分计算仍需高精度。

---

## 5.2 Activation Checkpointing

普通 Backpropagation：

```text
Forward
    ↓
保存大部分 Activations
    ↓
Backward 直接使用
```

Checkpointing：

```text
Forward
    ↓
只保存部分 Activations
    ↓
Backward 时重新计算缺失部分
```

它交换的是：

```text
Less memory
↔
More computation
```

---

## 5.3 Shard Model State

使用：

- ZeRO；
- FSDP；

将以下内容分到多块 GPU：

- optimizer states；
- gradients；
- parameters。

这样每块 GPU 只保存一部分模型状态。

代价是：

```text
Less memory per GPU
↔
More communication
```

---

## 5.4 Train Fewer Parameters

冻结 Base Model，只训练很小的 Adapter，例如 LoRA。

此时不需要为所有 Base Parameters 保存：

- 梯度；
- Optimizer state。

通常这是最显著、最实用的内存降低方式之一。

# 6. Adaptation Ladder

适配模型时，不应该一开始就选择最贵的方法。

正确策略是：

> Choose the least complex intervention that meets the requirement.

可以将适配方式从简单到复杂排列为：

```text
Prompting
    ↓
Few-shot Prompting
    ↓
Retrieval-Augmented Generation
    ↓
SFT / LoRA
    ↓
Preference Tuning
    ↓
Continued Pretraining
```

## 6.1 Prompting

特点：

- 不训练；
- 不更新权重；
- 成本最低；
- 上线最快。

适合：

- 模型已经具备能力；
- 只是需要更清楚的指令；
- Output format 可以通过 Prompt 控制。

---

## 6.2 Few-shot Prompting

在 Prompt 中放入几个示例：

```text
Input → Desired Output
Input → Desired Output
New Input →
```

优点：

- 不训练；
- 可以临时规定格式和行为。

代价：

- Prompt 更长；
- 每次请求都重复付出 Token 成本；
- 示例数量受 Context 限制。

---

## 6.3 Retrieval-Augmented Generation

适合模型缺少：

- 内部政策；
- 最新价格；
- 客户记录；
- 实时事实；
- 需要出处的知识。

核心：

```text
Query
    ↓
Retrieve relevant evidence
    ↓
Put evidence in prompt
    ↓
Generate grounded answer
```

---

## 6.4 SFT / LoRA

当模型知道事实，但行为不稳定时使用。

适合改变：

- 输出格式；
- 语气；
- Domain language；
- 决策风格；
- 稳定的任务模式。

---

## 6.5 Preference Tuning

当需要模型更符合偏好，而不是简单模仿一个标准答案时使用。

例如：

- 哪个回答更有帮助；
- 哪个回答更安全；
- 哪个回答更简洁；
- 哪个回答更符合公司语气。

---

## 6.6 Continued Pretraining

当 Base Model 几乎不理解整个领域时才考虑。

例如：

- 特殊科学语言；
- 新语言；
- 高度专业的代码；
- 与原始预训练数据差异极大的文档。

代价最大，需要大量未标注 Domain Corpus 和 Compute。

## RAG 就是：模型先去“翻书找答案”，再根据找到的资料回答你，而不是只靠自己脑子里的记忆。

RAG = 搜索（Retrieve）+ 阅读（Read）+ 回答（Generate）。模型不会凭空猜，而是先去知识库找资料，再基于资料生成答案。

所以你以后可以把 RAG 理解成：

"给 AI 接了一个会自动查资料的大脑外挂。"

模型本身没有变聪明，只是回答之前多了一步“查资料”。这也是为什么课程里强调：

Fine-tuning 解决的是 "How"（怎么回答、什么风格、什么格式）;
RAG 解决的是 "What"（回答需要哪些最新知识）
