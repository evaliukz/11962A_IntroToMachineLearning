# 论文 Attention Is All You Need by Ashish Vaswani

chrome-extension://efaidnbmnnnibpcajpcglclefindmkaj/https://proceedings.neurips.cc/paper_files/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf

Transformer 的诞生，不是为了替代 CNN，而是为了克服 RNN 在自然语言处理中的两个核心问题：（1）无法并行计算和（2）难以捕获长距离依赖。Transformer 用 Self-Attention 让每个词都能直接关注句子中的任意其他词，从而实现并行训练和更强的上下文建模能力。

# Transformer 最核心、也是整篇论文最重要的部分：Self-Attention

> **一句话理解 Self-Attention：**
> 对于一句话中的每个单词，它都会查看整句话，并根据与其他单词的相关程度，为不同单词分配不同的关注权重，然后综合这些信息生成一个新的、更有上下文含义的表示（Embedding）。

---

# 1. 为什么需要 Self-Attention？

## 回顾 CNN

CNN 工作流程：

```
Image
   │
   ▼
Convolution (Kernel)
   │
   ▼
Feature Map
   │
   ▼
Classification
```

CNN 的特点：

- 看局部（Local Receptive Field）
- 擅长图片
- Kernel 每次只能观察附近区域

例如：

```
Kernel = 3

The  cat  sat
```

CNN 无法直接看到一句话中距离很远的两个词。

---

## 回顾 RNN

RNN 工作方式：

```
The
 │
 ▼
animal
 │
 ▼
didn't
 │
 ▼
cross
 │
 ▼
...
 │
 ▼
it
```

特点：

- 一个词一个词顺序读取
- 前面的信息必须一直传递到后面

优点：

能够利用上下文。

缺点：

### ① 无法并行（Sequential）

必须：

```
Word1

↓

Word2

↓

Word3

↓

...

↓

Word100
```

GPU 无法同时计算所有词。

训练速度慢。

---

### ② Long-range Dependency（长距离依赖）

例如：

```
The animal didn't cross the street because it was too tired.
```

这里：

```
it
```

到底是谁？

```
animal

还是

street
```

RNN：

经过几十步以后，

最前面的信息越来越弱。

因此：

长句子效果不好。

---

# 2. Transformer 的核心思想

Transformer 认为：

> 为什么一定要一步一步传递信息？

为什么不能：

每个词

直接

查看

整句话？

于是：

Transformer 变成：

```
The

────────► animal

────────► street

────────► tired

────────► because

────────► it
```

每个词都可以直接访问其它所有词。

---

# 3. 什么是 Self-Attention？

举例：

```
Tom loves his dog because it is friendly.
```

当模型看到：

```
it
```

它不会只看：

```
it
```

而是查看整句话：

```
Tom

loves

his

dog

because

it

friendly
```

然后决定：

哪些词最重要。

例如：

| Word | Attention Weight |
|------|-----------------:|
| Tom | 0.05 |
| loves | 0.08 |
| his | 0.10 |
| dog | **0.60** |
| because | 0.05 |
| it | 0.02 |
| friendly | 0.10 |

说明：

```
it
```

最关注：

```
dog
```

因为：

它们最相关。

---

# 4. 每一个词都会执行 Attention

注意：

不是只有：

```
it
```

所有单词都会这样做。

例如：

```
Tom
```

可能关注：

```
loves

dog
```

而：

```
friendly
```

可能关注：

```
dog
```

所以：

Transformer 实际上是在问：

> **每个词都在问："整句话里，谁和我最相关？"**

---

# 5. 为什么叫 Self-Attention？

Self 的意思：

Attention 发生在：

**同一句话内部。**

例如：

```
The cat sat on the mat.
```

所有词：

都只关注：

这一句话里的其它词。

因此叫：

```
Self Attention
```

以后还会学习：

```
Cross Attention
```

表示：

一个序列关注另一个序列。

例如：

Decoder 看 Encoder。

---

# 6. Self-Attention 可以理解成什么？

## 会议室比喻

假设：

会议室有：

```
Alice

Bob

Charlie

David

Eva
```

轮到 Alice 发言。

她不会：

只讲自己的事情。

而是：

先观察大家。

例如：

```
Bob

负责数据库

Charlie

负责前端

David

负责 API
```

于是：

Alice 决定：

今天：

重点听：

David

一点听：

Bob

几乎不听：

Charlie

这就是：

Attention。

---

所有人都会这样做。

于是：

会议变成：

```
Alice ←→ Bob

Alice ←→ David

Charlie ←→ Eva

Bob ←→ Charlie

...
```

所有人：

互相交流。

而不是：

排队讲话。

---

# 7. Self-Attention 与 CNN、RNN 的区别

| 模型 | 信息传播方式 | 能否直接访问远距离信息 |
|------|-------------|----------------------|
| CNN | Kernel 滑动 | ❌ 否 |
| RNN | 顺序传递 | ❌ 很困难 |
| Transformer | Self-Attention | ✅ 可以直接访问任意词 |

例如：

CNN：

```
The cat sat
```

只能看到：

附近三个词。

RNN：

```
The

↓

cat

↓

sat

↓

on

↓

the

↓

mat
```

必须一步一步传播。

Transformer：

```
The ←────────────→ mat

cat ←────────────→ sat

on  ←────────────→ cat

...
```

任何两个词：

都可以直接通信。

---

# 8. Transformer 如何知道该关注谁？

这是论文最经典的设计。

Transformer 为每个词生成三种向量：

```
Query (Q)

Key (K)

Value (V)
```

然后：

```
Query

↓

和所有 Key 比较

↓

得到相关性（Attention Score）

↓

Softmax

↓

得到 Attention Weight

↓

对所有 Value 加权求和

↓

新的 Embedding
```

于是：

每个词都拥有：

新的、

融合上下文的信息。

---

# 9. 整个 Self-Attention 的流程

```
一句话

↓

Embedding

↓

生成 Q、K、V

↓

每个词拿自己的 Q

去和所有 K 比较

↓

得到 Attention Score

↓

Softmax

↓

得到 Attention Weight

↓

对所有 Value 加权平均

↓

得到新的 Embedding
```

这就是：

Transformer 最核心的计算过程。

---

# 10. 为什么这是革命性的？

CNN：

```
Kernel

↓

Feature

↓

Feature

↓

Classification
```

Transformer：

完全没有：

- Kernel
- Pooling
- Sliding Window

全部替换成：

```
Attention

↓

Attention

↓

Attention
```

因此：

论文标题：

```
Attention Is All You Need
```

意思就是：

> **只需要 Attention，就可以完成以前 RNN 的工作。**

---

# 本节重点（必须掌握）

## 为什么提出 Transformer？

解决 RNN 两个核心问题：

- 无法并行计算（Sequential）
- 长距离依赖（Long-range Dependency）

---

## Self-Attention 是什么？

> 每个单词都会查看整句话，
> 根据其它单词的重要程度，
> 分配不同的注意力权重，
> 然后综合这些信息，
> 生成新的、更有上下文意义的表示。

---

## 与 CNN、RNN 的区别

CNN：

```
只能看附近
```

RNN：

```
必须顺序传播
```

Transformer：

```
任何两个词都可以直接通信
```

---

# 下一节预习

论文最经典的一步：

```
Q（Query）

K（Key）

V（Value）
```

下一节将回答：

> **为什么作者要设计 Q、K、V 三个向量？**
>
> **Attention(Q, K, V) 的公式到底是怎么一步一步推导出来的？**
