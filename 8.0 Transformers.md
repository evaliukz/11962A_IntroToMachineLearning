# 论文 Attention Is All You Need by Ashish Vaswani

Transformer 的诞生，不是为了替代 CNN，而是为了克服 RNN 在自然语言处理中的两个核心问题：（1）无法并行计算和（2）难以捕获长距离依赖。Transformer 用 Self-Attention 让每个词都能直接关注句子中的任意其他词，从而实现并行训练和更强的上下文建模能力。

# Transformer 最核心、也是整篇论文最重要的部分：Self-Attention
