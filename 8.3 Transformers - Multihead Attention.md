# Transformer 学习笔记（五）
# Multi-Head Attention（多头注意力）

> **一句话总结：**
>
> Multi-Head Attention 并不是重复计算多次 Attention，而是让多个独立的 Attention Head 从不同角度理解同一句话。每个 Head 都拥有自己独立学习的 Query、Key 和 Value，因此能够学习不同类型的语言关系。最后，将所有 Head 的结果拼接（Concat）并经过一次线性变换，得到最终的上下文表示。

---

# 1. 为什么一个 Attention 不够？

上一节学习的 Self-Attention 中，每个单词都会计算自己与其它所有单词之间的相关性，并得到一组 Attention Weight。例如：

```
Tom loves his dog because it is friendly.
```

当模型处理：

```
it
```

可能得到：

| Word | Attention Weight |
|------|-----------------:|
| Tom | 0.05 |
| loves | 0.08 |
| his | 0.10 |
| dog | **0.60** |
| because | 0.05 |
| friendly | 0.12 |

说明模型认为：

```
it

↓

dog
```

具有最强的关联。

然而，一句话中往往同时存在很多不同类型的关系。例如：

```
Tom

↓

loves
```

表示主谓关系（Subject–Verb）。

```
his

↓

dog
```

表示所属关系（Possessive）。

```
because

↓

friendly
```

表示因果关系（Causal）。

Self-Attention 往往只能重点学习一种最重要的关系，而其它关系容易被忽略。因此，仅使用一个 Attention Head，模型的表达能力仍然有限。

---

# 2. 一个 Attention Head 可以理解成一个专家

理解 Multi-Head 最好的方法，就是把每一个 Head 看成一位不同领域的专家。

假设四位老师一起分析一句话：

```
Tom loves his dog because it is friendly.
```

不同老师负责不同任务：

- 第一位老师研究语法结构（Grammar）。
- 第二位老师研究主语和宾语关系（Subject–Object）。
- 第三位老师研究代词指代（Coreference）。
- 第四位老师研究情感和语义关系（Semantic/Sentiment）。

最后，四位老师分别给出自己的分析结果，再将这些结果综合起来。

Transformer 中，每一个 Attention Head 就像这样一位专家，它们分别学习不同类型的语言规律，因此整体理解能力远强于只有一个 Attention。

---

# 3. 每一个 Head 都拥有独立的 Q、K、V

Multi-Head Attention 最重要的一点是：

**每一个 Head 都拥有自己独立学习的 Query、Key 和 Value。**

例如：

```
Head 1

↓

Q₁

K₁

V₁
```

```
Head 2

↓

Q₂

K₂

V₂
```

```
Head 3

↓

Q₃

K₃

V₃
```

……

这些 Q、K、V 并不是共享的，而是分别通过不同的权重矩阵学习得到：

```
Head1：

WQ₁

WK₁

WV₁

Head2：

WQ₂

WK₂

WV₂

...
```

因此，每一个 Head 都可以学习完全不同的 Attention Pattern。

---

# 4. 不同 Head 学习不同的信息

假设一句话：

```
The boy who is wearing a red hat is running.
```

不同 Head 可能学习到完全不同的关系。

Head 1：

学习主谓关系：

```
boy

↓

running
```

Head 2：

学习颜色与物体关系：

```
red

↓

hat
```

Head 3：

学习修饰关系：

```
wearing

↓

hat
```

Head 4：

学习从句关系：

```
who

↓

boy
```

由此可以看出，一句话中包含大量不同层次的信息，一个 Head 很难同时学习所有关系，因此需要多个 Head 分工合作。

---

# 5. Multi-Head Attention 如何计算？

假设模型的 Embedding Dimension 为：

```
512
```

如果只有一个 Attention：

```
512

↓

Attention

↓

512
```

论文采用：

```
8 Heads
```

于是：

```
512

↓

Split

↓

64

64

64

64

64

64

64

64
```

注意：

这里切分的是 **Embedding Dimension**，而不是 Token 数量。

每一个 Head 分别处理 64 维表示，并独立完成一次完整的 Self-Attention。

例如：

```
Embedding

↓

Head1

↓

Attention

↓

64
```

同时：

```
Embedding

↓

Head2

↓

Attention

↓

64
```

一直计算到：

```
Head8
```

---

# 6. 为什么最后还要 Concat？

每个 Head 输出：

```
64 Dimensions
```

八个 Head：

```
64

+

64

+

64

+

...

=

512
```

论文将这一过程称为：

```
Concat
```

随后，再经过一个 Linear Layer：

```
Concat

↓

Linear

↓

Output Embedding
```

因此：

Multi-Head Attention 的输入和输出维度保持一致。

例如：

```
Input：

512

↓

Output：

512
```

维度不会发生变化。

---

# 7. 为什么 Multi-Head 比 Single Head 更强？

如果只有一个 Attention：

模型一次只能学习一种关系。

Multi-Head 可以同时学习：

- Grammar（语法关系）
- Subject–Verb（主谓关系）
- Object（宾语关系）
- Coreference（代词指代）
- Long-range Dependency（长距离依赖）
- Semantic Relation（语义关系）
- Modifier（修饰关系）
- Sentiment（情感关系）

论文后续对 Attention 进行可视化时发现：

有些 Head 专门学习：

```
代词指代
```

有些 Head 专门学习：

```
语法结构
```

有些 Head 专门学习：

```
长距离依赖
```

说明不同 Head 确实自动学会了不同类型的知识，而不是重复计算相同内容。

---

# 8. 为什么 GPT、BERT 和 Llama 都保留 Multi-Head？

随着模型越来越大，语言中需要学习的关系越来越复杂，因此 Head 数量也越来越多。

例如：

```
GPT-2：

12 Heads

GPT-3：

96 Heads

Llama：

更多 Heads
```

增加 Head 数量，可以让模型同时学习更多种语言模式，从而提高模型的表达能力。

需要注意的是：

Head 越多，并不意味着一定越好，因为每个 Head 的维度会相应变小，实际模型需要在计算成本和表达能力之间进行平衡。

---

# 9. Multi-Head Attention 的完整流程

```
Input Embedding

        │

        ▼

Linear Projection

        │

        ▼

Split into Multiple Heads

        │

        ├──────── Head1

        │

        ├──────── Head2

        │

        ├──────── Head3

        │

        ├──────── ...

        │

        ▼

Each Head Computes Self-Attention

        │

        ▼

Concat All Heads

        │

        ▼

Linear Layer

        │

        ▼

Output Embedding
```

---

# 本节重点（必须掌握）

## 为什么需要 Multi-Head？

一个 Attention Head 很难同时学习所有语言关系，因此需要多个 Head 分工合作，从不同角度理解同一句话。

---

## 每一个 Head 都有什么？

每一个 Head 都拥有自己独立学习的：

- Query（Q）
- Key（K）
- Value（V）

以及独立的参数矩阵：

- WQ
- WK
- WV

因此不同 Head 可以学习完全不同的 Attention Pattern。

---

## Multi-Head 的计算流程

```
Input Embedding

↓

Split

↓

Multiple Independent Self-Attention Heads

↓

Concat

↓

Linear

↓

Output Embedding
```

---

## Multi-Head 的核心作用

Multi-Head Attention 可以同时学习：

- 语法关系
- 主谓关系
- 代词指代
- 长距离依赖
- 修饰关系
- 语义关系
- 情感关系

因此，相比 Single Head，模型能够获得更加丰富、更加全面的上下文表示。

---

# 一句话总结

> **Multi-Head Attention 的本质是让多个独立的 Attention Head 从不同角度学习同一句话。每个 Head 都拥有自己独立学习的 Q、K、V 参数，因此能够分别学习不同类型的语言关系。最终，通过 Concat 和 Linear Layer 将多个 Head 的结果融合，形成更加丰富的上下文表示。这也是 Transformer 表达能力远强于传统 RNN 的重要原因之一。**
