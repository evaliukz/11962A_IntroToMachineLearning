# Transformer 学习笔记（六）
# Transformer Layer：Attention、FFN、Residual、LayerNorm

> **一句话总结：**
>
> 一个 Transformer Layer 可以理解成一个完整的信息加工单元。Multi-Head Attention 负责让不同 Token 之间交换信息；Feed Forward Network（FFN）负责每个 Token 独立加工这些信息；Residual Connection（残差连接）保留原始信息，避免深层网络退化；Layer Normalization（LayerNorm）保持数值稳定，使 Transformer 能够堆叠几十层甚至上百层。

---

# 1. Transformer Layer 的整体结构

论文 Figure 1 中，一个标准的 **Encoder Layer** 如下：

```
Input Embedding
        │
        ▼
Multi-Head Attention
        │
        ▼
Add (Residual)
        │
        ▼
LayerNorm
        │
        ▼
Feed Forward Network (FFN)
        │
        ▼
Add (Residual)
        │
        ▼
LayerNorm
        │
        ▼
Output Embedding
```

Transformer 并不是只有一层，而是不断重复这个 Layer。

例如：

- GPT-2 Small：12 Layers
- BERT Base：12 Layers
- GPT-3：96 Layers
- Llama：32～80 Layers

因此：

> **Transformer 本质上就是不断重复同一个 Layer。**

---

# 2. Multi-Head Attention 的作用

Attention 的职责是：

> **让不同 Token 之间互相交换信息。**

例如：

```
Tom loves his dog.
```

经过 Self-Attention 后：

```
Tom
```

已经不仅仅包含：

```
Tom
```

自己的信息，而是融合了：

```
loves

his

dog
```

的信息。

因此：

Attention 的作用可以理解成：

> **Token 与 Token 之间的信息交流。**

---

# 3. 为什么 Attention 后面还需要 FFN？

Attention 只是：

```
收集信息
```

并没有真正加工这些信息。

可以把整个过程理解成：

```
开会

↓

大家交流信息

↓

每个人回到办公室

↓

整理资料
```

Attention 就像：

```
开会
```

大家互相交流。

FFN 就像：

```
每个人回办公室

独立思考
```

负责进一步加工 Attention 收集到的信息。

因此：

Transformer 将：

```
Token 交流
```

和：

```
Token 自己加工
```

分成两个独立步骤。

---

# 4. Feed Forward Network（FFN）

FFN 本质上就是一个普通的多层感知机（MLP）。

典型结构：

```
Linear

↓

GELU（或 ReLU）

↓

Linear
```

这里没有任何新的结构。

就是：

```
全连接层

+

激活函数

+

全连接层
```

和我们之前学习的 MLP 完全一样。

---

# 5. FFN 与 Attention 的区别

Attention：

```
Tom

↓

dog
```

不同 Token 会互相交流。

FFN：

```
Tom

↓

Linear

↓

GELU

↓

Linear
```

Tom 只处理自己的数据。

与此同时：

```
dog

↓

Linear

↓

GELU

↓

Linear
```

dog 也只处理自己的数据。

因此：

**FFN 不会让 Token 再次互相交流。**

可以理解为：

> **FFN 是每个 Token 自己的 MLP。**

---

# 6. Residual Connection（残差连接）

Attention 得到新的 Embedding 后，

论文并没有直接继续计算。

而是：

```
Output

+

Input
```

例如：

```
Original Tom

+

Attention(Tom)
```

这一步称为：

```
Residual Connection
```

作用是：

> **保留原始信息，避免网络越来越深时丢失重要特征。**

---

# 7. 为什么要使用 Residual？

如果没有 Residual：

```
Input

↓

Attention

↓

Output
```

每一层都会不断修改 Embedding。

当网络很深时：

原始信息可能越来越少。

Residual：

则变成：

```
Input

↓

Attention

↓

Output

+

Input
```

这样：

模型始终保留：

```
原来的信息

+

新的信息
```

训练更加稳定。

---

# 8. Residual 与 ResNet 的关系

Residual Connection 并不是 Transformer 发明的。

它来自：

```
ResNet（2015）
```

CNN 中：

```
Input

↓

Convolution

↓

+

Input
```

Transformer 直接借鉴了 ResNet 的思想：

```
Input

↓

Attention

↓

+

Input
```

因此：

Transformer 实际上融合了 CNN 中的重要训练技巧。

---

# 9. Layer Normalization（LayerNorm）

Residual 后，

Embedding 的数值可能越来越大。

例如：

```
100

300

500
```

经过很多层以后：

可能变成：

```
1000

5000

9000
```

这样：

模型训练会越来越困难。

于是：

Transformer 在每一层都加入：

```
LayerNorm
```

将数据重新调整到稳定范围。

通常：

```
Mean ≈ 0

Variance ≈ 1
```

因此：

LayerNorm 的作用是：

> **保持数值稳定，加快训练，提高深层网络的收敛能力。**

---

# 10. 为什么不用 BatchNorm？

CNN 中：

最常见的是：

```
BatchNorm
```

因为：

图片大小通常一致。

Transformer 不同。

一句话可能：

```
10 Tokens

100 Tokens

1000 Tokens
```

长度差异很大。

因此：

BatchNorm 并不适合 Transformer。

论文最终采用：

```
LayerNorm
```

直到今天：

GPT

BERT

Llama

Claude

几乎所有大语言模型仍然使用 LayerNorm（或其改进版本 RMSNorm）。

---

# 11. 一个完整的 Transformer Layer

综合前面的所有模块：

```
Input Embedding

        │

        ▼

Multi-Head Attention
（Token 之间交换信息）

        │

        ▼

Residual Add
（保留原始信息）

        │

        ▼

LayerNorm
（稳定数值）

        │

        ▼

Feed Forward Network
（每个 Token 独立加工信息）

        │

        ▼

Residual Add
（再次保留原始信息）

        │

        ▼

LayerNorm
（再次稳定训练）

        │

        ▼

Output Embedding
```

然后：

输出进入：

```
下一层 Transformer Layer
```

整个过程不断重复。

---

# 12. 为什么 Transformer 可以堆叠很多层？

第一层：

可能学习：

```
词（Word）
```

第二层：

学习：

```
短语（Phrase）
```

第三层：

学习：

```
句法（Syntax）
```

第四层：

学习：

```
语义（Semantic）
```

更高层：

学习：

```
推理

逻辑

上下文关系
```

因此：

Transformer 并不是依靠某一层完成理解，

而是通过几十层不断提取越来越抽象的特征。

---

# 本节重点（必须掌握）

## 一个 Transformer Layer 包含四个核心模块

1. Multi-Head Attention
2. Residual Connection
3. Layer Normalization
4. Feed Forward Network（FFN）

---

## 每个模块负责什么？

### Multi-Head Attention

负责：

> **不同 Token 之间的信息交流。**

---

### Feed Forward Network（FFN）

负责：

> **每个 Token 独立加工 Attention 收集到的信息。**

---

### Residual Connection

负责：

> **保留原始信息，避免深层网络退化，使训练更加稳定。**

---

### LayerNorm

负责：

> **稳定数值范围，提高训练效率，使 Transformer 能够堆叠几十甚至上百层。**

---

# 一句话总结

> **Transformer Layer 可以理解成一个完整的信息加工工厂：Multi-Head Attention 负责让 Token 互相交流，Feed Forward Network 负责每个 Token 独立思考，Residual Connection 保留原始信息，LayerNorm 保持数值稳定。整个 Transformer 就是将这样的 Layer 不断重复堆叠，从低层特征逐步学习到高层语义表示。**
