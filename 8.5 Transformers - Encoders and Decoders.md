# Transformer 学习笔记（七）
# Encoder 与 Decoder：Transformer 三种架构的来源

> **一句话总结：**
>
> Transformer 最初由 **Encoder + Decoder** 两部分组成。Encoder 负责理解输入（Understand），Decoder 负责生成输出（Generate）。现代大语言模型都是在此基础上的三种变体：BERT 使用 Encoder Only，GPT/Llama 使用 Decoder Only，T5/BART 使用 Encoder + Decoder。

---

# 1. Transformer 的整体架构

Transformer 原始论文（2017）提出的模型包含两部分：

```
                Transformer

         ┌───────────────┐
Input --->│   Encoder     │
         └───────────────┘
                  │
                  ▼
        Encoder Output
                  │
                  ▼
         ┌───────────────┐
Target -->│   Decoder     │
         └───────────────┘
                  │
                  ▼
             Next Token
```

整个模型可以理解为两个阶段：

```
理解（Understand）

↓

生成（Generate）
```

Encoder 负责理解输入内容，Decoder 根据理解后的信息逐步生成输出。

---

# 2. Encoder 的作用

Encoder 的职责只有一个：

> **理解输入文本，并生成高质量的语义表示（Semantic Representation）。**

例如：

输入一句话：

```
The cat is sleeping.
```

经过多层 Encoder 后：

模型不会直接输出文字，而是得到：

```
Context Representation
```

例如：

```
cat

↓

动物

↓

主语

↓

正在睡觉
```

Encoder 会把整句话转换成能够表达语义的信息表示。

因此：

Encoder 更像一位：

> **阅读理解专家（Reader）**

它只负责：

```
理解输入
```

而不会负责生成文本。

---

# 3. Decoder 的作用

Decoder 的职责与 Encoder 相反。

它负责：

> **根据已经理解的信息，一个 Token 一个 Token 地生成输出。**

例如：

翻译：

```
The cat is sleeping.
```

Decoder 并不是一次输出：

```
猫正在睡觉。
```

而是逐步生成：

```
猫

↓

正在

↓

睡觉

↓

。
```

每一步都预测：

```
Next Token
```

因此：

Decoder 更像一位：

> **写作者（Writer）**

负责连续生成文本。

---

# 4. 为什么 Decoder 需要 Mask？

假设模型正在生成：

```
I love dogs
```

当预测：

```
love
```

时，

如果模型能够提前看到：

```
dogs
```

那么预测就失去了意义。

因此：

Decoder 的 Self-Attention 增加了一种特殊机制：

```
Masked Self-Attention
```

又称：

- Causal Mask
- Look-Ahead Mask

作用是：

> **只能看到当前 Token 左边已经生成的内容，不能看到未来 Token。**

例如：

```
      I   love  dogs

I     ✓    ✗     ✗

love  ✓    ✓     ✗

dogs  ✓    ✓     ✓
```

因此：

Decoder 始终按照：

```
过去

↓

现在

↓

未来
```

的顺序生成文本。

---

# 5. 为什么 Encoder 不需要 Mask？

Encoder 的目标不是生成，

而是理解。

例如：

```
Tom loves his dog.
```

理解：

```
Tom
```

时，

完全可以同时参考：

```
dog
```

因此：

Encoder 的 Self-Attention 可以看到整句话。

这种 Attention 被称为：

```
Bidirectional Attention
```

即：

所有 Token 都可以互相访问。

例如：

```
Tom

↓

dog
```

```
dog

↓

Tom
```

互相都可以关注。

因此：

Encoder 能够充分利用上下文理解整句话。

---

# 6. Encoder 与 Decoder 最大的区别

二者最大的区别并不是 Layer 数量，

也不是网络结构。

真正区别在于：

**Attention 是否使用 Mask。**

### Encoder

```
可以看到所有 Token
```

采用：

```
Bidirectional Attention
```

适合理解任务。

---

### Decoder

```
只能看到过去 Token
```

采用：

```
Masked Self-Attention
```

适合文本生成任务。

---

# 7. 什么是 Cross Attention？

论文中的 Decoder 实际上包含三个模块：

```
Masked Self-Attention

↓

Cross Attention

↓

Feed Forward Network
```

为什么还需要：

```
Cross Attention
```

原因是：

Decoder 在生成文本时，

不仅需要参考自己已经生成的内容，

还需要参考 Encoder 对输入的理解。

例如：

翻译：

```
The cat is sleeping.
```

Encoder 已经理解：

```
英文语义
```

Decoder 在生成：

```
猫

↓

正在

↓

睡觉
```

时，

会不断查看 Encoder 输出的信息。

因此：

Cross Attention 可以理解为：

> **Decoder 与 Encoder 之间的信息交流。**

---

# 8. 为什么 GPT 没有 Encoder？

GPT 的目标只有一个：

```
预测下一个 Token
```

它并不需要先完整理解输入，

再生成输出。

因此：

GPT 删除了：

```
Encoder
```

同时：

也删除了：

```
Cross Attention
```

最终只保留：

```
Masked Self-Attention

↓

Feed Forward Network
```

不断预测：

```
Next Token
```

因此：

GPT 属于：

```
Decoder Only
```

架构。

---

# 9. 为什么 BERT 没有 Decoder？

BERT 的目标不是生成文本，

而是理解文本。

例如：

- 文本分类
- 情感分析
- 检索
- 问答
- Sentence Embedding

这些任务都只需要：

```
理解
```

而不需要：

```
生成
```

因此：

BERT 保留：

```
Encoder
```

删除：

```
Decoder
```

因此属于：

```
Encoder Only
```

架构。

---

# 10. 为什么 T5 保留两者？

T5、BART 等模型主要完成：

- 翻译
- 摘要
- 文本改写
- 问答生成

这些任务同时需要：

```
理解输入

+

生成输出
```

因此：

它们完整保留：

```
Encoder

↓

Decoder
```

属于：

```
Encoder-Decoder
```

架构。

---

# 11. Transformer 三种经典架构

现代几乎所有大语言模型都属于下面三类。

| 架构 | 代表模型 | 主要作用 |
|------|---------|---------|
| Encoder Only | BERT、RoBERTa | 理解文本（Understanding） |
| Decoder Only | GPT、Llama、DeepSeek、Qwen | 文本生成（Generation） |
| Encoder + Decoder | T5、BART | 理解 + 生成（Seq2Seq） |

---

# 12. 为什么 ChatGPT 能聊天？

ChatGPT 本质上就是：

```
Decoder Only Transformer
```

它每一步都执行：

```
预测下一个 Token
```

例如：

```
Hello,

↓

how

↓

are

↓

you

↓

?
```

不断循环：

```
输入已有 Token

↓

预测下一个 Token

↓

加入输入

↓

继续预测
```

因此：

聊天本质上就是：

> **不断重复 Next Token Prediction。**

---

# 13. Transformer 家族的发展

Transformer 原始论文提出：

```
Transformer

        │
 ┌──────┴──────┐
 │             │
Encoder      Decoder
```

后来逐渐演化出三种方向：

```
Transformer

                │
      ┌─────────┴──────────┐
      │                    │

Encoder Only         Decoder Only

(BERT)              (GPT / Llama)

      │                    │

理解文本             生成文本

      └─────────┬──────────┘
                │

       Encoder + Decoder

          (T5 / BART)

      理解 + 生成
```

因此：

今天几乎所有 NLP 模型，

都可以看作 Transformer 的不同变体。

---

# 本节重点（必须掌握）

## Encoder

作用：

> **理解输入文本。**

特点：

- Bidirectional Attention
- 可以看到整句话
- 不生成文本

代表模型：

- BERT
- RoBERTa

---

## Decoder

作用：

> **逐步生成输出文本。**

特点：

- Masked Self-Attention
- 只能看到过去 Token
- 每次预测一个 Token

代表模型：

- GPT
- Llama
- DeepSeek
- Qwen

---

## Cross Attention

作用：

> **让 Decoder 在生成文本时参考 Encoder 的输出。**

主要用于：

- 翻译
- 摘要
- Seq2Seq 任务

---

## 三种 Transformer 架构

| 架构 | 是否理解输入 | 是否生成文本 |
|------|-------------|-------------|
| Encoder Only | ✅ | ❌ |
| Decoder Only | 部分（依赖上下文） | ✅ |
| Encoder + Decoder | ✅ | ✅ |

---

# 一句话总结

> **Transformer 最初由 Encoder 和 Decoder 两部分组成。Encoder 负责理解输入，因此可以看到整句话（Bidirectional Attention）；Decoder 负责生成输出，因此只能看到已经生成的内容（Masked Self-Attention）。现代大模型都是 Transformer 的不同演化方向：BERT 属于 Encoder Only，GPT/Llama 属于 Decoder Only，T5/BART 保留完整的 Encoder-Decoder 架构，用于同时完成理解和生成任务。**
