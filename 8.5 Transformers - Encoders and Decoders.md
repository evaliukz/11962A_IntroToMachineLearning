# Transformer 学习笔记（七）
# Encoder 与 Decoder：Transformer 三种架构的来源

> **一句话总结：**
>
> Transformer 最初由 **Encoder + Decoder** 两部分组成。Encoder 负责理解输入（Understand），Decoder 负责生成输出（Generate）。现代大语言模型都是在此基础上的三种变体：BERT 使用 Encoder Only，GPT/Llama 使用 Decoder Only，T5/BART 使用 Encoder + Decoder。

---

# 1. Transformer 的整体架构

Transformer 原始论文（2017）提出的模型包含两部分：

```
                Transformer

         ┌───────────────┐
Input --->│   Encoder     │
         └───────────────┘
                  │
                  ▼
        Encoder Output
                  │
                  ▼
         ┌───────────────┐
Target -->│   Decoder     │
         └───────────────┘
                  │
                  ▼
             Next Token
```

整个模型可以理解为两个阶段：

```
理解（Understand）

↓

生成（Generate）
```

Encoder 负责理解输入内容，Decoder 根据理解后的信息逐步生成输出。

---

# 2. Encoder 的作用

Encoder 的职责只有一个：

> **理解输入文本，并生成高质量的语义表示（Semantic Representation）。**

例如：

输入一句话：

```
The cat is sleeping.
```

经过多层 Encoder 后：

模型不会直接输出文字，而是得到：

```
Context Representation
```

例如：

```
cat

↓

动物

↓

主语

↓

正在睡觉
```

Encoder 会把整句话转换成能够表达语义的信息表示。

因此：

Encoder 更像一位：

> **阅读理解专家（Reader）**

它只负责：

```
理解输入
```

而不会负责生成文本。

---

# 3. Decoder 的作用

Decoder 的职责与 Encoder 相反。

它负责：

> **根据已经理解的信息，一个 Token 一个 Token 地生成输出。**

例如：

翻译：

```
The cat is sleeping.
```

Decoder 并不是一次输出：

```
猫正在睡觉。
```

而是逐步生成：

```
猫

↓

正在

↓

睡觉

↓

。
```

每一步都预测：

```
Next Token
```

因此：

Decoder 更像一位：

> **写作者（Writer）**

负责连续生成文本。

---

# 4. 为什么 Decoder 需要 Mask？

假设模型正在生成：

```
I love dogs
```

当预测：

```
love
```

时，

如果模型能够提前看到：

```
dogs
```

那么预测就失去了意义。

因此：

Decoder 的 Self-Attention 增加了一种特殊机制：

```
Masked Self-Attention
```

又称：

- Causal Mask
- Look-Ahead Mask

作用是：

> **只能看到当前 Token 左边已经生成的内容，不能看到未来 Token。**

例如：

```
      I   love  dogs

I     ✓    ✗     ✗

love  ✓    ✓     ✗

dogs  ✓    ✓     ✓
```

因此：

Decoder 始终按照：

```
过去

↓

现在

↓

未来
```

的顺序生成文本。

---

# 5. 为什么 Encoder 不需要 Mask？

Encoder 的目标不是生成，

而是理解。

例如：

```
Tom loves his dog.
```

理解：

```
Tom
```

时，

完全可以同时参考：

```
dog
```

因此：

Encoder 的 Self-Attention 可以看到整句话。

这种 Attention 被称为：

```
Bidirectional Attention
```

即：

所有 Token 都可以互相访问。

例如：

```
Tom

↓

dog
```

```
dog

↓

Tom
```

互相都可以关注。

因此：

Encoder 能够充分利用上下文理解整句话。

---

# 6. Encoder 与 Decoder 最大的区别

二者最大的区别并不是 Layer 数量，

也不是网络结构。

真正区别在于：

**Attention 是否使用 Mask。**

### Encoder

```
可以看到所有 Token
```

采用：

```
Bidirectional Attention
```

适合理解任务。

---

### Decoder

```
只能看到过去 Token
```

采用：

```
Masked Self-Attention
```

适合文本生成任务。

---

# 7. 什么是 Cross Attention？

论文中的 Decoder 实际上包含三个模块：

```
Masked Self-Attention

↓

Cross Attention

↓

Feed Forward Network
```

为什么还需要：

```
Cross Attention
```

原因是：

Decoder 在生成文本时，

不仅需要参考自己已经生成的内容，

还需要参考 Encoder 对输入的理解。

例如：

翻译：

```
The cat is sleeping.
```

Encoder 已经理解：

```
英文语义
```

Decoder 在生成：

```
猫

↓

正在

↓

睡觉
```

时，

会不断查看 Encoder 输出的信息。

因此：

Cross Attention 可以理解为：

> **Decoder 与 Encoder 之间的信息交流。**

---

# 8. 为什么 GPT 没有 Encoder？

GPT 的目标只有一个：

```
预测下一个 Token
```

它并不需要先完整理解输入，

再生成输出。

因此：

GPT 删除了：

```
Encoder
```

同时：

也删除了：

```
Cross Attention
```

最终只保留：

```
Masked Self-Attention

↓

Feed Forward Network
```

不断预测：

```
Next Token
```

因此：

GPT 属于：

```
Decoder Only
```

架构。

---

# 9. 为什么 BERT 没有 Decoder？

BERT 的目标不是生成文本，

而是理解文本。

例如：

- 文本分类
- 情感分析
- 检索
- 问答
- Sentence Embedding

这些任务都只需要：

```
理解
```

而不需要：

```
生成
```

因此：

BERT 保留：

```
Encoder
```

删除：

```
Decoder
```

因此属于：

```
Encoder Only
```

架构。

---

# 10. 为什么 T5 保留两者？

T5、BART 等模型主要完成：

- 翻译
- 摘要
- 文本改写
- 问答生成

这些任务同时需要：

```
理解输入

+

生成输出
```

因此：

它们完整保留：

```
Encoder

↓

Decoder
```

属于：

```
Encoder-Decoder
```

架构。

---

# 11. Transformer 三种经典架构

现代几乎所有大语言模型都属于下面三类。

| 架构 | 代表模型 | 主要作用 |
|------|---------|---------|
| Encoder Only | BERT、RoBERTa | 理解文本（Understanding） |
| Decoder Only | GPT、Llama、DeepSeek、Qwen | 文本生成（Generation） |
| Encoder + Decoder | T5、BART | 理解 + 生成（Seq2Seq） |

---

# 12. 为什么 ChatGPT 能聊天？

ChatGPT 本质上就是：

```
Decoder Only Transformer
```

它每一步都执行：

```
预测下一个 Token
```

例如：

```
Hello,

↓

how

↓

are

↓

you

↓

?
```

不断循环：

```
输入已有 Token

↓

预测下一个 Token

↓

加入输入

↓

继续预测
```

因此：

聊天本质上就是：

> **不断重复 Next Token Prediction。**

---

# 13. Transformer 家族的发展

Transformer 原始论文提出：

```
Transformer

        │
 ┌──────┴──────┐
 │             │
Encoder      Decoder
```

后来逐渐演化出三种方向：

```
Transformer

                │
      ┌─────────┴──────────┐
      │                    │

Encoder Only         Decoder Only

(BERT)              (GPT / Llama)

      │                    │

理解文本             生成文本

      └─────────┬──────────┘
                │

       Encoder + Decoder

          (T5 / BART)

      理解 + 生成
```

因此：

今天几乎所有 NLP 模型，

都可以看作 Transformer 的不同变体。

---

# 本节重点（必须掌握）

## Encoder

作用：

> **理解输入文本。**

特点：

- Bidirectional Attention
- 可以看到整句话
- 不生成文本

代表模型：

- BERT
- RoBERTa

---

## Decoder

作用：

> **逐步生成输出文本。**

特点：

- Masked Self-Attention
- 只能看到过去 Token
- 每次预测一个 Token

代表模型：

- GPT
- Llama
- DeepSeek
- Qwen

---

## Cross Attention

作用：

> **让 Decoder 在生成文本时参考 Encoder 的输出。**

主要用于：

- 翻译
- 摘要
- Seq2Seq 任务

---

## 三种 Transformer 架构

| 架构 | 是否理解输入 | 是否生成文本 |
|------|-------------|-------------|
| Encoder Only | ✅ | ❌ |
| Decoder Only | 部分（依赖上下文） | ✅ |
| Encoder + Decoder | ✅ | ✅ |

---

# 一句话总结

> **Transformer 最初由 Encoder 和 Decoder 两部分组成。Encoder 负责理解输入，因此可以看到整句话（Bidirectional Attention）；Decoder 负责生成输出，因此只能看到已经生成的内容（Masked Self-Attention）。现代大模型都是 Transformer 的不同演化方向：BERT 属于 Encoder Only，GPT/Llama 属于 Decoder Only，T5/BART 保留完整的 Encoder-Decoder 架构，用于同时完成理解和生成任务。**

# Transformer 笔记：为什么需要 Decoder？为什么要逐 Token 生成？

## 1. 为什么不能只有 Encoder？

很多人第一次学习 Transformer 时都会有一个疑问：

> Encoder 不是已经理解整句话了吗？为什么不能直接输出最终答案，而要再经过 Decoder 一个 Token 一个 Token 地生成？

答案是：

> **Encoder 负责理解输入，但不知道应该输出什么，也不知道应该输出多长。生成（Generation）本身是一个动态过程，而不是一次性的分类问题。**

**Without the causal mask, the model learns to rely on future tokens during training. At inference time those future tokens do not exist, so the model cannot generate text correctly.** 

---

# 2. Encoder 的作用：理解输入

例如机器翻译：

```text
Input:
I love machine learning.
```

Encoder 的任务就是把整句话编码成一系列高质量的语义表示。

可以理解为：

```text
Encoder

↓

"我已经理解了这句话表达的是：
一个人喜欢机器学习。"
```

但是：

Encoder **并不会直接输出中文。**

它只是得到：

```
Hidden Representation
```

也就是整个输入的语义表示。

---

# 3. 为什么不能一次输出整句话？

假设希望 Encoder 一次输出：

```text
我喜欢机器学习。
```

那么模型首先需要知道：

- 一共输出多少个 Token？
- 第一个 Token 是什么？
- 第二个 Token 是什么？
- ...
- 最后什么时候结束？

现实中：

不同句子的长度完全不同，例如：

```text
Hello.
↓

你好。
```

只有两个 Token。

而：

```text
The weather is very nice today.
↓

今天天气非常不错。
```

长度又完全不同。

因此：

> **Encoder 根本不知道应该输出多少个 Token。**

如果把每一句完整的话都当作一个分类类别，那么世界上的句子几乎是无限多的，这是不可能实现的。

---

# 4. Decoder 的作用：逐 Token 生成

Transformer 使用了一种更加灵活的方法：

不是预测整句话，而是：

> **一次预测一个 Token（Next Token Prediction）。**

例如：

目标句子：

```text
我 喜欢 机器 学习 。
```

Decoder 的工作过程：

```
Step1

输入：

<START>

↓

预测：

我
```

```
Step2

输入：

我

↓

预测：

喜欢
```

```
Step3

输入：

我 喜欢

↓

预测：

机器
```

```
Step4

输入：

我 喜欢 机器

↓

预测：

学习
```

一直重复，直到预测出：

```
<EOS>
```

表示句子结束。

这样无论句子长短，都可以生成。

---

# 5. 为什么 Decoder 每一步都要参考已经生成的内容？

语言具有强烈的上下文依赖。

例如：

已经生成：

```text
我
```

下一步可能是：

```text
喜欢
```

也可能是：

```text
讨厌
```

当已经生成：

```text
我 喜欢
```

下一步又可能变成：

```text
机器
```

或者：

```text
数学
```

因此：

> **Decoder 每一步都会把已经生成的 Token 当作新的输入，再预测下一个 Token。**

生成过程不断循环：

```
已有 Token

↓

预测 Next Token

↓

加入输入

↓

继续预测
```

直到生成结束。

---

# 6. Decoder 并不是"边学边生成"

这里很容易产生误解。

训练（Training）：

```
更新参数（Weight）
```

推理（Inference）：

```
固定参数

↓

不断预测 Next Token
```

因此：

Decoder 在生成过程中：

**不会继续学习。**

它只是不断利用已经训练好的参数，根据当前上下文预测下一 Token。

例如：

第一次：

```
P(我)=0.82

↓

输出：

我
```

第二次：

输入变成：

```
我
```

预测：

```
P(喜欢)=0.91

↓

输出：

喜欢
```

第三次：

输入变成：

```
我 喜欢
```

继续预测：

```
机器
```

模型参数始终保持不变。

---

# 7. Encoder 和 Decoder 的分工

可以把两者理解成：

```
Encoder

↓

阅读（Read）

理解（Understand）
```

Decoder：

```
写作（Write）

生成（Generate）
```

例如：

```
Encoder

"I love machine learning."

↓

已经理解整句话的含义
```

然后：

```
Decoder

↓

我

↓

喜欢

↓

机器

↓

学习

↓

。

↓

<EOS>
```

一步一步完成输出。

---

# 8. 为什么现代 GPT 不需要 Encoder？

最初 Transformer（2017）：

```
Encoder

↓

理解输入

↓

Decoder

↓

生成输出
```

后来研究发现：

如果一直训练模型完成：

```
Predict Next Token
```

模型会自动学会：

- 理解语言
- 推理
- 总结
- 翻译
- 问答

因此：

GPT 系列直接删除了 Encoder，只保留：

```
Decoder Only
```

整个训练目标始终只有一句话：

```
Predict the Next Token.
```

理解能力就是在不断预测下一个 Token 的过程中自然学出来的。

---

# 整个生成流程

```
Input

↓

Encoder（理解语义）

↓

Hidden Representation

↓

Decoder

↓

预测第1个 Token

↓

预测第2个 Token

↓

预测第3个 Token

↓

...

↓

预测 <EOS>

↓

结束
```

现代 GPT：

```
Prompt

↓

Decoder

↓

Next Token

↓

Next Token

↓

Next Token

↓

...

↓

<EOS>
```

---

# 本节重点

- Encoder 负责理解输入，不负责决定输出内容和输出长度。
- 自然语言长度是不固定的，因此不能把整句话作为一次分类问题。
- Decoder 采用自回归（Autoregressive）方式，每次只预测一个 Token，再利用已经生成的内容预测下一个 Token。
- 推理过程中模型参数不会更新，Decoder 只是不断调用已经训练好的模型完成生成。
- GPT 删除了 Encoder，只保留 Decoder，并通过持续进行 Next Token Prediction 同时获得语言理解和生成能力。

---

# 一句话总结

> **Encoder 负责理解（Understand），Decoder 负责生成（Generate）。语言生成不是一次性输出整句话，而是不断根据已有上下文预测下一个 Token，因此 Decoder 必须采用自回归（Autoregressive）的方式逐 Token 生成。现代 GPT 更进一步，将理解和生成统一到 Decoder 中，仅通过 Next Token Prediction 就学会了语言理解与文本生成。**


# 9. 为什么现代大语言模型（LLM）几乎都没有 Encoder？

最初的 Transformer（2017）采用的是 **Encoder + Decoder** 架构。

```
Input

↓

Encoder（理解输入）

↓

Hidden Representation

↓

Decoder（生成输出）
```

这种结构非常适合：

- 机器翻译（Translation）
- 文本摘要（Summarization）
- 问答（Question Answering）

因为：

输入和输出都是完整的句子。

例如：

```
English

↓

Chinese
```

Encoder 负责理解英文。

Decoder 根据 Encoder 的语义表示生成中文。

---

后来研究人员发现：

如果训练目标始终都是：

```
Predict the Next Token
```

模型在学习预测下一个 Token 的过程中，会自动学会：

- 语言理解（Language Understanding）
- 推理（Reasoning）
- 知识记忆（Knowledge）
- 代码生成（Code Generation）
- 翻译（Translation）
- 问答（Question Answering）

因此：

> **其实不需要单独设计一个 Encoder。**

Decoder 在不断预测下一个 Token 的过程中，也能够完成理解。

于是：

现代 GPT 系列直接删除了 Encoder，只保留 Decoder。

---

# 10. 为什么 Decoder Only 就足够？

例如：

用户输入：

```
Translate into Chinese:

I love machine learning.
```

整个 Prompt 变成：

```
Translate into Chinese:

I love machine learning.

Answer:
```

然后：

Decoder 开始预测：

```
我

↓

喜欢

↓

机器

↓

学习

↓

。
```

这里：

模型并没有一个专门负责"理解"的 Encoder。

而是：

在预测第一个 Token 之前，

Decoder 已经通过 Self-Attention 阅读了整个 Prompt。

因此：

> **对于 Decoder 来说，"理解 Prompt" 和 "生成答案" 是同一个过程。**

---

# 11. 为什么 Decoder 能看到整个 Prompt？

很多人误以为：

Decoder 每次只能看到前一个 Token。

实际上：

对于 Prompt：

```
Translate into Chinese:

I love machine learning.
```

Decoder 可以看到：

```
Translate

↓

into

↓

Chinese

↓

I

↓

love

↓

machine

↓

learning
```

整个 Prompt。

只是：

当它开始生成：

```
我
```

以后，

未来生成的 Token：

```
喜欢

机器

学习
```

还不存在。

因此：

Decoder 使用 **Causal Mask**：

```
Prompt

↓

全部可见

↓

已经生成的 Token

↓

可见

↓

未来还没有生成的 Token

↓

不可见
```

这保证了模型能够进行真正的自回归（Autoregressive）生成。

---

# 12. 当前主流 Transformer 架构

| 架构 | 是否有 Encoder | 是否有 Decoder | 代表模型 | 主要任务 |
|-------|---------------|---------------|----------|----------|
| Encoder Only | ✅ | ❌ | BERT、RoBERTa、DeBERTa | 文本理解、分类、NER、检索 |
| Encoder + Decoder | ✅ | ✅ | Original Transformer、T5、BART | 翻译、摘要、Seq2Seq |
| Decoder Only | ❌ | ✅ | GPT-1/2/3/4、ChatGPT、Llama、Mistral、Gemma、Qwen、DeepSeek、Claude（架构上）、Phi | 文本生成、大语言模型 |

---

# 13. 为什么现在几乎所有 LLM 都采用 Decoder Only？

原因主要有四个：

### （1）训练目标简单统一

始终只有一个目标：

```
Predict the Next Token
```

不需要针对不同任务设计不同模型。

---

### （2）所有 NLP 任务都可以转化成 Prompt

例如：

翻译：

```
Translate to Chinese:

I love AI.
```

总结：

```
Summarize:

...
```

问答：

```
Question:

...

Answer:
```

代码：

```
Write Python code...
```

模型始终做同一件事：

```
预测下一个 Token。
```

---

### （3）Scaling Law 效果最好

大量研究发现：

随着：

- 数据越来越多
- 参数越来越大
- 计算越来越多

Decoder Only 模型的性能持续提升。

这就是 GPT、Llama 等模型不断扩大的理论基础。

---

### （4）推理（Inference）更自然

聊天本身就是：

```
用户输入

↓

模型回答

↓

继续回答

↓

继续生成
```

天然符合：

```
Autoregressive Generation
```

因此 Decoder Only 非常适合聊天机器人。

---

# 本节重点

- 原始 Transformer 使用 Encoder + Decoder。
- GPT 系列发现，只训练 Next Token Prediction，Decoder 就能够同时学会语言理解和文本生成，因此删除了 Encoder。
- 现代 LLM（GPT、Llama、Qwen、DeepSeek、Gemma、Mistral、Phi 等）几乎全部采用 Decoder Only 架构。
- Decoder 在生成前能够看到整个 Prompt，在生成过程中只能看到已经生成的 Token，而不能看到未来 Token，因此需要使用 Causal Mask。
- Prompt Engineering 的本质，就是把各种 NLP 任务统一转换成"预测下一个 Token"的问题。

---

# 一句话总结

> **现代大语言模型几乎都采用 Decoder Only 架构，因为 Next Token Prediction 已经能够同时学习语言理解和文本生成。通过阅读整个 Prompt，再利用 Causal Mask 逐 Token 自回归生成，Decoder 将"理解"和"生成"统一到了同一个模型中。**


# Transformer 笔记：Attention Complexity 与 KV Cache

## 1. 为什么 Attention 的计算成本是 \(O(N^2)\)？

假设输入序列长度为 **N**，每个 Token 的隐藏维度（Head Width）为 **d**。

Self-Attention 的第一步是计算：

\[
QK^T
\]

其中：

- Q 的形状为 **N × d**
- K 的形状为 **N × d**
- \(K^T\) 的形状为 **d × N**

矩阵相乘后得到：

\[
QK^T
\]

其形状为：

\[
N \times N
\]

这个矩阵称为 **Attention Score Matrix**。

矩阵中的每一个元素表示：

> 第 \(i\) 个 Token 对第 \(j\) 个 Token 的 Attention Score。

也就是说，每一个 Token 都需要与所有 Token 计算一次 Dot Product。

例如：

```
Token0 → Token0 Token1 Token2 ... TokenN
Token1 → Token0 Token1 Token2 ... TokenN
...
TokenN → Token0 Token1 Token2 ... TokenN
```

因此：

总共有

```
N × N = N²
```

个 Attention Scores。

---

# 2. 为什么 Memory Complexity 是 \(O(N^2)\)？

因为必须保存整个：

```
Attention Score Matrix

↓

N × N
```

例如：

```
N = 1,000

↓

Score Matrix

↓

1000 × 1000

↓

1,000,000 Scores
```

如果：

```
N = 2,000
```

则：

```
2000 × 2000

↓

4,000,000 Scores
```

因此：

当 Context 长度翻倍时：

```
Memory

↓

4 倍
```

所以：

Attention 的内存复杂度为：

\[
O(N^2)
\]

---

# 3. 为什么 Compute Complexity 是 \(O(N^2d)\)？

每一个 Attention Score 都是一个 Dot Product：

\[
q \cdot k
\]

假设：

```
Head Width = d
```

那么：

一次 Dot Product 需要：

```
d 次乘加运算
```

而：

整个 Score Matrix 中共有：

```
N²
```

个 Dot Product。

因此：

总计算量为：

\[
O(N^2d)
\]

其中：

- \(N\)：Context Length
- \(d\)：Head Dimension（Head Width）

---

# 4. 为什么 Long Context 很昂贵？

Attention 的计算和存储都会随着：

```
N²
```

增长。

例如：

```
8K Context

↓

8K² = 64M Scores
```

如果扩展到：

```
32K Context

↓

32K² = 1B Scores
```

Context 增加 4 倍：

```
Memory

↓

16 倍
```

因此：

> **Long Context 最大的瓶颈就是 Self-Attention 的 Quadratic Scaling（平方增长）。**

目前很多研究（FlashAttention、Sparse Attention、Linear Attention 等）都是为了降低这一计算成本。

---

# 5. 为什么 Naive Generation 很慢？

GPT 采用：

```
Autoregressive Generation
```

即：

```
生成一个 Token

↓

再生成下一个 Token
```

假设已经生成：

```
I love
```

下一步预测：

```
machine
```

Naive 方法会重新计算：

```
I

↓

K、V

love

↓

K、V

machine

↓

K、V
```

下一步生成：

```
learning
```

又重新计算：

```
I

↓

K、V

love

↓

K、V

machine

↓

K、V

learning

↓

K、V
```

注意：

前面的：

```
I

love
```

已经固定，

它们对应的：

```
Key

Value
```

永远不会改变。

但是：

Naive 方法仍然每一步都重新计算一次。

这些计算全部都是重复计算（Redundant Computation）。

---

# 6. 为什么 Naive Generation 的复杂度也是 \(O(N^2)\)？

假设生成：

```
N
```

个 Token。

第 1 步：

```
计算 1 个 KV
```

第 2 步：

```
计算 2 个 KV
```

第 3 步：

```
计算 3 个 KV
```

...

第 N 步：

```
计算 N 个 KV
```

总计算次数：

\[
1+2+3+\cdots+N
=
\frac{N(N+1)}2
\]

因此：

复杂度为：

\[
O(N^2)
\]

其中大量计算都是重复的。

---

# 7. KV Cache 如何解决这个问题？

由于历史 Token 的：

```
Key

Value
```

一旦计算完成，就永远不会改变。

因此：

可以第一次计算之后直接保存下来。

例如：

第一次生成：

```
I

↓

计算 KI

计算 VI
```

保存到：

```
KV Cache
```

下一步：

```
love

↓

只计算：

Klove

Vlove
```

再加入：

```
KV Cache
```

继续生成：

```
machine

↓

只计算：

Kmachine

Vmachine
```

不断追加：

```
KV Cache

↓

KI

Klove

Kmachine

...

VI

Vlove

Vmachine

...
```

这样：

每一步只需要计算：

> **新 Token 自己的一行 K 和 V。**

历史 Token 的 K/V 全部直接复用。

---

# 8. KV Cache 带来的效果

没有 KV Cache：

```
Step1

↓

重新计算全部 KV

Step2

↓

重新计算全部 KV

Step3

↓

重新计算全部 KV

...
```

时间复杂度：

```
O(N²)
```

有 KV Cache：

```
Step1

↓

计算新 Token 的 KV

Step2

↓

计算新 Token 的 KV

Step3

↓

计算新 Token 的 KV

...
```

每一步：

只新增：

```
1 行 K

1 行 V
```

大幅减少重复计算。

因此：

现代所有 Decoder-only LLM：

- GPT
- ChatGPT
- Llama
- DeepSeek
- Qwen
- Gemma
- Mistral
- Phi

都会使用 **KV Cache** 来加速推理。

---

# 本节重点

- Self-Attention 需要计算所有 Token 两两之间的 Attention Score，因此 Score Matrix 的大小为 **N × N**。
- Score Matrix 的存储复杂度为 **\(O(N^2)\)**，计算复杂度为 **\(O(N^2d)\)**。
- 当 Context Length 翻倍时，Attention 的 Memory 和 Compute 都会增加约 **4 倍**（Quadratic Scaling）。
- GPT 在自回归生成过程中，如果每一步都重新计算所有历史 Token 的 K/V，会产生 **\(O(N^2)\)** 的重复计算。
- KV Cache 将历史 Token 的 Key 和 Value 保存下来，之后只计算新生成 Token 的 K/V，从而避免重复计算，大幅提升推理速度。

---

# 一句话总结

> **Attention 的主要瓶颈来自于 \(N \times N\) 的 Score Matrix，使得计算和存储都呈平方增长（Quadratic Scaling）。在自回归生成中，历史 Token 的 Key 和 Value 不会改变，因此现代 LLM 使用 KV Cache 将它们缓存下来，每一步只计算新 Token 的 K/V，从而避免 \(O(N^2)\) 的重复计算，这也是 GPT、Llama、DeepSeek 等模型实现高效推理的关键技术。**
