# Temperature（温度）与 Token Sampling

Temperature 是 推理（Inference）阶段的超参数，不参与模型训练。
Temperature 在 Softmax 之前对 Logits 做缩放。

## 1. 什么是 Temperature？

Temperature（温度）是 **Transformer 在推理（Inference）阶段使用的超参数**，用于控制模型生成文本时的随机性。它不会改变模型的参数，也不会影响 Attention 或 Transformer 的计算过程，而是在最后一步 **Softmax 之前对 Logits 进行缩放（Rescaling）**。

Temperature 的公式为：

<img width="117" height="55" alt="image" src="https://github.com/user-attachments/assets/3d857a88-0c73-4f5e-b85d-303d1a37ce78" />

其中：

- \(z_i\) 表示模型输出的 Logits。
- \(T>0\) 表示 Temperature。
- 当 \(T=1\) 时，恢复普通 Softmax，不对模型输出进行任何修改。

因此，Temperature 的本质就是：

> **在 Softmax 之前，将所有 Logits 除以 Temperature，然后再计算概率分布。**

---

# 2. Temperature 为什么能够改变生成结果？

Transformer 在预测下一个 Token 时，首先输出的是一组 Logits，而不是概率。例如：

| Token | Logit |
|------|------:|
| Paris | 8.0 |
| London | 7.0 |
| Berlin | 6.0 |

这些 Logits 会经过 Softmax 转换为概率，然后模型再根据这些概率选择下一个 Token。

Temperature 改变的是 Logits 之间的相对差距，因此会直接影响 Softmax 输出的概率分布。如果 Temperature 较小，Logits 之间的差距会被放大，使最高概率的 Token 更加突出；如果 Temperature 较大，Logits 之间的差距会缩小，使概率分布更加均匀。

因此，Temperature 并不会改变模型认为哪个 Token 更合理，而是改变模型选择 Token 时的随机程度。

---

# 3. 当 Temperature 趋近于 0 时会发生什么？

当 Temperature 非常小时，例如：

```
T → 0
```

Logits 会被一个很小的数相除，因此不同 Token 之间的差距会被迅速放大。

例如：

```
原始 Logits

8

7

6
```

如果：

```
T = 0.5
```

变成：

```
16

14

12
```

Softmax 后：

```
0.88

0.11

0.01
```

如果：

```
T → 0
```

则概率会越来越接近：

```
1

0

0
```

这种分布称为 **One-hot Distribution**。

也就是说：

模型几乎总是选择概率最大的那个 Token，这种生成方式也称为 **Greedy Decoding**。

因此：

> **Temperature 越低，模型越确定，生成结果越稳定，但创造性越低。**

---

# 4. 当 Temperature 等于 1 时会发生什么？

当：

```
T = 1
```

时，

公式变成：

\[
\text{Softmax}(z)
\]

这就是普通的 Softmax。

模型输出的概率分布保持训练过程中学到的真实分布，没有任何额外修改。

因此：

> **Temperature = 1 表示完全使用模型自己的预测概率。**

---

# 5. 当 Temperature 趋近于无穷大时会发生什么？

如果 Temperature 很大，例如：

```
T = 100
```

则：

```
8

7

6
```

会变成：

```
0.08

0.07

0.06
```

所有 Logits 会越来越接近。

经过 Softmax 后，概率也越来越接近：

```
0.33

0.33

0.34
```

最终：

所有 Token 的概率都会趋近于相等，也就是：

```
Uniform Distribution
```

这种情况下：

模型几乎是在随机选择 Token。

Slide 中提到：

```
maximum entropy
```

意思就是：

> **随机性达到最大，模型几乎不会偏向任何一个 Token。**

因此：

> **Temperature 越高，模型生成越随机、越具有多样性，但也越容易生成错误或不连贯的内容。**

---

# 6. Temperature 与 Entropy（熵）的关系

Entropy（熵）可以理解为：

> **概率分布的不确定性或随机程度。**

例如：

```
1

0

0
```

表示模型百分之百确定选择第一个 Token，此时 Entropy 最低。

而：

```
0.33

0.33

0.34
```

表示三个 Token 几乎都有相同概率，此时 Entropy 最大。

因此：

- Temperature 越低，Entropy 越低，模型越确定。
- Temperature 越高，Entropy 越高，模型越随机。

---

# 7. Temperature 在实际应用中的作用

Temperature 不会影响模型学到的知识，而是影响生成文本时的风格。

对于不同任务，通常会选择不同的 Temperature：

- **代码生成（Code Generation）**：通常使用较低的 Temperature（0~0.2），希望模型尽量输出最正确、最稳定的答案。
- **普通聊天（ChatGPT）**：通常使用中等 Temperature（约 0.7），在准确性和多样性之间取得平衡。
- **创意写作、小说、Brainstorm**：通常使用较高的 Temperature（0.8~1.2），让模型生成更多样、更有创造力的内容。
- **极高 Temperature（>1.5）**：模型会越来越随机，甚至可能生成语义不连贯或错误的信息。

---

# 8. Temperature 在 Transformer 中的位置

Transformer 的推理流程如下：

```
Input

↓

Transformer

↓

Logits

↓

Temperature Scaling

↓

Softmax

↓

Probability Distribution

↓

Sampling

↓

Next Token
```

可以看到：

Temperature 位于 **Transformer 输出 Logits 与 Softmax 之间**。

因此：

它不会改变 Transformer 本身，也不会影响 Attention 的计算，而只是控制最终生成 Token 的随机程度。

---

# 本节重点

- Temperature 是 Transformer 推理阶段的重要超参数，用于控制生成时的随机性。
- Temperature 的作用是在 Softmax 之前缩放 Logits，而不会修改模型参数。
- 当 **T < 1** 时，Logits 差距被放大，概率更加尖锐，模型更确定、更接近 Greedy Decoding。
- 当 **T = 1** 时，模型使用原始学习到的概率分布。
- 当 **T > 1** 时，Logits 差距缩小，概率更加平坦，模型生成更加随机、更加多样化。
- 当 **T → 0** 时，概率趋近于 One-hot Distribution；当 **T → ∞** 时，概率趋近于 Uniform Distribution，Entropy 达到最大。
- Temperature 只影响 Token Sampling，不影响 Transformer 的参数、Attention 或训练过程。

---

# 一句话总结

> **Temperature 是 Softmax 前对 Logits 进行缩放的超参数，用于控制模型生成时的随机程度。较低的 Temperature 会使模型更加确定、更倾向于选择概率最高的 Token；较高的 Temperature 会使概率分布更加均匀，从而增加生成结果的多样性和创造性，但同时也会增加生成错误内容的风险。**
