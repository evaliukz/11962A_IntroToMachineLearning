# Transformer 笔记：Cross-Attention（交叉注意力）GPT没有

## 1. 什么是 Cross-Attention？

Cross-Attention（交叉注意力）是 **Encoder-Decoder Transformer** 中 Decoder 使用的第二种 Attention 机制。与 Self-Attention 不同，Cross-Attention 并不是让一个 Token 去关注自己的序列，而是让 **Decoder 中的 Token 去查询（Query）Encoder 已经编码完成的输入序列**。

Self-Attention 可以理解为：

> **"我应该关注我自己这句话里的哪些 Token？"**

而 Cross-Attention 可以理解为：

> **"我现在准备生成下一个 Token，我应该回头参考输入句子的哪些部分？"**

因此，Cross-Attention 的作用就是：

> **让 Decoder 在生成每一个 Token 时，都能够读取 Encoder 已经理解好的输入信息。**

---

# 2. 为什么需要 Cross-Attention？

假设我们要完成机器翻译：

```
English

I love machine learning.
```

翻译成：

```
Chinese

我 喜欢 机器 学习。
```

Encoder 首先读取整个英文句子：

```
I

love

machine

learning
```

Encoder 利用 Self-Attention 理解整个句子的语义，并输出一组隐藏表示（Hidden States）。

随后：

Decoder 开始逐 Token 生成中文。

例如：

已经生成：

```
我

喜欢
```

下一步准备生成：

```
机器
```

这时：

Decoder 必须知道：

> **"我现在应该参考英文里的哪个单词？"**

于是：

Decoder 会通过 Cross-Attention 去查看 Encoder 的输出。

例如：

当前 Decoder Token：

```
喜欢
```

Cross-Attention 可能得到：

```
love

↓

Attention Weight = 0.92
```

说明：

Decoder 当前主要关注英文中的：

```
love
```

于是继续生成：

```
机器
```

因此：

> **Cross-Attention 的作用就是让 Decoder 在生成目标语言时，不断读取 Encoder 已经编码好的输入信息。**

---

# 3. Cross-Attention 的公式

Cross-Attention 与 Self-Attention 的数学公式几乎完全相同：

<img width="537" height="137" alt="image" src="https://github.com/user-attachments/assets/21e7eb8f-dada-42ba-890d-12f0a77e6e54" />


唯一不同的是：

- Query（Q）来自 **Decoder**
- Key（K）来自 **Encoder**
- Value（V）来自 **Encoder**

因此：

> **Cross-Attention 并没有改变 Attention 的计算方法，只改变了 Q、K、V 的来源。**

---

# 4. Self-Attention 与 Cross-Attention 的区别

在 Self-Attention 中：

所有的：

```
Q

K

V
```

都来自同一个序列。

例如：

Encoder：

```
English Sentence

↓

Q

K

V

↓

Self-Attention
```

或者：

Decoder：

```
Generated Tokens

↓

Q

K

V

↓

Masked Self-Attention
```

而 Cross-Attention 中：

```
Decoder

↓

Q

↓

Encoder

↓

K

V
```

Decoder 使用自己的 Query 去查询 Encoder 提供的 Key 和 Value。

可以理解为：

> **Decoder 负责提问（Query），Encoder 负责提供知识（Key、Value）。**

---

# 5. Decoder 每一层实际上包含两个 Attention

在原始 Transformer 中，

每一个 Decoder Layer 都会依次执行三个子模块：

```
1.

Masked Self-Attention

↓

2.

Cross-Attention

↓

3.

Feed Forward Network (FFN)
```

第一步：

Masked Self-Attention。

Decoder 首先只能关注已经生成的目标语言 Token。

例如：

```
我

喜欢
```

不能偷看未来还没有生成的：

```
机器

学习
```

因此：

这里必须使用：

```
Causal Mask
```

第二步：

Cross-Attention。

Decoder 利用当前 Token 的 Query 去读取 Encoder 的 Hidden States，从整个输入句子中寻找最相关的信息。

第三步：

Feed Forward Network。

进一步对当前 Token 的表示进行非线性变换。

因此：

每一个 Decoder Layer 都同时完成：

- 理解已经生成的目标序列；
- 查询输入序列；
- 更新当前 Token 的表示。

---

# 6. 为什么 Cross-Attention 不需要 Mask？

这是 Cross-Attention 与 Decoder Self-Attention 最大的区别。

对于 Decoder 自己来说：

未来 Token 还没有生成。

例如：

```
我

喜欢

？

？
```

如果允许当前 Token 看到未来 Token，

模型就会发生：

```
Information Leakage
```

因此：

Decoder Self-Attention 必须使用：

```
Causal Mask
```

但是：

Encoder 已经完整读取了整个输入句子。

例如：

```
I

love

machine

learning
```

这些输入在 Decoder 开始生成之前就已经全部存在。

因此：

Decoder 查看整个输入句子不会造成任何信息泄漏。

所以：

> **Cross-Attention 不需要使用 Causal Mask。**

---

# 7. Encoder-Decoder 的完整工作流程

整个 Transformer Encoder-Decoder 的工作过程如下：

```
Input Sentence

↓

Encoder

↓

Hidden States

↓

K

V

────────────────────────────

Decoder

↓

Masked Self-Attention

↓

当前 Token 的 Query

↓

Cross-Attention

↓

Q × KEncoder

↓

Weighted Sum(VEncoder)

↓

Feed Forward

↓

Predict Next Token
```

因此：

Encoder 负责理解输入；

Decoder 负责利用 Encoder 的知识逐步生成目标序列。

---

# 8. 为什么 GPT 没有 Cross-Attention？

现代 GPT、Llama、DeepSeek、Qwen 等 Decoder-only LLM 都没有 Encoder。

因此：

自然也没有：

```
Cross-Attention
```

例如：

Prompt：

```
Translate to Chinese:

I love AI.

Answer:
```

整个 Prompt 都已经放在 Decoder 输入中。

Decoder 自己就能够看到：

```
Translate

to

Chinese

I

love

AI

Answer:
```

因此：

Prompt 的理解和答案生成全部发生在：

```
Masked Self-Attention
```

里面。

所以：

GPT 不需要 Encoder，也不需要 Cross-Attention。

---

# 9. 三种 Attention 对比

| Attention 类型 | Query 来源 | Key 来源 | Value 来源 | 是否需要 Causal Mask | 是否读取其他序列 | 代表模型 |
|---------------|-----------|----------|------------|----------------------|-----------------|-----------|
| **Encoder Self-Attention** | Encoder | Encoder | Encoder | ❌ 不需要 | ❌ 只读取自己的输入序列 | BERT、RoBERTa、ViT、Transformer Encoder |
| **Decoder Masked Self-Attention** | Decoder | Decoder | Decoder | ✅ 需要 | ❌ 只读取已经生成的目标序列 | GPT、Llama、DeepSeek、Qwen、Transformer Decoder |
| **Cross-Attention** | Decoder | Encoder | Encoder | ❌ 不需要 | ✅ 读取 Encoder 编码后的输入序列 | Original Transformer、T5、BART、Encoder-Decoder 模型 |

---

# 本节重点

- Cross-Attention 与 Self-Attention 的计算公式完全相同，唯一的区别在于 Query 来自 Decoder，而 Key 和 Value 来自 Encoder。
- Cross-Attention 的作用是让 Decoder 在生成目标 Token 时，从 Encoder 已经编码好的输入序列中读取最相关的信息。
- 每一个 Decoder Layer 都包含两个 Attention：首先执行 Masked Self-Attention，再执行 Cross-Attention，最后经过 Feed Forward Network。
- Decoder Self-Attention 必须使用 Causal Mask，防止未来信息泄漏；Cross-Attention 不需要 Mask，因为 Encoder 已经完整读取了整个输入序列。
- GPT、Llama、DeepSeek、Qwen 等现代 Decoder-only 模型没有 Encoder，因此也没有 Cross-Attention；所有上下文理解都通过 Masked Self-Attention 完成。

---

# 一句话总结

> **Cross-Attention 是 Encoder-Decoder Transformer 中连接 Encoder 和 Decoder 的桥梁。Decoder 利用自己的 Query 去查询 Encoder 提供的 Key 和 Value，从输入序列中获取最相关的信息，从而逐 Token 生成目标序列。现代 GPT 等 Decoder-only 模型由于没有 Encoder，因此只保留了 Masked Self-Attention，而不使用 Cross-Attention。**
