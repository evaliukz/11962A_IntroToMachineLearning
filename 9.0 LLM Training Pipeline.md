# Transformer 笔记：LLM Training Pipeline（Pretraining → SFT → RL → LoRA）

---

# 1. LLM 的完整训练流程

现代大型语言模型（Large Language Model, LLM）的训练通常可以分为三个阶段：

```
大量互联网文本
        │
        ▼
Stage 1: Pretraining（预训练）
        │
        ▼
Stage 2: Supervised Fine-Tuning (SFT)
        │
        ▼
Stage 3: Reinforcement Learning (RL)
        │
        ▼
ChatGPT / Claude / Gemini / DeepSeek
```

整个流程中，每个阶段承担不同的任务：

- **Pretraining** 负责让模型学习语言、知识和推理能力。
- **SFT** 负责让模型学会按照用户指令回答问题。
- **RL** 负责让模型根据奖励不断优化回答质量，更符合人类偏好。

---

# 2. Stage 1：Pretraining（预训练）

## 2.1 Pretraining 的目标

Pretraining 是整个模型训练过程中计算量最大的阶段。

它只有一个训练目标： **预测下一个 Token（Next Token Prediction）。**

例如：

```
The capital of France is -> Paris
```

模型并不知道自己是在回答问题，也不知道自己是在翻译或者写代码，它只是不断完成一个简单的任务：

> **根据前面的 Token，预测最可能出现的下一个 Token。**

通过数万亿 Token 的训练，模型逐渐学习到：

- 语言结构（Grammar）
- 世界知识（Knowledge）
- 编程能力（Coding）
- 数学规律（Mathematics）
- 推理能力（Reasoning）

因此：

> **Pretraining 并不是教模型如何回答问题，而是让模型成为一个优秀的语言预测器（Language Predictor）。**

---

## 2.2 Scaling Law（缩放定律）

OpenAI 和 DeepMind 在训练 GPT 系列模型时发现了一个非常重要的规律：

> **模型性能会随着模型大小（Parameters）、训练数据（Data）和计算资源（Compute）的增加而持续提升。**

这种关系称为：Scaling Law

它满足一种近似的：Power Law（幂律）

也就是说,随着：

- 更多参数（Parameters）
- 更多训练数据（Tokens）
- 更多计算资源（FLOPs）

模型 Loss 会平滑下降。

<img width="1228" height="588" alt="image" src="https://github.com/user-attachments/assets/7c8e9626-0d67-4ba2-9d37-073d29ce438b" />


例如：

```
100M Parameters

↓

Validation Loss = 2.5

↓

1B Parameters

↓

Validation Loss = 2.2

↓

10B Parameters

↓

Validation Loss = 2.0
```

虽然提升越来越慢，但是：**模型不会突然达到一个性能上限，而是持续按照幂律改善。**

因此现代 LLM 的成功并不是来自更加复杂的网络结构，而是来自： **使用一个简单、通用的 Transformer，然后不断扩大模型规模。**

---

## 2.3 Chinchilla Scaling Law

早期研究者认为：

> **模型越大越好。**

DeepMind 在 **Chinchilla** 论文中提出：

> **固定计算预算（Compute Budget）下，模型大小和训练数据需要保持合理比例。**

例如：

同样使用：

```
100 GPU
```

可以训练：

```
70B 参数

100B Tokens
```

也可以训练：

```
35B 参数

400B Tokens
```

Chinchilla 发现：第二种通常效果更好。

因此： **模型参数增加时，训练数据也必须同步增加。**

如果模型非常大，却没有足够的数据训练, 模型容易：Overfitting（过拟合）

如果数据很多，而模型太小：模型又没有足够容量学习这些知识。

因此：现代 LLM 通常都会遵循：**Parameters 与 Tokens 共同增长。**

---

# 3. Stage 2：Supervised Fine-Tuning（SFT）

完成 Pretraining 后，模型已经能够预测下一个 Token，

但是：它仍然不会真正"当助手"。 例如：

用户输入：

```
How are you?
```

Pretraining 模型可能继续互联网文本：

```
How are you?

↓

How are you doing today?

↓

How are you feeling?
```

它只是： **继续文本（Continue Text）**

而不是：**回答用户的问题。**

因此需要第二阶段：Supervised Fine-Tuning (SFT)

---

## 3.1 SFT 的训练数据

SFT 使用人工整理好的：

```
Instruction

↓

Response
```

数据。

例如：

```
User:

Translate "Hello"

↓

Assistant:

你好
```

或者：

```
User:

Write a Python function

↓

Assistant:

def ...
```

模型依然执行：

```
Next Token Prediction
```

但是预测对象变成了：Assistant Response


因此,模型开始学习：

- 如何回答问题；
- 如何遵循用户指令；
- 如何模仿 ChatGPT 的回答格式。

所以：**SFT 并没有改变训练目标，只是改变了训练数据。**

---

# 4. Stage 3：Reinforcement Learning（RL）

SFT 本质上仍然属于：Imitation Learning（模仿学习）

模型学习的是：老师怎么回答 -> 我就怎么回答

但是：现实中很多问题没有唯一正确答案。例如：Write a poem. 可以有很多不同回答。

因此：需要 Reinforcement Learning（强化学习）。

---

## 4.1 RL 的基本流程

RL 的训练流程如下：

```
Prompt

↓

模型自己生成回答

↓

Reward（奖励）

↓

更新模型参数

↓

生成更好的回答
```

奖励可以来自：

- 人工训练出的 Reward Model；
- 自动验证器（Verifier）；
- 是否通过测试（Code Pass）；
- 数学答案是否正确。

因此：模型不再只是模仿老师，而是： **不断寻找能够获得更高 Reward 的回答。**

---

## 4.2 RL 为什么比 SFT 更强？

SFT 训练时, 模型始终看到：正确答案, 这种训练方式称为：Teacher Forcing

但是, 真实推理过程中，模型只能看到：自己刚刚生成的 Token, 如果前面生成错了，后面所有 Token 都可能继续受到影响。

这种训练和推理之间的不一致称为：Exposure Bias

RL 训练时, 模型必须使用：

```
自己的输出

↓

继续生成

↓

观察最终 Reward
```

因此：模型开始学习： **自己的行为会产生什么后果（Consequences）。**

RL 正是用来减少Exposure Bias带来的影响。

---

# 5. Reinforcement Learning 的三个问题

虽然 RL 能提高模型能力，但也存在很多失败模式（Failure Modes）。

---

## 5.1 Reward Hacking

如果：Reward 设计不好，模型可能找到一种：Shortcut（捷径）, 获得高 Reward，

但这并不是我们真正希望的行为。这种现象称为：Reward Hacking. 

例如：如果代码只要求Test Passed, 模型可能专门针对测试样例写代码，而不是学习真正的算法。

---

## 5.2 pass@k → pass@1

很多模型：会尝试很多不同解法。例如：pass@10表示: 生成 10 个答案，只要其中一个正确即可。

RL 往往不断奖励：最成功的那条路径，于是模型越来越倾向pass@1即：始终输出一种固定方案。

虽然成功率可能提高，但是：模型探索不同解法的能力下降，生成结果的多样性也会降低。

---

## 5.3 Hallucinated Reasoning

Reward 通常只评价：最终答案, 而不会检查：推理过程

因此：模型可能：推理过程完全错误，最后却恰好得到正确答案。

这种现象称为：Hallucinated Reasoning

如果长期训练，模型可能逐渐学会：错误推理，但仍然得到高 Reward。

---

# 6. LoRA（Low-Rank Adaptation）

如果需要微调：70B，甚至400B， 参数模型，直接更新全部参数：成本极高。因此提出：LoRA。

---

## 6.1 LoRA 的思想

LoRA 的核心思想非常简单： **冻结（Freeze）原模型，只训练一个很小的 Adapter。**

公式：W' = W + BA

其中：

- \(W\)：原始模型参数（冻结，不更新）
- \(BA\)：低秩矩阵（Low-Rank Adapter）
- \(W'\)：新的权重

训练过程中：只有BA需要更新。因此：训练参数数量大幅减少。

---

## 6.2 为什么 LoRA 更省参数？

假设：原矩阵：d × k， 需要训练：dk个参数。

LoRA把它拆成：

```
B

↓

d × r

A

↓

r × k
```

其中：

```
r << d,k
```

因此：

训练参数变成：r(d+k)， 远远小于：dk

所以：LoRA 能够以极低成本完成模型微调。

---

## 6.3 LoRA 常见超参数

LoRA 通常需要设置三个超参数：

### Rank（r）

Rank 决定 Adapter 的表达能力。

- r 越大，模型容量越强；
- r 越小，训练参数越少。

通常：4~64


---

### Scaling（α）

Scaling 控制：Adapter 更新的影响程度。

实际更新量：α， 通常：α = r或者2r

---

### Adapt 哪些 Layer？

最初 LoRA：只训练Wq，Wv

现代QLoRA：通常训练：Q，K，V，Output Projection， MLP。
Embedding、LayerNorm 等一般保持冻结。

---

# 7. 整个 LLM Pipeline

现代 LLM 的完整流程可以总结为：

```
Transformer Architecture

↓

Pretraining

↓

Scaling Law

↓

Supervised Fine-Tuning

↓

Reinforcement Learning

↓

LoRA Fine-Tuning

↓

Inference

↓

Temperature / Top-p Sampling
```

其中：

- Architecture 决定模型结构；
- Training 决定模型能力；
- Inference 决定生成风格。

---

# 8. The Bitter Lesson

Rich Sutton 在《The Bitter Lesson》中提出：**真正推动 AI 发展的，不是越来越复杂的人工规则，而是通用算法配合更多的数据和计算资源。**

过去几十年：AI 研究大量依赖：

- 专家规则（Expert Systems）
- 特征工程（Feature Engineering）
- 手工设计模型结构

但这些方法最终都被：

- 更多数据（Data）
- 更多计算资源（Compute）
- 更大的模型（Parameters）

所超越。

Transformer 正是这一思想最成功的实践。

它没有依赖复杂的人工规则，而是采用一个统一的 Transformer Block，通过不断扩大模型规模和训练数据，持续获得性能提升。

因此： **不要试图把所有聪明的规则都写进模型，而应该设计一个足够通用的模型，然后让数据和计算资源帮助它不断成长。**

---

# 本章重点

- LLM 的训练通常分为三个阶段：Pretraining、SFT 和 RL。
- Pretraining 使用 Next Token Prediction 学习语言、知识和推理能力；SFT 使用 Instruction-Response 数据学习如何成为助手；RL 则根据 Reward 优化模型行为，使其更符合人类偏好。
- Scaling Law 发现模型性能会随着参数、数据和计算资源增加而持续提升；Chinchilla Law 则指出模型大小和训练数据必须共同增长，固定算力下并不是模型越大越好。
- RL 能够缓解 SFT 的 Exposure Bias，但也可能带来 Reward Hacking、生成多样性下降以及 Hallucinated Reasoning 等问题。
- LoRA 通过冻结原模型，只训练低秩 Adapter，实现低成本模型微调，是目前工业界最广泛使用的 Fine-tuning 技术之一。
- The Bitter Lesson 强调：**AI 的成功来自通用算法与规模化训练，而不是越来越复杂的人工规则设计。**

---

# 一句话总结

> **现代大型语言模型的成功来自一个统一而清晰的训练流程：首先利用海量数据进行 Pretraining 学习语言和知识，再通过 SFT 学会遵循人类指令，通过 RL 根据奖励优化行为，并利用 LoRA 以低成本完成领域微调。整个发展过程都遵循 Scaling Law，即更多参数、更多数据和更多计算能够持续提升模型性能，而这正体现了 The Bitter Lesson 所强调的“通用方法加规模化训练优于人工规则”的核心思想。**
