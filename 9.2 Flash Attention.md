# Transformer 笔记：FlashAttention（Fast and Memory-Efficient Exact Attention）

---

# 一、FlashAttention 到底解决什么问题？

很多同学第一次听 FlashAttention，以为它发明了一种新的 Attention。

其实：**完全不是。** FlashAttention 计算出来的 Attention 与普通 Transformer 完全一样。

它改变的不是数学公式，而是：**数据在 GPU 里的搬运方式（Memory Access）。**

论文发现：Transformer 最大的问题不是：算得慢， 而是：搬数据太慢。

GPU 真正浪费时间的地方不是 Matrix Multiplication。而是：

```
HBM

↓

SRAM

↓

HBM

↓

SRAM
```

不停搬数据。

---

# 二、GPU 为什么这么慢？

GPU 有两种内存：

1. HBM（High Bandwidth Memory）容量很大，但是速度较慢

2. SRAM（Shared Memory）容量很小，但是速度非常快

可以理解成：HBM = 仓库， SRAM = 办公桌， 真正计算只能在：办公桌（SRAM）完成。

如果每算一步都跑仓库是不是特别慢。Transformer 原来的实现就是这样。

---

# 三、普通 Attention 为什么浪费内存？

普通 Attention：

计算：

```
QKᵀ

↓

Softmax

↓

AV
```

例如：N = 4096 那么：QKᵀ就是4096 × 4096矩阵。

它必须：

```
先全部算出来

↓

存进 HBM

↓

再读回来

↓

做 Softmax

↓

再存

↓

再读回来

↓

乘 V
```

因此整个过程：

```
不停：

HBM

↓

SRAM

↓

HBM

↓

SRAM
```

GPU大部分时间都在搬数据而不是做计算。

---

# 四、FlashAttention 的核心思想


1.Tiling

2.Online Softmax

3.Recomputation

三者结合后Attention完全不用保存巨大：N × N矩阵。

---

# 五、第一个思想：Tiling（分块计算）

FlashAttention不会一次计算整个QKᵀ

而是把矩阵切成很多小块。

例如原来：4096×4096

现在切成：128×128小块。

变成一次只算一块。这一块完全放进SRAM算完：立刻继续下一块。

所以GPU几乎不用回HBM。

---

## 为什么叫 Tiling？

Tile：就是瓷砖。整个Attention像铺地板。

不是一次铺整个房间。而是：一块，一块，一块，慢慢铺。因此叫：Tiling

---

# 六、第二个思想：Online Softmax

问题来了。Softmax是不是需要知道整行最大的Score？

例如：2，5，8，1

必须知道：max = 8才能计算：Softmax

以前所以必须等整个：QKᵀ全部算完。

FlashAttention提出：Online Softmax

什么意思？例如：

第一块：2  5  记录：当前max = 5

第二块：8  1  发现：新的max = 8

于是把前面统计自动更新。因此不用保存整个Matrix。

可以边计算，边更新Softmax。所以叫：Online Softmax

这里Online意思就是：Streaming Incrementally逐步计算，而不是网络Online。

---

# 七、第三个思想：Recomputation（重新计算）

<img width="755" height="399" alt="image" src="https://github.com/user-attachments/assets/a0692ea0-dacb-4d82-9b99-1864588358e1" />

很多人第一次看到这里都会觉得奇怪。为什么故意重新算？因为现代GPU一个重要规律：Compute便宜。但是Memory昂贵。

举个例子假设：重新算一次：

Matrix Multiplication：花1ms

但是从HBM读回来：可能5ms，所以论文认为：与其存下来，不如以后需要的时候重新算。

于是FlashAttention：很多中间变量都不保存。

Backward：需要的时候重新计算。因此节省大量显存。

这就是：Recomputation

---

# 八、为什么 FlashAttention 更快？

FlashAttention：真正快不是因为少算了。

实际上它算的东西一模一样。

真正快来自Memory IO减少。

论文提出一个新词：IO-Aware

意思就是：**设计计算方式时，不仅考虑计算量，还要尽量减少 GPU 内存读写。**

现代 GPU真正瓶颈通常不是FLOPs。而是：Memory Bandwidth

FlashAttention正是解决这个问题。

---

# 九、FlashAttention 与普通 Attention 对比

| 普通 Attention | FlashAttention |
|---------------|----------------|
| 一次计算整个 QKᵀ | 分块（Tiling）计算 |
| 保存完整 Attention Matrix | 不保存完整 Matrix |
| Softmax 需要等待全部 Score | 使用 Online Softmax 边计算边更新 |
| Backward 保存大量中间变量 | Backward 重新计算（Recomputation） |
| HBM 与 SRAM 来回搬运很多次 | 尽量在 SRAM 内完成计算 |
| IO 很大 | IO 很小 |
| 显存占用高 | 显存占用低 |
| 数学结果正确 | 数学结果完全一样（Exact） |

---

# 十、为什么叫 FlashAttention？

Flash：并不是改变Attention。而是像Flash Memory一样高速。

因此FlashAttention：真正强调的是：Fast，Memory Efficient，Exact

论文标题就是：Fast and Memory-Efficient Exact Attention

其中：**Exact** 非常重要。

它不是pproximate Attention。不是Sparse Attention。不是Linear Attention。

FlashAttention计算出来和普通Attention完全一样。

只是计算顺序不同。

---

# 十一、现代 LLM 为什么都用 FlashAttention？

几乎所有现代大模型：

- GPT-4
- Claude
- Gemini
- Llama
- DeepSeek
- Qwen
- Mistral

训练：

都会：

使用：

FlashAttention。

因为：

对于：

```
4K

8K

32K

128K

Context
```

Attention：

已经：

成为：

最大的：

显存：

瓶颈。

FlashAttention几乎已经成为Transformer标准实现。

---

# 本章重点

- FlashAttention 并没有改变 Transformer 的数学公式，而是重新设计了 Attention 的计算顺序。
- FlashAttention 的目标是减少 GPU 内存读写（Memory IO），因为现代 GPU 的瓶颈通常来自 Memory Bandwidth，而不是计算能力。
- FlashAttention 的三个核心思想分别是：**Tiling**（将 Attention Matrix 分块计算，使每块都能放入高速 SRAM）、**Online Softmax**（边计算边维护 Softmax 所需统计量，无需保存完整 Score Matrix）以及 **Recomputation**（反向传播时重新计算中间结果，用计算换显存）。
- FlashAttention 不需要保存完整的 \(N \times N\) Attention Matrix，因此显著降低显存占用，同时减少 HBM 与 SRAM 之间的数据搬运，提高 GPU 利用率。
- FlashAttention 计算得到的结果与普通 Attention **完全一致（Exact）**，只是计算过程更加高效，因此已经成为现代 GPT、Llama、Claude、Gemini、DeepSeek 等大型语言模型训练和推理中的标准实现。

---

# 一句话总结

> **FlashAttention 不是一种新的 Attention，而是一种更加高效的计算方式。它利用 Tiling、Online Softmax 和 Recomputation 三个关键思想，将原本需要频繁访问 GPU 大内存的 Attention 计算尽可能放到高速 SRAM 中完成，从而减少 Memory IO，在保证计算结果完全一致的前提下，大幅降低显存占用并提升训练和推理速度。**

